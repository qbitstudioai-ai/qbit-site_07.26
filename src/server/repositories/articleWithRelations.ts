import { assertLegacySectionOnUpdate } from "../content/legacySectionGuard";
import { transaction } from "../db/client";
import {
  getArticleById,
  updateArticleCore,
  type ArticleInput,
  type ArticleRecord,
} from "./articles";
import {
  ContentRelationError,
  listRelationsFrom,
  replaceRelationsFromCore,
  type ContentRelation,
  type ContentRelationInput,
} from "./contentRelations";

/**
 * Сохранение статьи вместе с её связями — ОДНОЙ транзакцией.
 *
 * Модуль существует ради одного свойства, которого нет ни у одного из двух репозиториев по
 * отдельности: правка текста статьи, замена её перелинковки и вывод прежней колонки
 * `articles.related_slugs` либо применяются целиком, либо не применяются вовсе. Порознь их вызвать
 * можно и сегодня, но между вызовами есть щель: отказ на связях оставил бы уже сохранённый текст с
 * прежним списком связей, а отказ на середине списка — статью вообще без перелинковки.
 *
 * ЭТОТ МОДУЛЬ — ЕДИНСТВЕННЫЙ ВЛАДЕЛЕЦ ТРАНЗАКЦИИ в операции. Внутри вызываются только
 * transaction-neutral ядра (`updateArticleCore`, `replaceRelationsFromCore`) и обычные `SELECT`
 * (`getArticleById`, `listRelationsFrom`), никогда обёртки с собственной транзакцией:
 * `transaction()` в проекте намеренно не реентерабельна, и вложенный `BEGIN` не просто падает —
 * его `ROLLBACK` отменяет ВНЕШНЮЮ транзакцию, после чего внешний `COMMIT` падает вторым,
 * бессмысленным сообщением. Проверка на это стоит отдельным тестом.
 *
 * Всё содержимое транзакции СИНХРОННО. `transaction()` не ждёт промисов: один `await` внутри
 * означал бы `COMMIT` до конца работы, то есть ровно ту потерю атомарности, ради которой модуль и
 * написан.
 *
 * Что здесь НЕ делается намеренно: `revalidateSection()`, IndexNow и любые другие внешние эффекты.
 * Они не откатываются вместе с транзакцией, поэтому их место — после успешного возврата, в роуте.
 *
 * ПЕРЕХОДНЫЙ DUAL-WRITE (Amendment 59). Публичный сайт до отдельного шага переключения читает
 * прежнюю колонку `articles.related_slugs`, а редактировать владелец сайта будет структурные связи.
 * Значит, у прежней колонки должен быть ровно один источник — структурные связи, и вычислять его
 * обязан сервер. Присланный клиентом `relatedSlugs` НЕ участвует в записи ни при каком входе:
 * иначе форма могла бы прислать одну структуру в `relations` и другую в `relatedSlugs`, и две
 * модели разошлись бы в пределах одного запроса.
 */

/**
 * Предел прежней модели: не больше шести материалов по теме.
 *
 * Совпадает с `.max(6)` у поля `relatedSlugs` в схеме запроса, но проверяется здесь, а не там.
 * Причина: ограничивать надо ВЫВЕДЕННОЕ подмножество, а его до вывода не существует — схема видит
 * только общий список связей четырёх типов. Репозиторий связей для этого тоже не годится: он
 * универсален по типу источника, и правило про колонку статей ему чуждо.
 */
export const MAX_LEGACY_ARTICLE_TARGETS = 6;

/**
 * Связи статьи после сохранения, ТРИ различимых состояния входа:
 *
 * | `relations`     | `content_relations`  | `articles.related_slugs`                |
 * | --------------- | -------------------- | --------------------------------------- |
 * | `undefined`     | не трогаются         | не трогается: берётся текущее из базы    |
 * | `[]`            | очищаются            | `[]`                                     |
 * | непустой массив | полная замена        | article-подмножество, выведенное сервером |
 *
 * Первое состояние — защита от СТАРОЙ ВКЛАДКИ админ-панели, открытой до выката формы связей. Она
 * шлёт `PUT` без поля `relations`, но с полем `relatedSlugs` — и значение в нём такое, каким оно
 * было в момент открытия вкладки, а не текущее. Записать его означало бы откатить чужую правку
 * перелинковки, оставив `content_relations` нетронутой, то есть развести две модели именно тем
 * механизмом, который их сводит. Поэтому в этой ветке колонка перезаписывается САМА В СЕБЯ:
 * значением, прочитанным из базы в этой же транзакции.
 *
 * Возврат связей БЕЗУСЛОВНЫЙ, в том числе в первой ветке. Форма админ-панели делает ответ сервера
 * новым состоянием и новым baseline (`useEditableForm.save()`), поэтому ответ без связей стирал бы
 * список на экране после первого же сохранения. «Ничего не менялось» и «связей нет» для клиента
 * обязаны выглядеть по-разному, и различает их не форма ответа, а его содержимое.
 */
export interface UpdateArticleWithRelationsResult {
  article: ArticleRecord;
  /** Итоговые связи статьи. Всегда фактическое состояние базы после операции. */
  relations: ContentRelation[];
}

/**
 * Прежняя перелинковка, выведенная из структурных связей.
 *
 * Порядок — пользовательский: `filter` сохраняет позиции исходного массива, а позиция в массиве и
 * есть порядок, который составил человек (явный `sortOrder` форма не присылает).
 *
 * Адрес цели берётся ТОЛЬКО из строки базы по идентификатору. Присланный клиентом адрес не
 * используется нигде: идентификатор стабилен, адрес владелец сайта меняет из админ-панели, и
 * доверие к присланному адресу означало бы, что перелинковку можно указать мимо структурных связей.
 *
 * Три отказа — про пригодность цели для ПУБЛИЧНОГО блока, а не про связь как таковую. Сегодня
 * блок «материалы по теме» показывает только опубликованные статьи ТОГО ЖЕ раздела и молча
 * отбрасывает всё остальное (с REL-02F.2 — `listPublishedArticleRelatedMaterials` отбирает только
 * опубликованные статьи того же раздела). Пока прежняя колонка остаётся публичным источником, связь, которую этот блок
 * заведомо не покажет, — не связь, а тихая пропажа. Поэтому вход проверяется явно и отказом.
 *
 * Раздел сверяется с `input.placement`, а НЕ с текущим разделом статьи: если в этом же сохранении
 * статью переносят, целями обязаны быть материалы раздела НАЗНАЧЕНИЯ. Сверка со старым разделом
 * сохранила бы связи, которые публичный блок перестал бы показывать сразу после переноса.
 *
 * Ссылку на себя и повтор цели здесь НЕ проверяют: их ловит `replaceRelationsFromCore()` теми же
 * кодами, что и прежде. Отказ там наступает уже после `UPDATE` статьи, но внутри той же транзакции,
 * поэтому откат полон в обоих случаях.
 *
 * Из этого следует единственное исключение ниже: САМА ИСХОДНАЯ СТАТЬЯ пропускается мимо проверок
 * публикации и раздела. Без него черновик, сославшийся сам на себя, получал бы отказ «цель не
 * опубликована» — формально верный и совершенно бесполезный: настоящая ошибка в том, что ссылка
 * ведёт на саму статью, и сообщить об этом должен `self_link`. Исключение не ослабляет проверку:
 * такая связь всё равно не доживёт до `COMMIT`.
 */
function deriveLegacySlugs(
  sourceId: string,
  relations: readonly ContentRelationInput[],
  placement: string,
): string[] {
  const targets = relations.filter((relation) => relation.targetType === "article");

  if (targets.length > MAX_LEGACY_ARTICLE_TARGETS) {
    throw new ContentRelationError(
      "too_many_legacy_targets",
      `Статей в блоке «материалы по теме» не больше ${MAX_LEGACY_ARTICLE_TARGETS}, ` +
        `а выбрано ${targets.length}`,
    );
  }

  return targets.map((relation) => {
    const target = getArticleById(relation.targetId);

    if (!target) {
      throw new ContentRelationError(
        "missing_entity",
        `Материал «article:${relation.targetId}» не существует (цель связи)`,
      );
    }

    // Ссылка на себя: отдать адрес и не мешать `replaceRelationsFromCore()` назвать причину.
    if (relation.targetId === sourceId) return target.slug;

    if (target.status !== "published") {
      throw new ContentRelationError(
        "unpublished_target",
        `Статья «${target.title}» не опубликована и не может стоять в материалах по теме`,
      );
    }

    if (target.placement !== placement) {
      throw new ContentRelationError(
        "placement_mismatch",
        `Статья «${target.title}» относится к другому разделу сайта и не появилась бы в блоке ` +
          `«материалы по теме»`,
      );
    }

    return target.slug;
  });
}

export function updateArticleWithRelations(
  id: string,
  input: ArticleInput,
  relations?: readonly ContentRelationInput[],
): UpdateArticleWithRelationsResult {
  return transaction(() => {
    // Обычный `SELECT` по первичному ключу: собственной транзакции у него нет, вложенного `BEGIN`
    // не возникает. Чтение идёт до записи и в том же снимке данных, что и всё остальное.
    const previous = getArticleById(id);

    /**
     * Скрытая legacy-секция «Материалы по теме» (REL-02F.2) сверяется с ТЕМ ЖЕ снимком `previous`,
     * по которому идёт запись, и ДО любой записи. Проверка в роуте по прочитанной до транзакции
     * статье пропустила бы правку секции, сделанную между чтением и записью в соседней вкладке.
     * Отказ — `LegacySectionError`; транзакция откатывается, ничего не записано.
     */
    if (previous) assertLegacySectionOnUpdate(previous.bodyMarkdown, input.bodyMarkdown);

    // Проверка на `undefined`, а не на длину: пустой массив — это явная очистка, и она обязана
    // дойти и до `DELETE`, и до обнуления прежней колонки.
    if (relations === undefined) {
      /**
       * Статьи нет — вход передаётся как есть, и `updateArticleCore()` бросит свою привычную
       * ошибку «не найдена». Заводить здесь второй, свой отказ незачем: роут отвечает 404 ещё до
       * транзакции, а два разных сообщения об одном и том же состоянии только запутывали бы.
       */
      const preserved = previous ? { ...input, relatedSlugs: previous.relatedSlugs } : input;
      const article = updateArticleCore(id, preserved);
      return { article, relations: listRelationsFrom("article", id) };
    }

    const relatedSlugs = deriveLegacySlugs(id, relations, input.placement);
    const article = updateArticleCore(id, { ...input, relatedSlugs });

    return { article, relations: replaceRelationsFromCore("article", id, relations) };
  });
}
