# DECISIONS

## Decision log

Каждая запись должна содержать:

- дата;
- контекст;
- варианты;
- принятое решение;
- последствия;
- skeptic review;
- согласование пользователя.

---

### 2026-07-14 — Package manager

- Контекст: Step 1 требует зафиксировать инструмент установки зависимостей; docs/09-technical-architecture.md выбора не делает.
- Варианты: npm, pnpm, yarn.
- Решение: npm (соответствует `npm run ...` в CLAUDE.md quality gate).
- Последствия: `package-lock.json` — единственный lock-файл репозитория.
- Skeptic review: пройден в рамках review плана Step 1 (замечание о преждевременности decision снято явным решением пользователя).
- Согласование пользователя: да, в рамках утверждения плана Step 1 (2026-07-14).

---

### 2026-07-14 — Test runner

- Контекст: docs/09-technical-architecture.md оставляет открытым выбор Vitest/Jest для unit-тестов.
- Варианты: Vitest, Jest.
- Решение: Vitest.
- Последствия: `vitest.config.ts`, unit-тесты пишутся под Vitest API.
- Skeptic review: пройден в рамках review плана Step 1.
- Согласование пользователя: да, в рамках утверждения плана Step 1 (2026-07-14).

---

### 2026-07-14 — Scope Step 1: design tokens включены, CI pipeline исключён

- Контекст: docs/13-implementation-plan.md, "Этап 0", включает CI и design tokens в состав репозиторного фундамента; CLAUDE.md quality gate явно не упоминает CI. Skeptic-review плана указал на нерешённый конфликт (FAIL, пункт 3).
- Варианты: (a) включить оба в Step 1; (b) включить только design tokens; (c) исключить оба.
- Решение: design tokens — в scope Step 1 (базовая заготовка, без финального арт-дирекшна); CI pipeline — вне scope Step 1, переносится в отдельный будущий шаг сверх текущих 8 (на момент записи; после Amendment 3, см. `DECISIONS.md` 2026-07-15 "Step 5: разбиение на два отдельных шага WORKPLAN (OQ-D, Amendment 3)" — сверх текущих 9).
- Последствия: WORKPLAN.md Step 1 явно перечисляет design tokens в in scope и CI pipeline в out of scope; CI появится отдельным пунктом плана позже.
- Skeptic review: конфликт был поднят skeptic как FAIL-блокер плана; решение принято пользователем, а не единолично planner'ом.
- Согласование пользователя: да, явный выбор через AskUserQuestion при утверждении плана Step 1 (2026-07-14).

---

### 2026-07-14 — README.md как упрощённый вид WORKPLAN.md

- Контекст: пользователь запросил вести статус шагов в README.md тремя метками ("Не приступили"/"В работе"/"Выполнено"). CLAUDE.md фиксирует обязательный 8-значный статусный enum для WORKPLAN.md (`PROPOSED/APPROVED/IN_PROGRESS/AWAITING_SKEPTIC/FAILED_REVIEW/BLOCKED/PASSED/COMPLETED`). Skeptic-review плана указал на риск двух несвязанных словарей состояния (FAIL, пункт 1).
- Варианты: (a) README — независимый словарь из 3 меток; (b) README — упрощённый вид WORKPLAN.md с явным mapping на 8-значный enum.
- Решение: (b). Таблица mapping зафиксирована в README.md. WORKPLAN.md остаётся единственным источником истины по фактическому статусу; README.md обновляется синхронно при каждом изменении статуса в WORKPLAN.md (входит в acceptance criteria всех шагов, не только Step 1).
- Последствия: обновление статуса шага требует правки обоих файлов одним коммитом.
- Skeptic review: конфликт поднят skeptic как FAIL-блокер плана; решение принято пользователем.
- Согласование пользователя: да, явный выбор через AskUserQuestion при утверждении плана Step 1 (2026-07-14).

---

### 2026-07-14 — MANIFEST.json ссылается на несуществующий README.md (known issue)

- Контекст: `MANIFEST.json` перечисляет `README.md` с конкретным `sha256`, но на момент начала Step 1 файл `README.md` в корне репозитория отсутствовал (создаётся только сейчас, в Step 1, с новым содержимым — хеш неизбежно не совпадёт с зафиксированным в манифесте).
- Варианты: (a) не трогать MANIFEST.json в Step 1, зафиксировать как known issue; (b) регенерировать MANIFEST.json целиком; (c) удалить запись о README.md из MANIFEST.json.
- Решение: (a) — MANIFEST.json не входит в scope Step 1. Известное расхождение фиксируется здесь как known issue без владельца-шага; требует отдельного решения пользователя в будущем (регенерация манифеста или отказ от его сопровождения).
- Последствия: `MANIFEST.json` временно не отражает фактическое состояние репозитория (README.md и, возможно, другие изменённые файлы).
- Skeptic review: замечание отражено в review плана как "hidden risk" без требования блокировать Step 1 по этой причине.
- Согласование пользователя: да, неявно — вопрос не был вынесен как блокирующий в финальном списке решений, пользователь утвердил план, содержащий этот пункт как "не исправляется в Step 1".

---

### 2026-07-14 — Инициализация git в рамках Step 1

- Контекст: репозиторий на момент планирования не был git-репозиторием, что блокировало commit/rollback-протокол CLAUDE.md ("Commit changes with git", деструктивные операции только с подтверждением). Skeptic отметил это как legitimate escalation, не как причину провалить план, но исполнение без git не соответствовало бы протоколу.
- Варианты: (a) инициализировать git как часть Step 1, до первого `npm install`; (b) отложить git до отдельного шага; (c) работать без git на первых шагах с ручным rollback через восстановление файлов.
- Решение: (a). `git init` выполнен, baseline-коммит `4080a7b` зафиксировал состояние репозитория до начала изменений Step 1.
- Последствия: rollback Step 1 (если потребуется) выполняется через `git revert`/`git reset` относительно `4080a7b`, а не через ручное восстановление файлов.
- Skeptic review: соответствует разделу "Hidden risks" review плана Step 1.
- Согласование пользователя: да, явный выбор через AskUserQuestion при утверждении плана Step 1 (2026-07-14).

---

### 2026-07-14 — Расположение Playwright-тестов внутри `src/tests`

- Контекст: skeptic review плана указал (Major, пункт 8), что план вводил отдельный верхнеуровневый `e2e/`, не описанный в docs/09-technical-architecture.md ("Возможная структура" перечисляет единственный `src/tests/` для всех тестов).
- Варианты: (a) top-level `e2e/` (стандартная конвенция Playwright, но расходится с docs/09); (b) `src/tests/e2e/` (соответствует документированной структуре).
- Решение: (b) — `src/tests/unit/` для Vitest, `src/tests/e2e/` для Playwright; `playwright.config.ts` указывает `testDir: 'src/tests/e2e'`.
- Последствия: структура каталогов соответствует docs/09 без необходимости менять документацию или получать отдельное одобрение на отклонение.
- Skeptic review: устраняет замечание Major #8 из review плана Step 1.
- Согласование пользователя: не требовалось отдельно — реализует уже одобренный принцип "не выходить за рамки задокументированной структуры" без изменения scope.

---

### 2026-07-14 — Порт разработки/e2e зафиксирован на 3100, host — localhost (Plan Amendment 1)

- Контекст: при первом прогоне `npm run test:e2e` тест прошёл, но проверил совершенно другой,
  посторонний сайт ("Qbit-Studio-AI"), потому что порт 3000 на машине разработки уже занят чужим
  Next.js-процессом, а `playwright.config.ts` (`reuseExistingServer: !CI`) молча переиспользовал
  уже слушающий сервер вместо запуска своего. Skeptic (review исполнения шага) вернул FAIL и
  потребовал: (1) явную amendment-запись вместо тихой правки уже одобренного раздела Step 1
  (нарушение non-negotiable правила CLAUDE.md); (2) устранить обнаруженную при этом реальную
  консольную ошибку в dev-режиме при host `127.0.0.1` (Next.js 16 `allowedDevOrigins` блокирует
  HMR-websocket для числового IP), которой нет при host `localhost`.
- Варианты порта: (a) оставить порт 3000 по умолчанию (рискует повторно зацепить чужой процесс);
  (b) зафиксировать другой порт (3100); (c) preflight-проверка занятости порта перед стартом или
  динамически выделяемый свободный порт.
  Варианты host: (a) `127.0.0.1` (провоцирует HMR cross-origin ошибку в dev); (b) `localhost`
  (чисто в dev и prod, подтверждено headless-проверкой консоли браузера).
- Решение: порт 3100 (вариант b); host `localhost` везде — `package.json` (`next dev -p 3100`,
  `next start -p 3100`), `playwright.config.ts` (`baseURL`/`webServer.url` —
  `http://localhost:3100`), `README.md`, `WORKPLAN.md` (manual checks). Вариант (c) для порта не
  реализован сейчас — оставлен как нерешённый остаточный риск без владельца-шага (см. WORKPLAN.md
  Step 1, Risks), чтобы не расширять scope Step 1.
- Последствия: `npm run dev`/`npm run start`/`npm run test:e2e` этого проекта используют
  `localhost:3100`, а не порт Next.js по умолчанию. Если порт 3100 в будущем тоже окажется занят
  посторонним процессом, `reuseExistingServer` может повторить тот же класс ошибки — потребуется
  либо preflight-проверка, либо переход на динамический порт в отдельном будущем шаге.
- Skeptic review: 1-й review — FAIL (среди прочего: нет amendment-записи). После добавления
  amendment с самостоятельно проставленным `Status: APPROVED` — 2-й review вернул **BLOCKED**:
  исполнитель не вправе сам себя одобрять за пользователя, даже для технической правки внутри
  уже одобренного scope. Статус исправлен на `AWAITING_USER_APPROVAL`, вопрос задан пользователю.
- Согласование пользователя: **получено** 2026-07-14 ("Да, утверждаю порт 3100 / localhost").
  Остаточный риск (порт 3100 тоже может быть занят в будущем) пользователь решил оставить known
  issue без owner-шага — не назначать отдельным шагом плана, решать по факту при повторении.

---

### 2026-07-14 — Step 2: runtime-валидатор контента — zod

- Контекст: Step 2 ("Typed content model") требует не только TypeScript-типов, но и runtime-
  валидации существующих JSON-данных (`In scope: ... validation tests`). Planner предложил как
  рекомендацию zod, вынеся выбор как open question — skeptic (review плана, BLOCKED) указал, что
  planner уже зашил zod как факт в Expected files/In scope, притворяясь что вопрос открыт;
  вопрос задан пользователю напрямую до фиксации плана.
- Варианты: (a) zod — TS-first схема-валидация с выводом типов; (b) самописные type guards без
  новой зависимости.
- Решение: (a) zod.
- Последствия: новая npm-зависимость (`package.json`/`package-lock.json`). Adapter-функции
  `src/content/*.ts` должны оставаться server-only (не импортироваться в `'use client'`-компоненты
  напрямую) — иначе zod и валидация попадут в client-бандл (Performance rules CLAUDE.md); это
  зафиксировано как явное архитектурное ограничение Step 2, а не просто риск.
- Skeptic review: соответствует Required correction #2 из BLOCKED-review плана Step 2.
- Согласование пользователя: получено 2026-07-14 напрямую ("zod (рекомендую)" — выбран пользователем).

---

### 2026-07-14 — Step 2: границы шага — все три JSON-файла главной страницы

- Контекст: буквальный текст `WORKPLAN.md` ограничивал Step 2 "пятью отделами"
  (`departments.json`). Planner предложил расширить на `homepage-copy.json` и `office-zones.json`
  (нужны Step 3), снова вынеся это как open question, но уже зашив в Expected files/Acceptance
  criteria — skeptic (BLOCKED) указал на то же смешение факта и вопроса; решение запрошено у
  пользователя напрямую.
- Варианты: (a) только `departments.json`; (b) все три существующих JSON-файла главной страницы.
- Решение: (b) — все три файла (`departments.json`, `homepage-copy.json`, `office-zones.json`)
  типизируются и валидируются в Step 2.
- Последствия: Step 2 включает типы/схему/adapter для `HomepageCopy` и `OfficeZone` (у которых,
  в отличие от `Department`, нет отдельного документированного контракта в `docs/12` — структура
  де-факто фиксируется по существующим данным, это тоже часть данного решения). Acceptance
  criteria Step 2 включают cross-consistency между `office-zones.json` и `departments.json`.
- Skeptic review: соответствует Required correction #2 из BLOCKED-review плана Step 2.
- Согласование пользователя: получено 2026-07-14 напрямую ("Все три файла (рекомендую)" — выбран
  пользователем).

---

### 2026-07-14 — Step 2: расхождение docs/12 vs data/departments.json — типизировать «как есть»

- Контекст: `docs/12-content-data-model.md` описывает для `Department` поля `beforeSteps`,
  `automationSteps` (структурированные `ProcessStep[]`) и полноценный `visual`
  (`DepartmentVisual` — hotspot/overview/detail/fallback/camera). Ни одного из этих полей нет в
  `data/departments.json` — там только `id, name, overviewLabel, overviewProblem, headline,
  problem, symptoms[], outcomes[], ctaLabel, solutionPath, reference`(строка-путь к PNG). Planner
  предложил не решать это в Step 2, отложив "не позднее Step 4" — skeptic (BLOCKED) указал, что
  сама implementation усечённой типизации в Step 2 уже фиксирует расхождение с approved-документом
  в коде (нарушение CLAUDE.md "Report conflicts before implementation" без реального resolution),
  и что откладывание решения — тот же паттерн самопровозглашённого решения, что и в Step 1. Вопрос
  задан пользователю напрямую, без отсрочки.
- Варианты: (a) типизировать «как есть» (только реально существующие поля), зафиксировав
  расхождение с `docs/12` как known issue в этой записи; (b) сначала дополнить
  `data/departments.json` полями `beforeSteps`/`automationSteps`/`visual` по `docs/12` — контентная
  задача, расширяющая scope и сроки.
- Решение: (a) — типизировать «как есть». `docs/12` в части `beforeSteps`/`automationSteps`/
  `visual` для отделов **не соответствует** текущим данным; это принятое, осознанное known issue,
  а не молчаливое противоречие — зафиксировано здесь по прямому решению пользователя, не отложено
  без даты/владельца на будущий шаг.
- Последствия: `src/content/types.ts`/`schema.ts` Step 2 не включают `beforeSteps`,
  `automationSteps`, полный `visual` — только поля, реально присутствующие в данных. На момент
  этой записи — Step 5 ("Desktop 10/90 shell"); **после Amendment 3 (см. запись "Step 5:
  разбиение на два отдельных шага WORKPLAN (OQ-D, Amendment 3)" ниже) — Step 5 ("Department
  selection state machine") и Step 6 ("Desktop 10/90 shell")** — любой из них, показывающий
  "текущий процесс/сценарий автоматизации" по Core concept CLAUDE.md, не сможет полагаться на
  структурированные `beforeSteps`/`automationSteps` из типизированной модели, пока
  `data/departments.json` не будет дополнен — это ограничение будет явно видно в типах (полей
  просто нет), а не скрыто.
- Skeptic review: соответствует Required correction #3 из BLOCKED-review плана Step 2 (решение
  получено, не отложено).
- Согласование пользователя: получено 2026-07-14 напрямую ("Типизировать «как есть» (рекомендую)"
  — выбран пользователем).

---

### 2026-07-14 — Step 3: CSS-подход — CSS Modules

- Контекст: `docs/09-technical-architecture.md` оставляет открытым выбор "CSS Modules или
  Tailwind". Planner при первом черновике плана Step 3 не зашил выбор как факт (в отличие от
  прецедентов Step 1/2), а явно вынес его пользователю как единственный Blocker. Влияет на
  инструментарий шагов 4–8 (на момент записи; после Amendment 3, см. ниже, — 4–9), не только
  Step 3.
- Варианты: (a) CSS Modules — без новых зависимостей, нативная поддержка Next.js; (b) Tailwind —
  новые зависимости и конфиг уже в Step 3, быстрее для сложной адаптивной вёрстки Steps 5–8 (на
  момент записи; после Amendment 3 — 5–9).
- Решение: (a) CSS Modules.
- Последствия: `*.module.css` рядом с каждым компонентом; никаких новых зависимостей в
  `package.json` для стилизации.
- Skeptic review: подтверждено как единственный реально открытый вопрос (Positive note в review
  плана Step 3) — не смешан с решённым фактом.
- Согласование пользователя: получено 2026-07-14 напрямую ("CSS Modules (рекомендую)").

---

### 2026-07-14 — Step 3: показ overviewProblem — CSS-раскрытие по hover/focus

- Контекст: skeptic (review плана Step 3, BLOCKED) обнаружил, что planner тихо решил показывать
  `overviewProblem` статично всегда для всех 5 отделов одновременно, что прямо противоречит
  `docs/05-homepage-state-machine.md` (`overview` показывает "весь офис, hero, CTA, пять отделов и
  подсказка" — без problem-текста; `department-preview`, запускаемое hover/focus, явно говорит
  "появляется короткая проблема") и подсказке `interactionHint` в `data/homepage-copy.json`
  ("Наведите курсор на отдел"). Это архитектурно-контентное решение с реальными последствиями для
  доступности/UX, а не тривиальная деталь — вынесено пользователю напрямую.
- Варианты: (a) показывать `overviewProblem` статично всегда для всех 5 зон одновременно
  (изначальный вариант planner'а — противоречит docs/05); (b) CSS-раскрытие по hover/focus
  (`:hover`/`:focus-within`), без JS, с сохранением доступности для клавиатуры/скринридеров;
  (c) не показывать `overviewProblem` вообще в Step 3, целиком отложить в Step 4 вместе с
  остальным `department-preview`.
- Решение: (b) — CSS-раскрытие по hover/focus. Соответствует `docs/05` и `interactionHint`;
  реализуется чистым CSS без JS-состояния, не нарушая уже принятое решение "все компоненты Step 3
  — Server Components".
- Последствия: `DepartmentHotspot` должен обеспечить программную доступность `overviewProblem`
  для клавиатуры (`:focus-within`) и скринридеров (текст присутствует в DOM/accessible tree,
  не только визуально скрыт через `display:none`, чтобы соответствовать "pointer и keyboard
  приводят к одинаковым смысловым состояниям" из инвариантов `docs/05`), а не только для мыши.
  Acceptance criteria Step 3 обновляются: не "overviewProblem виден всегда", а "overviewProblem
  доступен через hover ИЛИ focus, идентично для мыши и клавиатуры".
- Skeptic review: соответствует Required correction #2 из BLOCKED-review плана Step 3.
- Согласование пользователя: получено 2026-07-14 напрямую ("CSS-раскрытие по hover/focus
  (рекомендую)").

---

### 2026-07-14 — Step 3: импорт tokens.css в globals.css (Plan Amendment 2)

- Контекст: Step 1 явно зафиксировал (`src/styles/tokens.css`, комментарий-заголовок; см. также
  `WORKLOG.md` Entry 1): "не импортируется в globals.css до финального арт-дирекшна". Planner при
  черновике плана Step 3 предложил тихо пересмотреть это, включив импорт в "решения без вопроса".
  Skeptic (review плана Step 3, BLOCKED) указал, что это — изменение ранее зафиксированного и
  утверждённого инварианта, требующее формального `Plan amendments` и реального согласования
  пользователя (CLAUDE.md: "Never change the plan after approval without recording and approving
  the amendment"), а не решения исполнителя задним числом — тот же класс нарушения, что уже
  приводил к BLOCKED в Step 1 (Amendment 1).
- Варианты: (a) импортировать `tokens.css` в `globals.css` уже в Step 3 (нейтральные токены
  достаточно готовы для low-fidelity прототипа); (b) оставить неимпортированным до отдельного
  art-direction milestone, использовать в Step 3 только минимальные inline/CSS-правила без
  токенов.
- Решение: (a) — импортировать сейчас.
- Последствия: `src/app/globals.css` получает `@import` токенов; комментарий-заголовок
  `tokens.css` обновляется (более не "ждёт арт-дирекшна"). См. `WORKPLAN.md` → `Plan amendments`
  → `Amendment 2` для формальной записи изменения scope.
- Skeptic review: соответствует Required correction #3 из BLOCKED-review плана Step 3 — на этот
  раз оформлено как явный amendment с реальным согласованием ДО начала реализации Step 3 (не
  постфактум, как было с портом 3100 в Step 1).
- Согласование пользователя: получено 2026-07-14 напрямую ("Да, импортировать сейчас").

---

### 2026-07-14 — Изменение docs/05: отделы раскрываются по клику CTA, не видны с самого начала

- Контекст: пользователь посмотрел живой результат Step 3 (`npm run dev`) и указал, что ожидал
  другой сценарий: сначала только hero поверх фото офиса, пять отделов появляются только после
  клика на основную CTA ("Найти потери в моём бизнесе"). Это прямо противоречило уже утверждённому
  `docs/05-homepage-state-machine.md`, где состояние `overview` описывало все пять отделов как
  видимые одновременно с hero и CTA с самого начала. Расхождение между source-of-truth документом
  и явно высказанным пользователем намерением — типичный случай "Report conflicts before
  implementation" из CLAUDE.md; решение не могло быть принято исполнителем самостоятельно.
- Варианты: (a) оставить `docs/05` как есть, отделы видны сразу (текущая реализация Step 3);
  (b) добавить новое состояние `hero` перед `overview`: сначала виден только hero-блок поверх
  фона офиса, клик по любой CTA раскрывает `overview` с пятью отделами.
- Решение: (b). `docs/05-homepage-state-machine.md` обновлён: добавлено состояние `hero` (общая
  схема, описание, invariant про progressive enhancement без JS), событие `ACTIVATE_CTA`.
  `overview` как состояние и его описание не менялись — просто стало недостижимо напрямую от
  `boot`, только через `hero`. Прямой URL `?department=<id>` пропускает `hero` (не требует лишнего
  клика при шаринге ссылки).
- Последствия для реализации:
  - Раскрытие по клику — это client-side state transition (`hero` → `overview`), то есть
    относится к Step 4 ("Homepage state machine"), а не к Step 3 ("Semantic office overview",
    явно ограниченный server components и no-op кликом). Step 3 не переделывается ради этого:
    без JavaScript (fallback/progressive enhancement) `hero` и `overview` рендерятся вместе
    статично — это ровно то, что Step 3 уже реализует (все пять отделов в DOM с самого начала,
    доступны без JS). Step 4 добавит JS-переключение (по умолчанию — скрыть, раскрыть по клику).
  - И основная, и вторичная CTA запускают `ACTIVATE_CTA` (обе ведут к одному и тому же раскрытию
    на этом этапе продукта, пока не реализована диагностика — `docs/13` "Этап 8", макро-фаза
    документа, а не WORKPLAN-шаг) — техническое решение исполнителя, не требующее отдельного
    вопроса (низкий риск, легко перепривязать к диагностике
    позже).
  - `docs/04-homepage-copy.md` не менялся — вся копия (`headline`/`subheadline`/`interactionHint`
    и т.д.) остаётся релевантной, просто `interactionHint` семантически принадлежит состоянию
    `overview` (после раскрытия), а не `hero`.
- Skeptic review: _(будет запрошен вместе с планом-дополнением к Step 3/Step 4)_.
- Согласование пользователя: получено 2026-07-14 напрямую ("Отделы появляются по клику CTA").
  Отдельно пользователь подтвердил: порядок работы остаётся прежним (low-fidelity Steps 1–8, на
  момент этой записи; после Amendment 3, см. ниже, — Steps 1–9 → затем отдельный этап art
  direction с реальным фото офиса) — "Продолжать по плану (low-fi → art direction)", без ускорения
  art-direction milestone.

---

### 2026-07-14 — Step 3: исправление отсутствия full-viewport без скролла

- Контекст: `docs/08-responsive-behavior.md` явно требует для Desktop ≥1280: "полноэкранный офис
  … без вертикального скролла, кроме аварийного режима по высоте" — то же самое в Core concept
  CLAUDE.md. Это требование не было явно включено в acceptance criteria плана Step 3 (упущение
  planner'а и обоих раундов skeptic review плана/исполнения — никто явно не сверил verstka с
  docs/08). Пользователь обнаружил это сам, посмотрев `npm run dev`: измерено 1280px контента в
  вьюпорте 900px при 1440×900 — то есть страница реально скроллится, что нарушает документ.
- Варианты: не рассматривались как развилка — это однозначный баг относительно уже утверждённого
  требования, не архитектурный выбор. Требует исправления verstka Step 3 независимо от решения
  выше про `hero`/`overview`.
- Решение: зафиксировать как обязательную доработку Step 3 (не новый шаг, не самостоятельная
  архитектурная развилка) — привести раскладку к 100dvh без вертикального скролла на десктопе.
- Последствия: `HomepageShell`/`OfficeExperience`/`OfficeSemanticMap` пересматриваются so that
  шапка + hero + карта офиса вместе помещаются в высоту viewport; карта офиса переходит с
  `aspect-ratio`-блока на flex-заполнение оставшейся высоты.
- Skeptic review: **1-й раунд (после этой правки) — `FAIL`.** Независимая проверка нашла: (a)
  Blocker — обновлённый `docs/05` (см. запись выше про `hero`/`ACTIVATE_CTA`) описывал no-JS
  fallback формулировкой, вводящей в заблуждение о текущем поведении кода при включённом
  JavaScript (создавалось впечатление, что JS-путь уже отличается от no-JS, хотя реализация
  раскрытия по клику ещё не существует — это Step 4); (b) Major — сам no-scroll фикс не имел
  нижнего предела высоты: хотспоты сжимались пропорционально без ограничения, на 1280×500 один
  хотспот измерен в 24.7px (граница читаемости touch-target), при этом `docs/08` прямо описывает
  требуемое поведение для этого случая ("Низкий desktop": уменьшить масштаб, разрешить внутренний
  скролл, не обрезать заголовок/навигацию/CTA) — фикс не реализовывал ни один из этих трёх пунктов,
  и это не было раскрыто как компромисс нигде в `WORKPLAN.md`/`WORKLOG.md`/здесь; (c) Major — эта
  запись (текущая, до правки) сформулировала "согласование не требовалось" достаточно широко, чтобы
  прикрыть и то, что фактическая реализация *как именно* достигнут no-scroll (жёсткий
  `overflow:hidden` без исключения для низкой высоты) молча закрыла часть того же требования
  docs/08, которое цитировалось как обоснование. Оба этих находки требовали исправления перед
  повторным review — см. записи ниже ("docs/05: честность..." и "low-height fallback...").
- Согласование пользователя: не требовалось отдельно для *цели* (зафиксировать no-scroll — уже
  утверждённое требование docs/08). Однако конкретный способ реализации низкого-высотного случая
  (аварийный режим) оказался развилкой, которую нужно было решить пользователю явно — см.
  следующую запись.

---

### 2026-07-14 — Step 3: honesty-правка docs/05 после skeptic FAIL

- Контекст: skeptic (1-й раунд review после no-scroll фикса) указал Blocker: раздел `hero` в
  `docs/05-homepage-state-machine.md` описывал no-JS fallback формулировкой ("раскрытие недоступно
  как отдельное действие... hero и overview рендерятся одновременно"), которая при чтении в отрыве
  от кода создаёт впечатление, что при включённом JavaScript поведение уже отличается (hero сначала,
  отделы — после клика). На деле код Step 3 не содержит вообще никакой JS-логики раскрытия: у
  основной CTA нет `onClick`, все компоненты — server components без `'use client'`, все 5 отделов
  рендерятся одинаково с JS и без него. Документ не лгал прямо, но создавал ложное впечатление
  через недосказанность — что достаточно для Blocker согласно данному этой сессией определению.
- Варианты: не рассматривались как развилка — это правка точности формулировки уже утверждённого
  документа, а не новое продуктовое решение (само решение "отделы раскрываются по клику" уже
  утверждено пользователем отдельной записью выше).
- Решение: добавлен явный абзац "Статус реализации (актуально после Step 3)" в раздел `hero`
  `docs/05`, прямо говорящий, что клиентское раскрытие ещё не реализовано, текущий код рендерит
  только no-JS вариант независимо от JS, и что реализация — задача Step 4.
- Последствия: `docs/05` больше не может быть неверно истолкован как описание уже работающего
  JS-поведения; будущий читатель документа как source-of-truth увидит явную пометку о разрыве между
  документом (целевое состояние) и кодом (текущее состояние) до завершения Step 4.
- Skeptic review: адресует Blocker #1 из FAIL-review (round 2) Step 3 — статус: ожидает повторной
  независимой проверки в рамках round 3.
- Согласование пользователя: не запрашивалось отдельно — это исправление точности формулировки
  документа, реализующее уже принятое пользователем решение ("Отделы появляются по клику CTA"), не
  новая развилка.

---

### 2026-07-14 — Step 3: low-height fallback для docs/08 "Низкий desktop"

- Контекст: skeptic (round 2, FAIL) нашёл, что no-scroll фикс не реализует ни одного из трёх
  пунктов, которые `docs/08-responsive-behavior.md` явно требует для случая низкой высоты
  ("Низкий desktop": уменьшить масштаб, скрыть второстепенное, разрешить внутренний скролл content
  panel; заголовок/навигация/CTA не обрезаются) — вместо этого хотспоты бесконечно сжимались
  (24.7px на 1280×500), а `.shell` жёстко запрещал скролл без исключения. Это реальная развилка
  "что делать сейчас в рамках Step 3", вынесенная пользователю напрямую вместо решения
  исполнителем.
- Варианты: (a) задокументировать как known gap (фикс покрывает только высоты ≥720px, аварийный
  режим docs/08 остаётся нереализованным, owner — будущий шаг вроде Step 6/7 по нумерации на
  момент этой записи, до Amendment 3); (b) реализовать low-height fallback сейчас, в рамках
  текущей коррекции Step 3, без расширения scope на новый шаг.
- Решение: (b) — реализовать сейчас.
- Последствия:
  - `HeroCopy.module.css`: `@media (max-height: 700px)` уменьшает padding/font-size/margins hero
    (частично реализует "уменьшить масштаб" из docs/08).
  - `OfficeExperience.module.css`: та же media query добавляет `.office { overflow-y: auto; }` —
    панель офиса (ближайший аналог "content panel" до появления реальной 10/90-панели — на момент
    этой записи ожидавшейся в Step 5; **после Amendment 3 — это Step 6, "Desktop 10/90 shell"**)
    получает собственный внутренний скролл вместо клиппинга или растяжения документа.
  - `OfficeSemanticMap.module.css`: та же media query фиксирует `.zoneList { min-height: 340px;
    flex-shrink: 0; }` — хотспоты (позиционированные в % от `.zoneList`) больше не сжимаются ниже
    читаемого размера; избыток высоты обрабатывается скроллом `.office`, а не документа.
  - `.shell` остаётся `overflow: hidden` без изменений — страница в целом по-прежнему не
    скроллится ни на каких высотах; скролл теперь есть только у панели офиса и включается лишь
    при нехватке места.
  - Новый e2e-тест (`office-overview.spec.ts`) на 1280×500: заголовок/CTA не обрезаны, документ
    не скроллится, каждый хотспот после `scrollIntoViewIfNeeded()` имеет высоту ≥44px.
  - Независимая ручная проверка (Playwright-скрипт, не сохранён в репозитории): на 1280×500 все 5
    хотспотов измерены в диапазоне 81.6–122.4px (было 24.7px до этой правки), заголовок полностью
    в пределах viewport, `document.scrollHeight === window.innerHeight` (500).
  - "Скрыть второстепенное" (третий пункт docs/08) не реализовано отдельно — сочтено
    необязательным при уже достаточном эффекте первых двух мер; при необходимости может быть
    добавлено позже без изменения архитектуры (просто ещё один media-query-blok).
- Skeptic review: адресует Major-находки #2/#3 из FAIL-review (round 2) Step 3 — статус: ожидает
  повторной независимой проверки в рамках round 3.
- Согласование пользователя: получено 2026-07-14 напрямую через `AskUserQuestion` — выбран вариант
  (b) "Реализовать min-height floor / low-height fallback сейчас".

---

### 2026-07-15 — Step 4: библиотека состояния для `office-machine` (OQ-1)

- Контекст: `docs/09` "Стек" перечисляет Zustand без альтернативы, но Step 1 явно отнёс его в
  "Out of scope" на момент появления реальной клиентской интерактивности — то есть именно сейчас
  (Step 4, первый клиентский компонент проекта). Planner вынес выбор библиотеки состояния как
  открытый вопрос (OQ-1) вместо того, чтобы зашить Zustand как факт только потому, что он упомянут
  в docs/09 (прецедент BLOCKED — Step 2, zod).
- Варианты: (a) `useState`/`useReducer` внутри одного client-компонента — без новой зависимости,
  минимальный риск, потребует рефакторинга при росте состояния; (b) React Context + `useReducer` —
  тоже без зависимости, избегает prop-drilling, но полностью самописный код; (c) Zustand — новая
  npm-зависимость (по прецеденту `zod`), внешний store, проще расширять для будущих
  Diagnostic/Calculator состояний, но вводится раньше, чем требует нынешний объём состояния.
- Решение: (a) `useState`/`useReducer`.
- Последствия: `src/features/office-machine/*` реализуется без новой npm-зависимости;
  `package.json`/`package-lock.json` не меняются в Step 4. При будущем росте состояния (Step 5+)
  выбор state-библиотеки может быть пересмотрен отдельным вопросом, не наследуется автоматически.
- Skeptic review: план (включая нейтральную формулировку всех трёх вариантов OQ-1) прошёл
  skeptic review плана — `PASS`, round 6 (`94a0ebc`); OQ-1 ни разу не был отмечен как асимметричный
  или скрыто решённый ни в одном из 6 раундов review.
- Согласование пользователя: получено 2026-07-15 напрямую через `AskUserQuestion` — выбран вариант
  "useState/useReducer (рекомендую)".

---

### 2026-07-15 — Step 4: граница Step 4/Step 5 по глубине состояний выбора отдела (OQ-2)

- Контекст: черновик плана Step 4 изначально (round 1 review плана, `b8cecb6`) предлагал только два
  варианта границы между Step 4 и Step 5, оба молча предполагавшие, что `SELECT_DEPARTMENT`/
  `CLOSE_DEPARTMENT` входит в Step 4 — хотя уже утверждённая формулировка Step 5 ("Реализовать
  выбор, панель отделов и основное поле") прямо назначает "выбор" именно Step 5. Skeptic вернул
  `FAIL` и потребовал добавить третий, буквально согласующийся со Step 5 вариант — добавлен как
  OQ-2(c).
- Варианты: (a) Step 4 реализует полный набор состояний `docs/05` (`opening → active ↔ switching →
  closing`) с намеренно минимальным, не финальным UI — Step 5 только переоформляет в 10/90; (b)
  Step 4 реализует только упрощённый бинарный `SELECT_DEPARTMENT`/`CLOSE_DEPARTMENT` без
  под-состояний — Step 5 добавляет и раскладку, и полноценные переходы; (c) Step 4 не реализует
  выбор отдела вовсе — ограничивается `hero ↔ overview`, вся логика выбора целиком в Step 5.
- Решение: (c) — Step 4 не реализует выбор отдела.
- Последствия: Step 4 остаётся минимальным (только клиентский toggle `hero`/`overview`); Step 5
  получает больший объём работы, чем в вариантах (a)/(b) — реализует state machine выбора отдела с
  нуля одновременно с 10/90-раскладкой (см. `WORKPLAN.md`, Step 4 Risks и Step 5 Objective — явно
  зафиксировано как "важно при детальном планировании Step 5", не потеряно молча).
- Skeptic review: находка round 1 (отсутствие варианта (c)) устранена в round 2+; после этого OQ-2
  прошёл ещё 4 раунда review на предмет тональной нейтральности формулировок вариантов (round
  2–5 находили остаточную асимметрию каждый раз), финальная нейтральная формулировка подтверждена
  round 6 `PASS` (`94a0ebc`).
- Согласование пользователя: получено 2026-07-15 напрямую через `AskUserQuestion` — выбран вариант
  "(c) Step 4 = только hero↔overview (рекомендую)". См. также следующую запись — этот ответ
  первоначально создал конфликт с ответом на OQ-3, разрешённый отдельным вопросом.

---

### 2026-07-15 — Step 4: конфликт OQ-2/OQ-3 — автооткрытие отдела по прямому URL переносится в Step 5

- Контекст: пользователь ответил на OQ-2 = (c) (Step 4 не реализует выбор отдела вовсе) и на OQ-3 =
  "(b) сразу открывать отдел" по прямому URL `?department=<id>` — но сам план явно указывал, что
  OQ-3(b) неприменим, если OQ-2 = (c): открыть `department-active` физически нечем, если в Step 4
  нет состояния "активный отдел". Это реальный, а не надуманный конфликт (зафиксирован в тексте
  плана до того, как пользователь дал оба ответа) — он не мог быть тихо разрешён исполнителем в
  пользу одного из ответов.
- Варианты, представленные пользователю напрямую: (a) оставить Step 4 = только пропуск `hero`
  (OQ-3 фактически становится (a) для Step 4); реальное автоматическое открытие конкретного отдела
  по ссылке становится частью Step 5 вместе с остальной логикой выбора отдела; (b) пересмотреть
  OQ-2 — перевести Step 4 на вариант (b) (упрощённый бинарный select/close), чтобы у Step 4
  появилось состояние "активный отдел", и тогда OQ-3(b) можно реализовать уже в Step 4.
- Решение: (a) — Step 4 ограничивается пропуском `hero` при валидном/невалидном
  `?department=<id>`; реальное автоматическое открытие конкретного отдела по ссылке (изначально
  предпочтённое пользователем поведение, OQ-3 "(b) Сразу открывать отдел") переносится в Step 5,
  где будет и состояние "активный отдел", и связанная с ним honesty-правка `docs/05` (по прецеденту
  Step 3), так как текущая формулировка `docs/05` описывает только пропуск `hero`, а не
  автоматическое открытие отдела.
- Последствия: `WORKPLAN.md` Step 4 Acceptance criterion 5 фиксирует финальное поведение Step 4
  (`hero` пропускается, `overview` показывается, отдел не открывается автоматически); `WORKPLAN.md`
  Step 5 Objective дополнен явным примечанием о переносе этой ответственности, чтобы не потерять
  её при детальном планировании Step 5. `docs/05` в Step 4 не редактируется — текущая формулировка
  ("пропускает hero и открывает сразу overview") уже точно описывает поведение Step 4 как есть;
  honesty-правка про автооткрытие отдела откладывается до Step 5, когда для неё появится реальный
  код.
- Skeptic review: конфликт обнаружен основной сессией при сопоставлении двух ответов пользователя
  с уже существующим в плане предупреждением (не skeptic-агентом отдельно — сам факт, что план уже
  явно предвидел этот конфликт в тексте OQ-3, является результатом предыдущих раундов skeptic
  review плана). Отдельный повторный review плана после разрешения конфликта — round 7, см.
  `WORKPLAN.md` Step 4 Skeptic verdict.
- Согласование пользователя: получено 2026-07-15 напрямую через `AskUserQuestion` (второй,
  уточняющий вопрос после обнаружения конфликта) — выбран вариант "Step 4: только пропуск hero;
  полное авто-открытие — в Step 5 (рекомендую)".

---

**Примечание о нумерации шагов ниже и выше по файлу.** Все записи выше этой строки (OQ-1, OQ-2,
конфликт OQ-2/OQ-3 и т.д. при планировании Step 4) используют "Step 5" в значении, действовавшем
**до** Amendment 3 — единый нераздельный черновик "Desktop 10/90 shell". Это исторические записи
дневникового типа (фиксируют, что реально обсуждалось и решалось в момент написания), и по
прецеденту `WORKPLAN.md` ("Open questions (Step 4) — RESOLVED" — сохранён как есть "для истории/
трассируемости") сознательно не переписаны заново под пост-Amendment-3 нумерацию, кроме отдельных
мест, где ссылка стала буквально ложной (см. точечные аннотации "после Amendment 3 — ..." выше и
ниже). Начиная с записи OQ-A ниже, "Step 5"/"Step 6" используются уже в текущем, актуальном
значении (Step 5 = "Department selection state machine", Step 6 = "Desktop 10/90 shell") —
результат решения по OQ-D, см. запись "Step 5: разбиение на два отдельных шага WORKPLAN (OQ-D,
Amendment 3)" ниже.

---

### 2026-07-15 — Step 5: механизм переходов opening/switching/closing (OQ-A)

- Контекст: `docs/09` "Стек" перечисляет GSAP без альтернативы, но ни один из шагов 1–4 его не
  вводил. Planner вынес выбор механизма перехода как открытый вопрос вместо того, чтобы зашить
  GSAP как факт только потому, что он упомянут в docs/09 (тот же класс прецедента, что OQ-1 Step 4
  для Zustand).
- Варианты: (a) GSAP — таймлайны с явным lifecycle-управлением, первая новая npm-зависимость с
  момента `zod` (Step 2); (b) CSS-переходы/keyframes, управляемые классами из состояния редьюсера
  (тот же подход, что уже использован для `hero → overview` в Step 4) — без новой зависимости; (c)
  без анимации вовсе — `opening`/`switching`/`closing` существуют только как учёт для
  focus-management.
- Решение: (b) — CSS-классы, как в Step 4.
- Последствия: `src/features/office-machine/*` продолжает не требовать новой npm-зависимости;
  GSAP не вводится в Step 5. Переходы `opening/switching/closing` реализуются тем же паттерном, что
  `hero ↔ overview` — классы, управляемые состоянием редьюсера, момент завершения перехода
  определяется через `transitionend` для диспетча `OPEN_COMPLETE`/`SWITCH_COMPLETE`/
  `CLOSE_COMPLETE`.
- Согласование пользователя: получено 2026-07-15 напрямую через `AskUserQuestion` — выбран вариант
  "CSS-классы, как в Step 4 (рекомендую)".

---

### 2026-07-15 — Step 5: механизм URL-синхронизации при выборе/переключении/закрытии отдела (OQ-B)

- Контекст: клиентский выбор/переключение/закрытие отдела должно обновлять `?department=<id>` в
  адресной строке без полной перезагрузки страницы. Два кандидата с разными компромиссами
  (идиоматичность Next.js vs риск повторного рендера серверного дерева).
- Варианты: (a) Next.js `useRouter`/`useSearchParams` (`next/navigation`) — идиоматично для App
  Router, потенциально поддерживает браузерные назад/вперёд, но может инициировать повторный
  рендер серверного `page.tsx`/`HomepageShell` при каждой смене отдела; (b) сырой
  `history.pushState`/`replaceState` — чисто косметическое обновление URL, без серверного/
  React-рендер-цикла, не поддерживает браузерные назад/вперёд (вне scope шага в любом случае).
- Решение: (b) — `history.pushState`/`replaceState`.
- Последствия: URL-синхронизация в Step 5 — первый сырой DOM/browser API вызов в проекте в обход
  навигации Next.js; исключается риск повторного вызова adapter'ов `src/content/*` или заметного
  мигания при каждом переключении отдела (прямое смягчение риска, названного в Step 4: новый класс
  гидратационных проблем). Поддержка браузерных назад/вперёд остаётся вне scope Step 5, как и было
  зафиксировано в Out of scope независимо от этого решения.
- Согласование пользователя: получено 2026-07-15 напрямую через `AskUserQuestion` — выбран вариант
  "history.pushState/replaceState (рекомендую)".

---

### 2026-07-15 — Step 5: глубина содержимого 90%-области отдела (OQ-C)

- Контекст: Core concept CLAUDE.md требует показывать "проблему, текущий процесс, сценарий
  автоматизации, результат и CTA", а полное дерево `docs/09` включает `DepartmentScene`/
  `BeforeAfterSequence`. Но `data/departments.json` (известный gap Step 2) не содержит
  структурированных `beforeSteps`/`automationSteps` — только неструктурированный `problem`-текст и
  `outcomes[]`.
- Варианты: (a) полное дерево `docs/09` сейчас, но со сниженной детализацией
  `BeforeAfterSequence` (один абзац `problem` вместо пошаговой последовательности); (b) не создавать
  `DepartmentScene`/`BeforeAfterSequence` вовсе — только `DepartmentCopy`/`OutcomePanel`/
  `DepartmentCTA`, аналогично тому, как Step 3 сознательно пропустил `OfficeVisualLayer`; (c)
  заблокировать реализацию содержимого отдела до дозаполнения `data/departments.json` — превращает
  известный gap Step 2 в реальный блокер именно сейчас, без назначенного владельца контентной
  задачи.
- Решение: (b) — только `DepartmentCopy`/`OutcomePanel`/`DepartmentCTA`, без `DepartmentScene`/
  `BeforeAfterSequence`.
- Последствия: `src/components/departments/` — на момент этой записи ожидался в Step 5; **после
  Amendment 3 (см. запись "Step 5: разбиение на два отдельных шага WORKPLAN (OQ-D, Amendment 3)"
  ниже) этот каталог создаётся в Step 6** — не включает `DepartmentScene.tsx`/
  `BeforeAfterSequence.tsx` — явное, раскрытое ограничение по прецеденту Step 3, не молчаливое
  упущение; `data/departments.json` дозаполнение остаётся отдельной, не назначенной ни одному шагу
  контентной задачей (без изменений относительно Step 2 Risks).
- Согласование пользователя: получено 2026-07-15 напрямую через `AskUserQuestion` — выбран вариант
  "Только DepartmentCopy/OutcomePanel/CTA (рекомендую)".

---

### 2026-07-15 — Step 5: разбиение на два отдельных шага WORKPLAN (OQ-D, Amendment 3)

- Контекст: решение по OQ-2 при планировании Step 4 объединило в один будущий шаг то, что
  `docs/17-execution-protocol.md` в своём примере "хороших шагов" перечисляет как два разных
  пункта ("4. Реализовать выбор отдела." и "5. Реализовать 10/90 shell."). Skeptic review плана
  Step 5 отметил это как крупнейший blast radius в проекте на сегодня (state machine с нуля + 10/90
  + URL-sync одновременно). Planner явно поднял вопрос вместо того, чтобы молча унаследовать
  увеличенный объём одного шага — по прямому правилу CLAUDE.md "не объединяй несколько шагов в один
  проход реализации".
- Варианты: (a) оставить одним шагом `WORKPLAN.md` с двумя внутренними, отдельно верифицируемыми
  под-фазами (5.1 — state machine, 5.2 — 10/90-раскладка), без пересмотра нумерации; (b) формально
  разбить на два отдельных шага `WORKPLAN.md`, сдвинув нынешние Steps 6/7/8 → 7/8/9 — требует
  Amendment к уже утверждённому 8-шаговому скелету.
- Решение: (b) — формальное разбиение на два шага.
- Последствия: утверждённый 8-шаговый скелет (`WORKPLAN.md`, раздел `Approval`, "Approved scope")
  становится 9-шаговым (**Amendment 3**): "Step 5 — Department selection state machine" (полная
  state machine выбора отдела, инверсия no-op хотспотов, Escape/кнопка «Закрыть», focus
  management, honesty-правка `docs/05` — без 10/90-раскладки, UI отдела — минимальный/неоформленный
  блок) и новый "Step 6 — Desktop 10/90 shell" (10/90-раскладка, `DepartmentNavigationRail`,
  реальное содержимое отдела по решению OQ-C, URL-sync по решению OQ-B, переходы по решению OQ-A);
  бывшие Step 6 ("Mobile touch flow") → Step 7, Step 7 ("Reduced motion and fallback") → Step 8,
  Step 8 ("Browser acceptance tests") → Step 9. `README.md` дополняется девятой строкой. Этот
  Amendment и вытекающее из него разделение `WORKPLAN.md` требуют отдельного review skeptic перед
  тем, как оба новых шага (5 и 6) смогут получить `Plan status: APPROVED`.
- Согласование пользователя: получено 2026-07-15 напрямую через `AskUserQuestion` — выбран вариант
  "Разбить на два шага (рекомендую)".

---

### 2026-07-15 — Коррекция: устаревшие перекрёстные ссылки на номера шагов в Steps 1–4 после Amendment 3

- Контекст: skeptic review первой версии разбиения (Amendment 3) вернул `FAIL` — тексты уже
  `COMPLETED` секций Step 3/Step 4 в `WORKPLAN.md` содержали перекрёстные ссылки на номера/названия
  шагов по нумерации, действовавшей до Amendment 3 (например, "Step 5 (\"Desktop 10/90 shell\")",
  "Step 7 (\"Reduced motion and fallback\")", "8-шаговый milestone"), часть из которых после
  разбиения стала не просто устаревшей, а буквально ложной (например, Step 5 больше не называется
  "Desktop 10/90 shell"). Planner сознательно не трогал текст Steps 1–4 при выполнении Amendment 3
  (по прямой инструкции "не трогай уже закрытые шаги"), из-за чего эти ссылки остались
  нескорректированными и незадокументированными как known issue.
- Решение: не оставлять как известную проблему без исправления, а точечно скорректировать сами
  ссылки — без изменения содержательного scope/решений Steps 3/4 (которые остаются `COMPLETED` и
  не пересматриваются). Каждое место аннотировано в духе "на момент этого шага было X; после
  Amendment 3 — Y", чтобы не переписывать историю бесследно, а показать её эволюцию.
- Затронутые места (WORKPLAN.md, Steps 3 и 4): ссылки на "Step 5"/"Step 6"/"Step 7"/"Step 8" по
  старой нумерации и на "8-шаговый скелет/milestone" — все заменены на аннотированные версии,
  указывающие как старое, так и новое (post-Amendment-3) значение.
- Skeptic review: **round 1** — находка зафиксирована в раунде review Amendment 3 (`FAIL`,
  2026-07-15); коррекция коммита `e42e90a` затронула только `WORKPLAN.md`/`README.md`/
  `DECISIONS.md`. **round 2** (`FAIL`) — обнаружено, что тот же класс устаревших/ложных ссылок на
  номера шагов остался нескорректированным в `WORKLOG.md` (Entry 3 и Entry 4 — записи именно про
  Step 3/Step 4, симметричные `WORKPLAN.md`), что создавало прямое фактическое противоречие между
  двумя source-of-truth документами (например, owner axe-сканирования — "Step 8" в `WORKLOG.md`
  против "Step 9" в уже исправленном `WORKPLAN.md`). Исправлено той же аннотированной техникой в
  `WORKLOG.md` (строки, относящиеся к Entry 3 "Known limitations"/"Skeptic review"/"Correction
  iteration" и Entry 4 "Completion evidence"/"Closing").
- Примечание к git-гигиене (замечание round 2, Minor): коммит `e42e90a` по факту содержит не только
  точечную правку ссылок, но и первое коммитируемое материализованное состояние самого разбиения
  Amendment 3 (новые секции "Step 5"/"Step 6", переномерация 7/8/9, 9-строчный `README.md`) — это
  не скрыто (полностью описано в `WORKPLAN.md`, разделе "Amendment 3", и в записи OQ-D выше), но
  сообщение коммита само по себе не упоминало этот более широкий scope. Зафиксировано здесь для
  ясности будущего аудита; отдельного исправления не требует.

---

### 2026-07-15 — Логотип в Header/favicon: использовать `references/logo/Logo111.svg` как есть,
несмотря на несовпадение с названием продукта

- Контекст: пользователь попросил заменить логотип в Header и favicon, указав, что файл уже есть в
  репозитории ("У тебя должна быть фотография логотипа"). Единственный найденный кандидат —
  `references/logo/Logo111.svg` — визуально содержит текст **"Qbit StudioAI"**, а не "Allqbit".
  Это название совпадает с посторонним, не относящимся к репозиторию процессом ("Qbit-Studio-AI"),
  который уже занимал порт 3000 на этой машине разработки (см. `WORKPLAN.md` Step 1 Risks,
  `WORKLOG.md` Entry 1) — то есть существовал правдоподобный риск, что файл попал в `references/`
  по ошибке (перепутан с чужим проектом), и молчаливое использование такого лого было бы
  рискованным для реального продукта.
- Варианты, представленные пользователю напрямую (`AskUserQuestion`): (a) использовать этот файл
  как есть, несмотря на текст "Qbit StudioAI" на нём; (b) это не тот файл — нужен другой
  файл/источник для реального логотипа Allqbit.
- Решение: (a) — использовать `references/logo/Logo111.svg` как есть.
- Последствия: `public/logo.svg` (копия файла), `src/components/homepage/Header.tsx`
  (`next/image`, логотип перед текстовым брендом "Allqbit" — текст сохранён, т.к. логотип не несёт
  этого слова), `src/app/layout.tsx` (`metadata.icons.icon`). Полная реализация и верификация — см.
  `WORKLOG.md`, "Ad hoc — Логотип в Header и favicon".
- Skeptic review: не запрашивался — точечная, обратимая, низкорисковая правка вне архитектуры
  9-шагового плана (аналог "trivial one-line correction" по духу, хотя и не буквально одна строка);
  полный planner/skeptic protocol не применялся.
- Согласование пользователя: получено 2026-07-15 напрямую через `AskUserQuestion` — выбран вариант
  "Да, использовать как есть".

---

### 2026-07-15 — Step 5: отклонение от плана — механизм завершения переходов `transitionend` → таймер

- Контекст: план Step 5 (`WORKPLAN.md`, In scope) и решение по OQ-A явно фиксировали механизм
  определения момента завершения перехода `opening`/`switching`/`closing` через `transitionend`
  ("момент завершения перехода определяется через `transitionend` для диспетча
  `OPEN_COMPLETE`/`SWITCH_COMPLETE`/`CLOSE_COMPLETE`"). При реализации выяснилось, что
  `ActiveDepartmentPanel.module.css` использует CSS `@keyframes`/`animation` (`.opening`/
  `.switching`/`.closing`), а не CSS `transition` — событие `transitionend` в принципе не срабатывает
  для `animation` (для этого существует отдельное событие `animationend`), поэтому буквально
  реализовать OQ-A через `transitionend` было невозможно без переписывания CSS-техники. Skeptic
  review исполнения (Phase B) обнаружил, что это отклонение от согласованного механизма не было
  зафиксировано как решение — WORKLOG Entry 5 упоминало переход на таймер как факт, но не как
  осознанное отклонение от AC21 ("новых записей не требуется, если... не возникнет новых, не
  предвиденных развилок" — развилка возникла и не была честно раскрыта).
- Решение (постфактум, зафиксировано по итогам skeptic review): используется фиксированный
  JS-таймер (`setTimeout`, 800/600/700мс по ориентировочным длительностям `docs/07`) вместо
  `transitionend`/`animationend`. Это сознательный выбор, а не молчаливая замена: таймер не зависит
  от того, какую именно CSS-технику (transition/animation/none) использует конкретная реализация
  временного `ActiveDepartmentPanel`, что делает книгоучёт состояний устойчивым к будущим
  правкам CSS этого временного блока — но ценой риска рассинхронизации JS-таймера и реальной
  CSS-длительности, если она изменится, а таймер — нет (см. Risks Step 5, дополнено).
- Последствия: функциональность не пострадала (переходы `opening/switching/closing` по-прежнему
  работают, содержание отдела доступно сразу независимо от завершения анимации — Motion rules
  CLAUDE.md); риск дрейфа между CSS-длительностью и JS-таймером — принят как известный, отмечен в
  Risks Step 5 как задача учесть при реализации Step 6 (если Step 6 меняет CSS-технику перехода,
  таймеры в `OfficeMachine.tsx` нужно сверить/обновить вручную).
- Skeptic review: находка зафиксирована в review исполнения Step 5 (Phase B) — не блокирует (Major,
  но исправимо в рамках текущего scope без пересмотра плана); устранено этой записью и пометкой в
  `WORKPLAN.md`/`WORKLOG.md`.
- Согласование пользователя: не запрашивалось отдельно — техническая реализационная деталь в
  рамках уже утверждённого Step 5 (аналог "технических решений исполнителя, не требующих отдельного
  вопроса", см. прецедент Step 3 сортировки хотспотов), задокументирована постфактум по требованию
  skeptic, а не как решение, нуждающееся в согласовании пользователя.

---

### 2026-07-16 — Step 6: состав `DepartmentNavigationRail` — 4 кнопки + 1 не интерактивный маркер
активного отдела

- Контекст: при исполнении Step 6 обнаружилось расхождение внутри уже утверждённого плана между
  двумя формулировками. `WORKPLAN.md` Step 6 In scope буквально требует "список из 4 доступных
  кнопок для оставшихся отделов" (активный отдел кнопкой не рендерится вовсе), что напрямую
  наследует формулировку `docs/03-office-map.md` "Режим 10/90": "Левая панель 10–14%: возврат; **пять
  миниатюр**; активное состояние; клавиатурная навигация" — литературально 5 миниатюр, не 4. Тот же
  Step 6 In scope одновременно требует: "Активный отдел обозначен не только цветом (`docs/14`) —
  например, текстом/`aria-current="true"`/иконкой" — то есть индикация активного отдела **в
  rail**, что не имеет смысла, если активный отдел в rail вообще отсутствует. Это неразрешённая при
  planning-review (Phase A, round 1–7) развилка внутри самого плана — round 7 (финальный) отметил
  только один пограничный вопрос (формулировка Dependencies) и не поймал это внутреннее противоречие
  между "4 кнопки" и "активный элемент rail обозначен".
- Решение (принято при исполнении, без отдельного вопроса пользователю — техническая деталь малого
  риска, аналог прецедента Step 3 "сортировка хотспотов"): `DepartmentNavigationRail` рендерит все 5
  отделов (примиряет "пять миниатюр" `docs/03`), но только 4 неактивных — как кликабельные
  `<button>`, диспетчерящие `SWITCH_DEPARTMENT` (примиряет "4 доступных кнопки" `WORKPLAN.md`);
  активный отдел рендерится как единственный не интерактивный элемент списка (`<span
  aria-current="true">` с текстовым/иконочным маркером "●", не только цветом рамки/фона — `docs/14`)
  и не входит в Tab-последовательность (не `<button>`) — это согласуется с acceptance criterion 5
  ("4 элемента rail" в Tab-порядке при активном отделе), которое осталось бы неверным при буквальных
  5 кнопках.
- Последствия: `src/components/office/DepartmentNavigationRail.tsx` реализует пять отделов на входе
  (`departments` — все 5, а не заранее отфильтрованные 4); фильтрация происходит внутри компонента.
  Юнит-тест `department-navigation-rail.test.tsx` и e2e
  `desktop-10x90-shell.spec.ts`/`department-selection.spec.ts` проверяют оба свойства: ровно 4
  кнопки И наличие `aria-current="true"` для активного отдела.
- Skeptic review: находка предвидится этой же записью (написана до вызова skeptic Phase B по итогам
  исполнения) — не находка skeptic задним числом, а самостоятельно раскрытая развилка при
  исполнении, по прецеденту "новые записи нужны только если... возникнут новые, не предвиденные
  развилки" (`WORKPLAN.md` Step 6 acceptance criterion 15).
- Согласование пользователя: не запрашивалось отдельно — реконструкция двух уже утверждённых
  пользователем формулировок плана без изменения видимого поведения ни одной из них (обе стороны
  требования выполнены буквально), не новое продуктовое решение.

---

### 2026-07-16 — Step 6: длительности CSS-переходов сохранены идентичными Step 5 (риск дрейфа не
материализовался)

- Контекст: запись выше ("Step 5: отклонение от плана — механизм завершения переходов
  `transitionend` → таймер") оставила открытым риском для Step 6: "если Step 6 меняет CSS-технику
  перехода, таймеры в `OfficeMachine.tsx` нужно сверить/обновить вручную".
- Решение: `DepartmentExperience.module.css` (`.opening`/`.switching`/`.closing`) использует те же
  CSS `@keyframes`/`animation`-длительности (0.8s/0.6s/0.7s), что и удалённый
  `ActiveDepartmentPanel.module.css`, и что уже жёстко закодированы как JS-таймеры в
  `OfficeMachine.tsx` (`OPEN_DURATION_MS`/`SWITCH_DURATION_MS`/`CLOSE_DURATION_MS` = 800/600/700) —
  `OfficeMachine.tsx` не менялся в Step 6. Риск дрейфа не материализовался; таймеры не требуют
  правки.
- Последствия: нет расхождения между визуальной длительностью перехода и моментом диспетча
  `OPEN_COMPLETE`/`SWITCH_COMPLETE`/`CLOSE_COMPLETE`.
- Skeptic review: не запрашивался отдельно для этой записи — фиксирует отсутствие изменения, а не
  новое решение.
- Согласование пользователя: не требуется — сохранение уже согласованного поведения без изменений.

---

### 2026-07-16 — Step 6: остановка Phase B correction loop после round 5 BLOCKED

- Контекст: Phase B review исполнения Step 6 прошёл 5 раундов подряд (round 1–4 `FAIL`, round 5
  `BLOCKED`), при этом ни один из пяти не нашёл дефекта в функциональной реализации, тестах,
  архитектуре, accessibility или производительности — все пять были самовоспроизводящимися
  bookkeeping-дефектами в самих процессных документах (`WORKPLAN.md`/`WORKLOG.md`): git-индекс
  рассинхронизирован (round 1); отсутствующие/противоречивые Phase B поля (round 2); буквально
  ложная формулировка о выводе одной git-команды (round 3); самоссылочное ложное утверждение +
  устаревший счётчик раундов (round 4); дублирующий бюллет, оставленный правкой round 4 (round 5).
  На round 5 skeptic прямо отметил паттерн: каждое исправление одного bookkeeping-несоответствия
  порождало соседнее новое того же класса, и порекомендовал не открывать round 6 в прежнем виде без
  решения пользователя — либо принять функциональное состояние Step 6 закрытым с этим известным, не
  блокирующим bookkeeping-дефектом как отдельным known issue, либо перейти на более простой формат
  Phase B bookkeeping. `src`-diff (код и тесты Step 6) не менялся ни разу за все 5 раундов — 20
  файлов, 652 insertions/140 deletions идентично во всех пяти независимых прогонах skeptic'а.
- Решение (принято исполнителем в рамках уже утверждённого протокола CLAUDE.md "Phase C —
  correction loop": "если фикс требует... остановиться... спросить пользователя"; не самостоятельное
  решение по существу, а следование протоколу): дубль-бюллет round 5 устранён точечно (это
  механическая, однозначная правка — удаление буквально повторяющегося текста, не требующая
  интерпретации); `WORKPLAN.md` Step 6 `Status` переведён в `BLOCKED`; вопрос о том, продолжать ли
  Phase B correction loop (round 6) или принять текущее состояние, вынесен пользователю напрямую
  через `AskUserQuestion`, а не решён исполнителем самостоятельно.
- Последствия: до ответа пользователя Step 6 остаётся `BLOCKED`, не `COMPLETED`, несмотря на то что
  вся функциональная часть (15 acceptance criteria, 6 verification commands) независимо
  верифицирована 5 раз подряд.
- Skeptic review: находка и рекомендация — от самого skeptic (round 5, `BLOCKED`), эта запись
  фиксирует его рекомендацию как решение, а не переопределяет её.
- Согласование пользователя: запрошено 2026-07-16 напрямую через `AskUserQuestion` с тремя
  вариантами (открыть round 6 сейчас; принять текущее состояние и закрыть Step 6 — рекомендовано;
  упростить формат Phase B bookkeeping и затем продолжить). Пользователь выбрал: **"Принять текущее
  состояние и закрыть Step 6 (рекомендация)"**. Основание, приведённое в самом вопросе и принятое
  пользователем: функциональная реализация (все 15 acceptance criteria, все 6 verification commands)
  независимо подтверждалась корректной 5 раз подряд, включая живую проверку skeptic'ом в браузере
  каждый раз, и ни разу не менялась за все 5 раундов; все 5 находок Phase B были исключительно
  bookkeeping-дефектами процессных документов, не кода. Step 6 закрывается без формального `PASS`
  от skeptic (Phase B review остановился на round 5 `BLOCKED`) — это осознанное, явно
  запрошенное и полученное решение пользователя, а не самостоятельное решение исполнителя и не
  самопровозглашённый `PASS` (см. `WORKPLAN.md` Step 6 `Status`/`Skeptic verdict (Phase B)` для
  полной, честной формулировки этого расхождения с обычным правилом "не начинать следующий шаг без
  PASS").

---

### 2026-07-16 — Step 6: коммит закрытия

- Контекст: skeptic Phase A round 1 (при планировании Step 7) нашёл Critical — план Step 7
  ссылался на "коммит закрытия Step 6", которого не существовало (Step 6 был только застейджен).
  Вопрос задан пользователю напрямую через `AskUserQuestion`.
- Решение: пользователь выбрал «Да, закоммить сейчас (рекомендация)» — отдельный коммит,
  предшествующий началу реализации Step 7.
- Последствия: коммит создаётся после этой записи, включает все файлы Step 6 (24, см. `WORKLOG.md`
  Entry 6) плюс процессные правки Phase A планирования Step 7, накопившиеся в тех же файлах
  (`WORKPLAN.md`/`WORKLOG.md`) к этому моменту — Step 7 **кода** в этом коммите нет (реализация не
  начата), только планирование в процессных документах. Полное хирургическое разделение "чисто
  Step 6" от "Step 6 + Phase A планирования Step 7" в рамках одних и тех же непрерывных markdown-
  файлов потребовало бы ручного построчного patch-staging — признано непропорциональным риском для
  low-fidelity milestone; коммит трактуется как "Step 6 завершён, код Step 7 отсутствует", что
  удовлетворяет исходную озабоченность skeptic'а (не смешивать future Step 7 КОД с Step 6).
- Skeptic review: находка — от skeptic (Phase A round 1); это решение — прямой ответ на неё.
- Согласование пользователя: получено 2026-07-16 через `AskUserQuestion`.

### 2026-07-16 — Step 7: ответы на OQ-M1–OQ-M4

- Контекст: план Step 7 (`WORKPLAN.md`) содержал 4 открытых вопроса, требующих решения пользователя
  до `APPROVED`. Заданы напрямую через `AskUserQuestion`.
- Решения пользователя:
  - **OQ-M1 (диапазон Tablet 768–1279px) = (c)** — «Сейчас же добавить отдельный шаг для Tablet».
    Требует формального Amendment к утверждённому 9-шаговому скелету (по прецеденту Amendment 3,
    2026-07-15) — новый шаг для Tablet должен быть вставлен в последовательность Steps, с
    аккуратной перенумерацией/переразметкой существующих Step 8 ("Reduced motion and fallback")/
    Step 9 ("Browser acceptance tests") и обновлением всех перекрёстных ссылок. Формальная запись
    Amendment — отдельно, после того как `planner` подготовит конкретную формулировку нового шага.
  - **OQ-M2 (глубина свайпа) = (b)** — «Не реализовывать сейчас (рекомендация)». Совпадает с
    default-предположением, уже заложенным в черновик плана Step 7 — правок текста не требует.
  - **OQ-M3 (представление списка/переключение) = (b)** — «Горизонтальная карусель/пейджинг».
    **Отличается от default-предположения черновика плана** (вариант (a), вертикальный список +
    полноэкранная карточка без прямого переключения), которое было использовано по умолчанию во
    всём тексте In scope/Expected files/Acceptance criteria черновика Step 7. Выбор (b) требует
    существенной переработки этих разделов плана — не точечной правки: overview на мобильном
    показывает одну карточку отдела с самого начала (не список), переключение между соседними
    отделами — напрямую (`SWITCH_DEPARTMENT`, постранично, кнопки и/или свайп), без промежуточного
    возврата к списку. Роль `DepartmentNavigationRail`/новых paging-контролов на мобильном требует
    отдельного технического решения при повторном планировании.
  - **OQ-M4 (`interactionHint` на мобильном) = (a)** — «Не показывать на ≤767px (рекомендация)».
    Совпадает с default-предположением черновика — правок текста не требует.
- Последствия: план Step 7 в его нынешнем виде (черновик, прошедший skeptic Phase A round 3 `PASS`)
  **устарел в частях, зависящих от OQ-M1/OQ-M3**, и не может быть переведён в `APPROVED` без
  повторного прохода через `planner` (переработка Step 7 под OQ-M3=(b) + подготовка Amendment 4 для
  нового Tablet-шага под OQ-M1=(c)) и повторного skeptic Phase A review переработанного плана.
  Round 3 `PASS` относится к черновику, который эти решения ещё не учитывал — не переносится
  автоматически на переработанную версию.
- Skeptic review: не применимо к этой записи напрямую — ближайший skeptic review будет над
  переработанным планом.
- Согласование пользователя: получено 2026-07-16 через `AskUserQuestion`.

---

### 2026-07-16 — Коррекция: формулировка OQ-M1=(c) выше ссылалась на буквальную перенумеровку,
Amendment 4 в итоге выбрал нелинейную метку

- Контекст: запись выше ("Step 7: ответы на OQ-M1–OQ-M4") описывает последствие OQ-M1=(c) как
  "аккуратную перенумерацию/переразметку существующих Step 8/Step 9 и обновление всех перекрёстных
  ссылок" — это была формулировка ДО того, как `planner` фактически спроектировал Amendment 4 и
  сравнил два варианта нумерации. Skeptic Phase A review переработанного плана Step 7/Step 7.5
  (round 1, 2026-07-16) отметил это расхождение как Major находку: запись решения и фактический
  Amendment 4 говорят разное про один и тот же вопрос.
- Решение: `planner`, готовя Amendment 4, сравнил буквальный сдвиг номеров (вариант (a) — 35
  измеренных вхождений "Step 8"/"Step 9" в трёх процессных файлах, тот же класс работы, что уже
  стоил 6 раундов skeptic `FAIL` при Amendment 3) с нелинейной меткой "Step 7.5" (вариант (b) — ноль
  правок существующего текста Step 8/Step 9) и рекомендовал (b). Запись выше не отражала этот выбор,
  так как была сделана раньше, чем он появился — честно зафиксировано здесь, не переписана задним
  числом в исходной записи.
- Последствия: формулировка "аккуратной перенумерацией/переразметкой" в записи выше — устаревшая,
  относится к предполагавшемуся на тот момент подходу (a), не к фактически выбранному (b). Реальное
  решение о самой схеме нумерации ("Step 7.5" vs буквальный сдвиг) — рекомендация `planner`'а,
  отдельно ожидающая подтверждения пользователя (не совпадает автоматически с более ранним согласием
  на структурную развилку OQ-M1=(c) — см. `WORKPLAN.md` "### Amendment 4" → `Status`).
- Skeptic review: находка — от skeptic (Phase A review, round 1, 2026-07-16); эта запись — прямой
  ответ на неё.
- Согласование пользователя: не запрашивалось отдельно для этой корректирующей записи — честно
  документирует уже случившееся расхождение, не вводит новое решение, требующее отдельного вопроса.

---

### 2026-07-16 — Step 7/Step 7.5: OQ-M5, схема нумерации "Step 7.5", OQ-T1 — финальные ответы
пользователя перед `APPROVED`

- Контекст: после трёх раундов skeptic Phase A review переработанного плана Step 7/Step 7.5
  (round 1 `BLOCKED` → round 2 `FAIL` → round 3 `PASS`, см. `WORKPLAN.md`) оставалось три открытых
  пункта, заданных пользователю напрямую через `AskUserQuestion` в одном запросе.
- Решения пользователя:
  - **OQ-M5 (конфликт формулировки OQ-M3 с `docs/03-office-map.md` "Mobile") = (a)** — «Подтвердить
    карусель, поправить docs/03». Пользователь осознанно подтвердил ранее выбранный карусельный
    вариант (OQ-M3=(b)) как намеренный, зная о расхождении с буквальным текстом `docs/03`, и явно
    поручил честно поправить документ под фактическое поведение — не откатывать архитектуру и не
    оставлять расхождение скрытым. Реализовано этой же правкой: `docs/03-office-map.md` "Mobile"
    переписан под карусель/пейджинг + прямое переключение, с честной пометкой правки (тот же
    прецедент, что дважды применялся к `docs/05`).
  - **Схема нумерации "Step 7.5" (Amendment 4) = подтверждена** — «Да, «Step 7.5» (рекомендация)».
    Пользователь принял рекомендацию `planner`'а: нелинейная метка вместо буквального сдвига
    Step 8→9/Step 9→10, во избежание повторения 6-раундового цикла skeptic `FAIL`, уже случившегося
    при аналогичном сдвиге в Amendment 3. `Amendment 4` `Status` переведён в единый `APPROVED`.
  - **OQ-T1 (`interactionHint` на Tablet 768–1279px) = (b)** — «Показывать как есть». В отличие от
    Mobile (OQ-M4=(a), скрыть), на Tablet пользователь предпочёл сохранить существующее поведение
    без изменений — известный, явно принятый content-mismatch (единственная строка копии буквально
    описывает hover, не гарантированный на Tablet), не блокирующий, не требующий правки
    `data/homepage-copy.json`.
- Последствия: все три пункта, гейтившие `APPROVED`, закрыты. `WORKPLAN.md` Step 7/Step 7.5
  `Status` переводятся в `APPROVED` этой же правкой — план готов к началу реализации (после
  фактического закрытия Step 7 для Step 7.5, см. её Dependencies).
- Skeptic review: не применимо к этой записи напрямую — план прошёл skeptic Phase A round 3 `PASS`
  до того, как эти три пункта были заданы пользователю (сами пункты — гейты `APPROVED`, не предмет
  отдельного технического review).
- Согласование пользователя: получено 2026-07-16 через `AskUserQuestion` (один запрос, три вопроса).

---

### 2026-07-16 — Overview: офис на весь экран, hero скрывается

- Контекст: пользователь сверял живой прототип (скриншоты hero и overview) с документами перед
  началом Step 7.5. По текущему коду и `docs/05-homepage-state-machine.md` ("overview" — "Видны весь
  офис, hero, CTA, пять отделов и подсказка") hero (заголовок, подзаголовок, обещания, обе CTA)
  остаётся видимым одновременно с офисом после раскрытия (`ACTIVATE_CTA`) — решение, принятое в
  Step 3/4/5. Пользователь счёл это неверным: после раскрытия офис должен занимать весь экран, hero
  должен полностью исчезать.
- Технический факт, важный для последствий этого решения: `HeroCopy` рендерится в
  `OfficeMachine.tsx` безусловно, без привязки к `state.view` — в отличие от `OfficeExperience`,
  видимость которой уже переключается (`isRevealed={state.view !== "hero"}`). Значит текущее
  поведение "hero виден всегда" не специфично для Desktop — оно одинаково и на Mobile (Step 7,
  только что пройден skeptic Phase B `PASS`, ждёт подтверждения пользователя), и будет унаследовано
  Tablet (Step 7.5, ещё не начат). Это не только "правка одного скриншота Desktop" — это правка
  общего поведения state machine для всех breakpoint'ов.
- Варианты не рассматривались отдельно — пользователь дал прямое, однозначное решение.
- Принятое решение: в состоянии `overview` hero (весь блок: заголовок/подзаголовок/обещания/обе CTA)
  и подсказка ("Наведите курсор на отдел") полностью скрываются; офис (карта отделов на Desktop/
  Tablet, карусель на Mobile) занимает весь экран. Hero остаётся видимым только в состоянии `hero`
  (до `ACTIVATE_CTA`) — это не меняется.
- Последствия: `docs/05-homepage-state-machine.md` ("overview") и `docs/03-office-map.md` ("Центр
  объединяет офис и резервирует место для hero") честно поправлены под новое решение, с явной
  правкой-пометкой (не переписаны молча). В `WORKPLAN.md` добавлен новый шаг **"Step 7.2 — Overview
  full-screen (hide hero)"**, вставленный между Step 7 и Step 7.5 (Tablet должен уже строиться на
  исправленном поведении, а не наследовать устаревшее). Код не менялся — пользователь явно запретил
  трогать код на этом этапе ("Пока ничего не меняй в коде. Скорректируй только план."). Реализация
  этого нового шага потребует также пересмотра уже пройденных (`PASSED`/`COMPLETED`) Steps 3–7 в
  части их acceptance criteria/тестов, которые полагались на "hero виден вместе с офисом" — это
  явный риск нового шага, не скрытый.
- Skeptic review: не проводился для этой записи — пользователь запросил только правку плана/
  документов, формальный skeptic Phase A review нового шага предстоит перед началом его реализации
  (по протоколу `CLAUDE.md`).
- Согласование пользователя: это решение пользователя, полученное напрямую в диалоге 2026-07-16 (не
  через `AskUserQuestion` — пользователь сам сформулировал и подтвердил выбор после того, как ему
  было предложено на выбор "оставить как в документах" / "поправить документы под новое видение").

---

### 2026-07-16 — Department-active: панель с фото + колонки "боль" (20%) / "выгода" (80%), 5 пар

- Контекст: продолжение той же сверки живого прототипа с документами (см. предыдущую запись). Для
  состояния `department-active` (открытый отдел) пользователь описал новую структуру: слева — панель
  отделов с фото (не только текст) на фоне бледнеющего общего фото офиса; основная область — не одна
  колонка, а две (20% боль / 80% выгода). Ассистенту предложено дать свободный совет "как для крутого
  сайта, максимально привлекающего заказчиков" перед фиксацией.
- Совет ассистента (дан, не решение): (1) связать боль и выгоду один-к-одному, а не показывать общий
  список результатов; (2) не обещать конкретных цифр экономии без реальных данных пользователя (уже
  требование `CLAUDE.md` "Copy rules"); (3) CTA может отражать выбранную боль; (4) лёгкая подсветка
  соседних отделов в панели, чтобы ощущались как единая система.
- Решение пользователя: принял пункт (1) как структурное решение — **ровно 5 пар "боль → выгода"**
  на отдел (не 3 симптома + 5 несвязанных результатов, как сейчас). Клик по конкретной боли слева
  показывает именно её выгоду справа. Пункты (2)–(4) пользователь не подтверждал и не отклонял явно —
  остаются предложением ассистента, не зафиксированы как решение.
- Технические последствия (не открытый вопрос, установленный факт): текущая модель данных —
  `symptoms: string[]` (максимум 3 в UI, `docs/12` "Правила") и независимый `outcomes: string[]`
  (сейчас 5 у каждого отдела, `data/departments.json`) — не подходит для требуемой связки 1:1.
  Нужна новая структура данных (например, `painPoints: { pain: string; gain: string }[]`, длина
  ровно 5) — правка `docs/12-content-data-model.md`, `src/content/schema.ts`, `content/types.ts` и
  реального содержимого `data/departments.json` для всех 5 отделов (сейчас заполнено только по 3
  симптома на отдел — 2 новые пары на отдел ещё не существуют даже черновиком, кроме одного примера
  "Дирекция", наброшенного в `WORKPLAN.md` Step 7.3 по прямой просьбе пользователя "текст можешь
  написать, но потом будем править").
- Последствия: `docs/03-office-map.md` ("Режим 10/90") и `docs/12-content-data-model.md` ("Правила")
  честно поправлены под новое решение (правки-пометки, не молчаливая перезапись). Новый шаг
  `WORKPLAN.md` **"Step 7.3 — Department view redesign (pain/gain panel)"** добавлен после Step 7.2,
  `Status: PROPOSED` (черновик, ждёт planner/skeptic Phase A перед `APPROVED`, как и Step 7.2, — по
  прямой инструкции пользователя "пока акцент на это не делаем, просто фиксируем"). Код не менялся.
- Skeptic review: не проводился — по той же причине, что и для предыдущей записи (открытый диалог,
  фиксация решения, не завершённый шаг).
- Согласование пользователя: получено напрямую в диалоге 2026-07-16, после того как пользователь сам
  запросил у ассистента творческий совет и затем явно выбрал, что из совета принять (пункт 1), а что
  оставить нерешённым (пункты 2–4).

---

### 2026-07-16 — Аудит и облегчение workflow: один Master Plan, без re-plan перед каждым шагом

- Контекст: пользователь указал, что текущий цикл (SPEC→PLAN→PLAN STEP→IMPLEMENT→REVIEW→FIX→NEXT
  STEP) вызывает повторное планирование уже известной задачи перед почти каждым шагом — лишний
  расход токенов. Запрошен полный аудит workflow и его облегчение без потери качества.
- Находки аудита: (1) `CLAUDE.md` Phase A буквально требовал planner+skeptic review плана перед
  КАЖДЫМ шагом, не только один раз — это и есть источник тяжёлого цикла; `WORKPLAN.md` "Approval"
  прямо фиксировал это правило. (2) Протокол дублирован в 4 местах: `CLAUDE.md` (авторитетный),
  `docs/17-execution-protocol.md` (206 строк, почти полный повтор), `.claude/skills/strict-plan-
  execution/SKILL.md` (повтор короче), `.claude/README.md` (короткий повтор). (3)
  `docs/18-work-plan-template.md`/`docs/19-work-log-template.md` — шаблоны, избыточные теперь, когда
  `WORKPLAN.md`/`WORKLOG.md` уже существуют и сами демонстрируют формат. (4) `WORKPLAN.md` уже
  превысил 256KB (не читается одним Read) — из-за повествовательного стиля записей (полное
  цитирование каждого раунда skeptic, а не итогового факта) и того, что Step 6 потребовал 5 раундов
  skeptic, Step 7 Phase A — 3, Phase B — 2, при этом почти все находки были bookkeeping-несоответствиями
  документов, а не дефектами кода. (5) Агенты (`planner`/`skeptic`/`frontend-architect`/`qa-reviewer`/
  `ux-strategist`/`motion-engineer`) — не дублируют друг друга, каждый — свой фокус; не найдено
  оснований удалять кого-либо. (6) Skills — 7 Allqbit-специфичных (`allqbit-*`, `browser-qa`,
  `frontend-quality`, `interactive-office`) уже минимальны (12–24 строки, каждый — свой набор docs
  для своего ракурса ревью) — не дублируют друг друга. `motion-framer`/`ui-ux-pro-max` — общие
  референс-skills не по теме workflow, вызываются ситуативно, не часть цикла — вне аудита.
- Принятое решение: (a) `CLAUDE.md` "Mandatory strict execution protocol" переписан — один Master
  Plan (создаётся `planner`'ом один раз, неизменен кроме Amendment), явный запрет re-plan перед уже
  описанным в плане шагом, единый цикл "TODO → реализация → проверки → skeptic → фикс → следующий
  TODO", `planner` вызывается только в 3 случаях (создание/пересмотр Master Plan, изменение scope,
  повторный FAIL/BLOCKED на одном шаге), skeptic обязан различать blocking и non-blocking находки
  (non-blocking — формулировки/bookkeeping — не блокируют сами по себе), milestone review — только на
  реальных milestone, не на каждом шаге. (b) `.claude/skills/strict-plan-execution/SKILL.md` сокращён
  до короткой ссылки на `CLAUDE.md` (сам файл не удалён — попытка `rm -rf` заблокирована защитным
  механизмом Claude Code как необратимое действие без явного поимённого согласия пользователя;
  де-дублирование достигнуто сокращением содержимого, не удалением файла). (c) `.claude/README.md`
  "Strict orchestration" и `.claude/agents/skeptic.md`/`planner.md` приведены в соответствие с новым
  протоколом (короче, без повтора чек-листов, что уже есть в `CLAUDE.md`). (d) `WORKPLAN.md`
  "Approval" и `WORKLOG.md` (шапка) дополнены пометками о новом, более компактном формате будущих
  записей — старая история не переписывается. (e) `docs/17-execution-protocol.md`,
  `docs/18-work-plan-template.md`, `docs/19-work-log-template.md` — рекомендованы к удалению как
  чистое дублирование, но фактическое удаление заблокировано тем же защитным механизмом (нужно явное,
  поимённое согласие пользователя на удаление именно этих файлов) — ждёт ответа пользователя.
- Последствия: шаги 1–7 (уже пройденные) не переоткрываются задним числом. Шаги 7.2/7.3/7.5/8/9
  реализуются напрямую по уже написанному в `WORKPLAN.md` объёму, без нового вызова `planner`, если
  не меняется scope. Существующий текст `WORKPLAN.md`/`WORKLOG.md`/`DECISIONS.md` не сокращался и не
  архивировался этой правкой (риск потери реальной истории) — предложено пользователю отдельно, как
  опция, не выполнено без согласия.
- Skeptic review: не проводился — это правка самого процесса (мета-уровень), не шаг `WORKPLAN.md`;
  собственный новый пункт 5 протокола ("planner только в 3 случаях") сюда не подпадает и на review
  самого этого решения не распространяется.
- Согласование пользователя: получено напрямую в диалоге 2026-07-16 — пользователь дал подробное
  прямое техническое задание (10 пронумерованных пунктов). Удаление `docs/17-execution-protocol.md`/
  `docs/18-work-plan-template.md`/`docs/19-work-log-template.md` отдельно подтверждено пользователем
  (`AskUserQuestion`, "Да, удалить все 3") после того, как первая попытка была заблокирована защитным
  механизмом Claude Code как необратимое действие без поимённого согласия — файлы удалены этой же
  правкой; ссылка на них в `WORKPLAN.md` "Source requirements" обновлена.

---

### 2026-07-16 — OQ-T1 переопределён: `interactionHint` скрывается и на Tablet

- Контекст: skeptic Phase A review плана Step 7.2 ("Overview full-screen (hide hero)") обнаружил, что
  Objective/AC4 этого шага (скрыть `interactionHint` на ВСЕХ ширинах) прямо противоречит уже
  `APPROVED` Step 7.5, где пользователь ранее ответил на **OQ-T1 = (b)** «Показывать как есть» —
  подсказка на Tablet (768–1279px) должна была остаться видимой без изменений. Ни planner (при
  подготовке Step 7.2), ни исходная запись "Overview: офис на весь экран, hero скрывается" (эта же
  дата, выше) не заметили и не обсуждали Tablet явно — пользователь тогда сверял только Desktop/
  Mobile-скриншоты. Skeptic вернул `BLOCKED` именно по этому пункту (`WORKPLAN.md` Step 7.2, Skeptic
  findings round 1) и потребовал явного решения пользователя, а не молчаливого выбора одной из сторон.
- Вопрос пользователю (`AskUserQuestion`): прятать `interactionHint` и на Tablet тоже (что требует
  формального Amendment к уже `APPROVED` Step 7.5, AC12), или оставить видимой на Tablet как уже
  решено (сузив Step 7.2 до Desktop+Mobile, без Amendment).
- Принятое решение: **прятать `interactionHint` и на Tablet** — единообразное поведение `overview` на
  всех breakpoint'ах. Это переопределяет прежний ответ OQ-T1 = (b) для Step 7.5 конкретно по этому
  пункту (остальные решения OQ-T1-соседних вопросов Step 7.5 — ширина rail, hover-gating
  `overviewProblem`, тап-таргеты — не затрагиваются, они не были предметом этого вопроса).
- Последствия: требуется формальный `Amendment 5` в `WORKPLAN.md` "## Plan amendments", меняющий уже
  `APPROVED` Step 7.5 (Objective/In scope пункт про `interactionHint`, Expected files, Acceptance
  criterion 12, "Open questions (Step 7.5)" OQ-T1) — по `CLAUDE.md` "Never change the plan after
  approval without recording and approving the amendment". После Amendment 5 Step 7.2 возвращается из
  `BLOCKED` в `PROPOSED`, готовый к повторному skeptic Phase A review (round 2) с уже устранённым
  противоречием.
- Skeptic review: находка обнаружена skeptic'ом (Step 7.2 Phase A round 1, `BLOCKED`); формальный
  Amendment 5 и обновлённый Step 7.2 подлежат отдельному skeptic review (round 2) перед `APPROVED`.
- Согласование пользователя: получено напрямую через `AskUserQuestion` 2026-07-16, вариант «Прятать и
  на Tablet (Recommended)».

---

### 2026-07-16 — Глобальный возврат в hero + тэглайн в шапке (прямое поручение, без предварительного Phase A)

- Контекст: пользователь визуально проверял живой результат Step 7.2 (скриншот overview) и указал:
  из `overview` нет способа вернуться на исходный `hero`-экран, кроме перезагрузки страницы —
  реальный, ранее не замеченный пробел в навигации (Step 7.2 сам умышленно скрыл hero, но не добавил
  способ вернуться). Пользователь дал два прямых поручения одним сообщением: (1) кнопка «Вернуться в
  офис» сверху под логотипом, возвращающая на главный экран; (2) текст «Помогаем экономить ДЕНЬГИ и
  ВРЕМЯ» посередине шапки. Явная инструкция: "Сначала это выполни. С моей стороны будут ещё
  замечания" — прямой приказ реализовать немедленно, без предварительного planner Phase A.
- Процессное решение: полноценный planner Phase A пропущен по прямому указанию пользователя
  (прецедент отличается от Step 7.2/7.3, где Phase A проводился до реализации) — но реализация
  выполнена с тем же качеством проверки, что и Phase B любого другого шага: полный набор
  verification commands реально прогнан, добавлены unit/e2e тесты на новую логику, skeptic review
  не пропущен (не подлежит пропуску ни при каких обстоятельствах, `CLAUDE.md`). Задним числом
  зафиксирован как новый шаг `WORKPLAN.md` ("Step 7.4"), Objective/Expected files/AC описаны по
  факту уже сделанного — честно помечено как ретроактивная документация, не как план, написанный до
  кода.
- Техническое решение (найдено при реализации, не поручение пользователя буквально): кнопка
  видна во всех состояниях, КРОМЕ `hero` (в `hero` возвращаться уже некуда); клик — новое действие
  редьюсера `RETURN_TO_HERO`, мгновенный переход в `hero` из любого состояния (включая
  `department-active`), с очисткой `activeDepartmentId`/URL и переносом focus на заголовок hero
  (тот же паттерн, что focus-management при открытии отдела/после `ACTIVATE_CTA`). Технически это
  потребовало архитектурной правки: `Header` был статичным server-компонентом вне `OfficeMachine`
  (не имел доступа к состоянию редьюсера) — перенесён внутрь `OfficeMachine.tsx` (уже `"use client"`)
  как первый sibling `<main>`, чтобы получить `view`/`dispatch` через обычные пропсы, без Context
  (сохранена валидная семантика `<header>`/`<main>` как соседей, не вложенных друг в друга).
  `tagline`/`returnToOfficeLabel` — новые поля `HomepageCopy` (`data/homepage-copy.json`,
  `content/types.ts`, `content/schema.ts`), не хардкод в компоненте (`CLAUDE.md` "Keep content
  separate from components").
- **Известное напряжение с `CLAUDE.md` Copy rules, зафиксировано честно, не проигнорировано
  молча:** "Do not make unsupported promises about... savings". Тэглайн «Помогаем экономить ДЕНЬГИ
  и ВРЕМЯ» — прямое, не подкреплённое расчётом заявление про экономию (в отличие от существующего
  headline «Найдём, где ваш бизнес теряет время и деньги», который говорит про обнаружение потерь,
  не про гарантию экономии). Это прямая, явная формулировка пользователя (не предположение
  ассистента) — реализована как поручено; конфликт зафиксирован здесь для прозрачности, не решён
  односторонне (ни отказом реализовать, ни молчаливой заменой текста).
- Skeptic review: не проводился до реализации (Phase A пропущен по указанию пользователя) —
  обязателен после реализации (Phase B review), как и для любого другого шага.
- Согласование пользователя: сама инструкция и есть согласование — получено напрямую в диалоге
  2026-07-16.

---

### 2026-07-16 — Возврат кнопки из Header в панель офиса (правка после Step 7.4)

- Контекст: Step 7.4 (кнопка «Вернуться в офис» в `Header`) прошёл skeptic Phase B `PASS` и ждал
  подтверждения пользователем. Пользователь, ещё до подтверждения, указал: "Я тебе неправильно
  поставил задачу. Кнопка «Вернуться в офис» должна быть не в шапке, а в окне ниже. Пусть она
  просто будет над подразделениями (выше «Дирекция»). Измени. Шапку верни как была." — прямое
  признание, что исходная постановка задачи была неточной, с новой, более конкретной локацией.
- Уточняющий вопрос ассистента (`AskUserQuestion`): тэглайн «Помогаем экономить ДЕНЬГИ и ВРЕМЯ»,
  добавленный тем же поручением, тоже под откат вместе с "верни шапку как была", или остаётся?
  Ответ: **убрать только кнопку** — тэглайн остаётся в `Header`.
- Принятое решение: кнопка «Вернуться в офис» перенесена из `Header` в `OfficeExperience` —
  рендерится первым элементом в overview-ветке, над сеткой карточек отделов (структурно выше
  "Дирекция", т.к. это первая карточка по сортировке `officeZones`, не хардкод конкретного
  отдела). Видна **только в `overview`**, не в `department-active` — структурное следствие
  того, что overview-ветка (карта + карусель + теперь кнопка) вообще не рендерится, когда отдел
  открыт (та же ветка, что уже скрывает `interactionHint`/`OfficeSemanticMap` в `department-active`
  с самого Step 6). Из открытого отдела путь назад в hero стал двухшаговым: «Закрыть» → overview →
  «Вернуться в офис» → hero — не заявлено пользователем явно, но прямое следствие того, что кнопка
  теперь живёт в overview-специфичной части разметки, а не в общем для всех состояний `Header`.
  `Header` упрощён обратно: единственный проп — `tagline`, кнопка/пропы `showReturnButton`/
  `onReturnHome` удалены.
- Последствия: `WORKPLAN.md` Step 7.4 переведён из `PASSED` обратно в `AWAITING_SKEPTIC`
  (round 2) — это не новый Amendment (шаг ещё не был `COMPLETED`, значит формальный Amendment не
  требуется по прецеденту "correction iteration", уже применённому в Step 7 Entry 8), но требует
  повторного skeptic Phase B review, так как поведение (область видимости кнопки, число шагов
  возврата из department-active) реально изменилось, а не только косметика. `docs/05` "## Возврат
  в hero" честно переписан под новое поведение. Тесты обновлены: `header.test.tsx` упрощён под
  новый `Header`; `office-experience.test.tsx` — кнопка теперь тестируется здесь (видна+работает в
  overview, отсутствует в department-active); `office-overview.spec.ts` — тест прямого возврата из
  `?department=<id>` переписан под двухшаговый путь.
- Skeptic review: не проводился для этой конкретной правки отдельно — часть round 2 Phase B review
  Step 7.4 целиком (план и код review вместе, по тому же прецеденту, что Amendment-правки в
  предыдущих шагах).
- Согласование пользователя: получено напрямую в диалоге 2026-07-16; уточнение по тэглайну — через
  `AskUserQuestion`, вариант «Убрать только кнопку из шапки (Recommended)».

---

### 2026-07-17 — Переименование кнопки: «Вернуться в офис» → «Выйти из офиса»

- Контекст: прямое поручение пользователя переименовать текст кнопки, без изменения поведения.
- Принятое решение: `data/homepage-copy.json` — значение `returnToOfficeLabel` изменено на «Выйти
  из офиса»; имя самого поля (`returnToOfficeLabel`, в `content/types.ts`/`content/schema.ts`,
  пропы `OfficeExperience`) не переименовано — это внутренний идентификатор, не обязан буквально
  совпадать с текстом копии (тот же принцип, что уже применялся к другим полям `HomepageCopy`).
  Обновлены комментарии в коде (`OfficeMachine.tsx`, `OfficeMachine.module.css`) и честная
  документация (`docs/05-homepage-state-machine.md` "## Возврат в hero"), а также форвард-смотрящие
  описания в `WORKPLAN.md` Step 7.4 (Objective/In scope/Out of scope/Acceptance criteria/Risks).
  Тесты, использующие `copy.returnToOfficeLabel` через `getHomepageCopy()`, подхватывают новое
  значение автоматически; тест с захардкоженным литералом
  (`office-experience.test.tsx`) обновлён вручную.
  Исторические записи `WORKLOG.md`/`DECISIONS.md`, описывающие прошлые раунды review (когда текст
  кнопки ещё был «Вернуться в офис»), **не переписаны** — они честно фиксируют, что было проверено
  на момент review, это не текущая спецификация.
- Skeptic review: см. отдельный Phase B review этой правки (чисто контентное изменение, без
  изменения логики/структуры).
- Согласование пользователя: сама инструкция и есть согласование — получено напрямую в диалоге
  2026-07-17.

---

### 2026-07-17 — Step 7.3: ответы на OQ-P1–OQ-P6

- Контекст: `planner` подготовил Phase A план для Step 7.3 (панель боль/выгода), skeptic Phase A
  review — `PASS`. Шесть открытых вопросов заданы пользователю напрямую через `AskUserQuestion`
  (см. `WORKPLAN.md` "## Open questions (Step 7.3)" для полного текста вариантов).
- **OQ-P1 (источник фото) = вариант (a), с уточнением пользователя.** Пользователь: «У тебя в папке
  есть references, в нём лежат фотографии. Эти фотографии используются как полноразмерные так и для
  миниатюр.» — то есть реальные `references/**/*.png` используются и как общий фон офиса
  (полноразмерно), и как источник для миниатюр в `DepartmentNavigationRail` (уменьшенные версии тех
  же изображений, не отдельные файлы). Расходится с рекомендацией исполнителя (CSS-заглушка) —
  пользователь сознательно выбрал реальные фото сейчас, а не позже. Технический механизм (прямой
  статический импорт `next/image` из `references/`, без ручного копирования в `public/` и без
  ручного предварительного сжатия — оптимизация размера per-use происходит через встроенный
  Image Optimization пайплайн Next.js) — уточняется при реализации, не требует отдельного
  согласования.
- **OQ-P2 (дефолт панели выгоды) = вариант (a), с уточнением пользователя.** Пользователь: «По
  умолчанию №1 при открытии. Всегда сбрасывается на это состояние (№1), если выбран другой отдел
  или возврат в предыдущий. То есть при выборе отдела показываются все боли и сразу выбрана первая
  боль с решением по ней в правой части.» — совпадает с рекомендацией исполнителя; согласовано и с
  ответом на OQ-P3 (сброс на переключении, не «липкая» память).
- **OQ-P3 (место хранения состояния выбора) = вариант (a)** — локальное `useState`, сбрасывается на
  пункт №1 при переключении между уже открытыми отделами (через remount-key или эффект, привязанный
  к `department.id`). Совпадает с рекомендацией исполнителя.
- **OQ-P4 (клавиатурная модель) = вариант (a)** — обычный последовательный Tab-порядок, нативные
  `<button>`, Enter/Space — без ARIA tablist/listbox. Совпадает с рекомендацией исполнителя, без
  нового паттерна взаимодействия для проекта.
- **OQ-P5 (объём копирайтинга) = вариант (a)** — черновая (явно не финальная) копия боль/выгода
  пишется для всех 4 оставшихся отделов (Продажи/Поддержка/HR/Логистика — 20 пар) в рамках
  реализации этого шага, тем же тоном, что уже есть у «Дирекция». Совпадает с рекомендацией
  исполнителя.
- **OQ-P6 (Mobile-раскладка) = вариант (b)** — отдельный, специально спроектированный
  mobile-вариант (список боли на всю ширину, тап раскрывает/заменяет выгодой), а не переиспользование
  20/80-раскладки бок о бок. Совпадает с рекомендацией исполнителя.
- Последствия: OQ-P1 и OQ-P6 материально расширяют scope относительно того, что видел skeptic на
  Phase A round 1 (там оба пункта были явно помечены как «решится по итогу OQ» с минимальным
  фиксированным содержанием) — реальные фото-ассеты вместо заглушки и отдельный mobile-компонент
  вместо условной регрессионной проверки. `WORKPLAN.md` Step 7.3 переписан с учётом всех 6 ответов;
  требуется второй раунд skeptic Phase A review перед переводом в `APPROVED` (тот же прецедент, что
  Step 7 после ответов на OQ-M1–OQ-M4, когда ответ разошёлся с default-черновиком).
- Skeptic review: см. `WORKLOG.md` Step 7.3, round 2 (после этой записи).
- Согласование пользователя: получено напрямую через `AskUserQuestion`, 2026-07-17 (два раунда по
  4 и 2 вопроса).

---

### 2026-07-17 — Step 8: visual layer — это фотослой, а не WebGL (разрешение отложенного вопроса)

- Контекст: начиная со Step 3 в плане висел явно зафиксированный вопрос без ответа — «что вообще
  является visual layer, для которого будущий шаг делает fallback» (Step 3 Risks: «`OfficeVisualLayer`
  без владельца до Step 8»; Step 5/Step 6 Out of scope: «owner — перед стартом Step 8»). Ответ
  требовался до старта Step 8, потому что от него зависит само содержание AC 2 («Visual layer error
  не блокирует контент»).
- **Решение: visual layer этого milestone — фотослой `next/image`** (общий фон офиса позади
  10/90-раскладки + 5 миниатюр отделов в `DepartmentNavigationRail`, введены в Step 7.3 по OQ-P1).
  `OfficeVisualLayer`, WebGL и Canvas не вводятся ни одним шагом Steps 1–9.
- Это **подтверждение уже зафиксированного, а не новое решение**: Steps 3/5/6 Out of scope уже
  перечисляли «WebGL/Canvas — не вводятся», OQ-A исключил GSAP, OQ-C исключил `DepartmentScene`/
  `BeforeAfterSequence`, а CLAUDE.md «First milestone» прямо говорит «No final 3D art». Поэтому
  оформлено записью решения, а не Amendment: ничего утверждённого не отменяется.
- Следствие для AC 2: «ошибка визуального слоя» = ошибка загрузки фото, а не падение WebGL-контекста.
  Обрабатывается локально (`OfficePhoto` + `onError` → нейтральный плейсхолдер). Машинное состояние
  `error-fallback` и событие `SCENE_ERROR` из `docs/05` сознательно **не** реализуются: все фото
  декоративны (`alt=""`), а требуемый ими результат — «статичный overview с пятью HTML-кнопками» —
  уже является базовым состоянием продукта, а не аварийным. `docs/05` приведена в соответствие
  честной правкой (прецедент Step 3).
- Владелец на будущее: если настоящая WebGL/Canvas-сцена появится в отдельной макро-фазе (`docs/13`
  Этап 2/9), вопрос `SCENE_ERROR`/`error-fallback` открывается заново — тогда у сцены будет ошибка,
  которую действительно нужно поднимать до состояния.
- Согласование пользователя: прямая инструкция 2026-07-17 «Приступай к Step 8. Сразу выполняй без
  дополнительного планирования» — санкция на старт без Phase A. Само содержание решения пользователю
  отдельным вопросом не задавалось, так как оно не расширяет и не сужает утверждённый scope; при
  несогласии оно отменяемо без переделки кода за пределами Step 8.

### 2026-07-17 — Step 9: scope = axe-скан + фиксация back/forward (базовые AC уже покрыты)

- Контекст: при старте Step 9 («Browser acceptance tests») инспекция показала, что пять именованных
  потоков AC1 (desktop, keyboard, mobile, URL, reduced-motion) и «нет ошибок в консоли» (AC2) **уже
  покрыты 93 e2e-тестами**, добавленными по ходу Steps 3–8 (каждый шаг вносил свои потоки и свой
  `no console errors`-тест). То есть базовые AC1/AC2 Step 9 были выполнены прошлыми шагами ещё до его
  старта. Возник реальный вопрос: что Step 9 должен добавить, а не продублировать.
- Найдено, что план **четырежды** явно маршрутизировал в Step 9 как owner два конкретных отложенных
  пункта: **(A)** автоматизированный axe-скан (Step 3 Risks «владелец назначен»; Step 4 и Step 8 Out
  of scope — «owner Step 9») и **(B)** поддержку/фиксацию кнопок «назад»/«вперёд» браузера
  (`popstate`) (Step 5/6 Out of scope; Step 8 Out of scope — «owner Step 9»).
- **Решение (выбор пользователя через `AskUserQuestion`, 2026-07-17, вариант «Axe-скан + фиксация
  back/forward»):** Step 9 добавляет (A) автоматизированный axe-скан трёх состояний × двух ширин с
  порогом serious+ и (B) acceptance-фиксацию **текущего** поведения истории браузера — **без
  реализации** SPA-истории отделов. Отвергнуты: «только консолидация» (дублирует существующее
  покрытие) и «реализовать `popstate`» (противоречит решению OQ-B `replaceState`, потребовал бы
  отдельного Amendment и, вероятно, выходит за рамки low-fidelity-прототипа).
- Почему это раскрытие, а не Amendment: Objective («зафиксировать основные потоки Playwright») и оба
  AC не менялись; axe и popstate-фиксация — пункты, которые план сам адресовал в Step 9. `popstate`
  как поведение не меняется — тест лишь фиксирует уже действующий `replaceState`-инвариант.

### 2026-07-17 — Step 9: затемнение токена `--color-accent-warm` (реальная axe-находка контраста)

- Контекст: автоматизированный axe-скан (пункт A выше) нашёл **реальное** нарушение
  `color-contrast` уровня serious: CTA-кнопки (`HeroCopy.primaryCta`, `DepartmentCTA.cta`) —
  белый текст `#ffffff` на акцентном фоне `#b5793a` = контраст 3.64:1, ниже порога WCAG AA 4.5:1 для
  обычного текста. Нарушение проявлялось в hero и department-active (в overview CTA скрыты по Step
  7.2, поэтому overview axe-скан проходил).
- **Решение: затемнить единый токен `--color-accent-warm` с `#b5793a` до `#a0642b`** (контраст с
  белым 4.82:1). Это одна правка в `tokens.css`, чинящая обоих нарушителей сразу, так как оба
  используют общий токен.
- Почему безопасно (аудит всех 8 применений токена): фон-с-белым-текстом — только эти два CTA (чинятся
  затемнением). Прочие применения — рамка (`.painButtonSelected`, `.current` — фон graphite-100),
  текст-на-светлом (`.currentMark` на graphite-100), focus-outline (globals/DepartmentCopy). Во всех
  них затемнение акцента **повышает** контраст либо косметически нейтрально. Ни одной пары «тёмный
  текст на акценте» или «акцент как светлый фон» не существует — регресса контраста нет by
  construction; подтверждено повторным зелёным axe-прогоном всех 6 состояний.
- Почему это решение, а не Amendment: значение токена помечено «предварительным» ещё в Step 1 и явно
  ждёт финальной палитры от art direction (`docs/02`). Правка не преждирует палитру, только вводит
  обязательную нижнюю границу контраста, которой финальная палитра всё равно обязана удовлетворять.
  Пользователь заранее санкционировал починку найденных нарушений (выбор scope выше). Единственное
  user-visible следствие — CTA-кнопки чуть темнее; полностью отменяемо `git revert`.

---

### 2026-07-18 — Milestone 1 review: gate PASS + приведение docs/06 к модели pain/gain

- Контекст: конец 9-шагового milestone (Steps 1–9 + 7.2–7.6 — все `COMPLETED`). По `CLAUDE.md` §6
  запущен milestone review — 4 независимых read-only ревьюера (`frontend-architect`, `qa-reviewer`,
  `ux-strategist`, `motion-engineer`). Все вернули **Blocker/Critical нет**; `qa-reviewer` независимо
  перепрогнал quality gate (133 unit + 101 e2e, все команды exit 0). Gate: **PASS**. Единственная
  находка уровня Major, требующая правки в этом milestone (не бэклог) — рассинхрон source-of-truth:
  `docs/06-department-content.md` описывал устаревший шаблон отдела (проблема → 3 симптома → «До» →
  автоматизация → «После» → результаты), тогда как код и `data/` перешли на модель `painPoints`
  (ровно 5 пар «боль → выгода») ещё в Step 7.3. `docs/06` оказался единственным source-of-truth
  документом, не обновлённым Step 7.3, что нарушает `CLAUDE.md` «Never silently contradict approved
  documents».
- Варианты: (a) починить `docs/06` сейчас, затем закрыть milestone; (b) закрыть как есть, отправив
  рассинхрон в бэклог; (c) пока не закрывать.
- Решение (выбор пользователя через `AskUserQuestion`, 2026-07-18, вариант «Починить docs/06, затем
  закрыть»): `docs/06` приведён к модели pain/gain. Чтобы не создавать новый рассинхрон docs↔data,
  документ описывает **модель и редакционные правила** и делегирует каноничные значения
  `data/departments.json` (источник структурированного контента по `CLAUDE.md`), оставляя один
  иллюстративный пример-отдел. Тот же приём и датированная пометка супрессии, что применил Step 7.3 к
  `docs/12`. Раздел «Технический слой» сохранён (не противоречит коду, forward-looking).
- Последствия: source-of-truth рассинхрон устранён. Major B (CTA — фокусируемый no-op, отсутствует
  lead-destination) и все Minor-находки review осознанно отправлены в бэклог следующего этапа —
  запланированные ограничения low-fidelity milestone (backend/CRM/форма вне scope, OQ #15/#9
  открыты). Milestone 1 закрыт (все шаги уже «Выполнено» в `README.md`/`WORKPLAN.md`; отдельного
  WORKPLAN-шага milestone review не имеет — это gate, не шаг).
- Skeptic review: не применяется — milestone review сам является независимой read-only приёмкой
  (4 ревьюера заменяют одиночного skeptic на границе milestone); правка `docs/06` документная и в
  пределах уже принятого и реализованного решения Step 7.3.
- Согласование пользователя: да, 2026-07-18 через `AskUserQuestion` («Починить docs/06, затем
  закрыть»).

---

### 2026-07-18 — Старт milestone «Этап 2 — Art direction»: план и решения по арт-дирекшну

- Контекст: после закрытия Этапа 1 пользователь выбрал движение по плану на Этап 2 (art direction) и
  сообщил, что изображения уже в проекте (`references/**`, подписаны) — брать в работу. `planner`
  составил PROPOSED-план (Steps 10–16, сохранён в `WORKPLAN.md`); `skeptic` Phase A вернул `PASS`
  (5 non-blocking findings внесены). Открытые вопросы вынесены пользователю.
- Решения пользователя (`AskUserQuestion`, 2026-07-18):
  - **OQ-A2-1 = (a):** overview — настоящее мастер-фото офиса на весь экран, отделы как зоны-хотспоты
    поверх фото (immersive), а не фото-подложка под карточками.
  - **OQ-A2-8 = (a):** у каждого из 5 отделов — своя сцена в `department-active`, не общий
    continuity-фон (пересматривает решение `docs/03`/Step 7.3 об общем фоне; правится в Step 13).
  - **OQ-A2-7 = «черновой арт»:** 6 изображений `references/**` — черновые, могут
    меняться/перерисовываться, НЕ финальные. Следствие: milestone делается на черновиках осознанно —
    структура арт-независима; Step 10 (пайплайн) удешевляет замену draft→final; Step 11 (палитра) и
    Step 12 (координаты зон) — предварительные, легко перекалибруемые, не золотятся под черновик;
    контрастные полы WCAG AA держатся независимо от арта. Не blocker.
- Принятые по умолчанию технические решения (озвучены пользователю, возражений нет):
  - **OQ-A2-4:** `sharp` devDependency + скрипт генерации; форматы WebP+AVIF; ширины 768/1280/1536
    (+1920 при нужде); responsive-отдача ручным `<picture>`/srcset (оригиналы на лету не
    оптимизируются — порт-эксгейст, `departmentPhotos.ts`).
  - **OQ-A2-5:** минимальные функциональные маркеры логотипа (стрелка боль→выгода, active-маркер
    рельса); «линии передачи данных» — Этап 3.
  - **OQ-A2-6:** контролируемый transform-зум + crossfade между сценами; pointer-параллакс НЕ вводится
    (как в Этапе 1).
  - **Нумерация:** сквозная `Step 10–16` (skeptic подтвердил — коллизий нет).
  - **OQ-A2-3 (палитра) / OQ-A2-2 (координаты):** не решаются абстрактно — предъявляются пользователю
    в браузере на Step 11/Step 12; помечены «предварительные до финального арта».
- Последствия: план обновлён под все решения (блок «Open questions … RESOLVED», поправка на черновой
  арт, правки Step 11/12/16). Все preconditions к `APPROVED`, кроме явного «старт реализации»,
  закрыты.
- Skeptic review: план прошёл Phase A `PASS` (2026-07-18); non-blocking findings 1–5 внесены до
  утверждения. Phase B (review исполнения) обязателен на каждом шаге.
- Согласование пользователя: решения по OQ — да (2026-07-18, `AskUserQuestion`); перевод плана в
  `APPROVED` и старт Step 10 — **ожидает явного подтверждения пользователя**.
