# WORKPLAN

## Amendment 44 — публикация шести исследовательских статей `/blog` (2026-07-25)

- Status: `COMPLETED` (skeptic: `PASS`; blocking findings: none).
- User approval: прямое ТЗ пользователя и переданный `deep-research-report (2).md`.
- Scope:
  1. Импортировать шесть финальных статей без сокращений и редакторской подмены исходного текста,
     сохранив существующие slugs.
  2. Превратить `/blog` в индекс опубликованных материалов, добавить оглавление, источники, связанные
     статьи, CTA и адаптивное отображение внутри существующей notebook-композиции.
  3. Добавить уникальные metadata, canonical, Open Graph/Twitter, `BlogPosting`, `BreadcrumbList`,
     `ItemList` и все семь blog URL в sitemap.
  4. Проверить целостность импорта по SHA-256, навигацию, history, mobile overflow, accessibility,
     structured data и production build.
- Out of scope: изменение текста исследования, выдумывание отсутствующих URL источников, создание
  отсутствующих внутренних страниц `/examples` и `/ai-assistant-for-business`, редизайн других страниц.
- Verification: scoped formatting/lint, typecheck, unit tests, build, production blog e2e,
  desktop/tablet/mobile captures и независимый read-only skeptic.

### Step 44 — final research publication

- Status: `COMPLETED` (skeptic: `PASS`; blocking findings: none).
- Reviewer: независимый read-only skeptic после локальных и браузерных проверок.

## Amendment 43 — визуальная и техническая основа `/blog` (2026-07-25)

- Status: `COMPLETED` (skeptic round 2: `PASS`; blocking findings: none).
- User approval: прямое ТЗ пользователя от 2026-07-25 и переданная фотография рабочего стола.
- Scope:
  1. Подготовить исходный PNG и производные WebP/AVIF без нарушения пропорций.
  2. Реализовать `/blog` и шесть `/blog/[slug]` из единого чернового конфигурационного файла.
  3. Переиспользовать общий Header, изменив только ссылку «Блог» на `/blog`.
  4. Создать постоянную desktop-навигацию, отдельный скролл статьи, мобильный disclosure и обычный
     document scroll на mobile.
  5. Добавить короткую смену «листа», reduced-motion crossfade, draft/noindex и доступную
     клавиатурную навигацию.
- Out of scope: реальные экспертные статьи, финальный SEO/GEO, JSON-LD BlogPosting, изменения
  продуктов, отделов, главной страницы, FAQ и Контактов.
- Verification: scoped Prettier/ESLint, typecheck, unit tests, build, blog production e2e,
  screenshots 1440/1024/390 и независимый read-only skeptic.
- Risk: существующий dirty worktree; rollback — удалить новые blog-файлы/ассеты и отменить только
  точечные hunks ссылки Header/test.

### Step 43 — notebook blog foundation

- Status: `COMPLETED` (skeptic round 2: `PASS`; blocking findings: none).
- Reviewer: независимый read-only skeptic после локальных, production e2e и визуальных проверок.

## Amendment 42 — SEO/GEO-доработка `/products` и продуктовых страниц (2026-07-25)

- Status: `COMPLETED` (skeptic round 2: `PASS`).
- User approval: прямое ТЗ пользователя от 2026-07-25.
- Scope:
  1. Добавить на `/products` один компактный видимый H1 и описание внутри существующей строки
     управления, сохранив фото, кнопки и one-window композицию.
  2. Заменить только `summary` десяти продуктов утверждёнными самостоятельными описаниями.
  3. Представить минимальные цены через `PriceSpecification.minPrice`: по три `Offer` для продуктов
     1–9 и один `Offer` для n8n; поддержку оставить отдельной от минимальной цены.
  4. Унифицировать пользовательское и SEO-написание бренда как `QBit-Studio-Ai`, не меняя домен,
     URL, package name и технические имена.
  5. Сохранить вкладки с JavaScript и показать четыре смысловых раздела последовательно без
     JavaScript, без дублирования контента.
- Out of scope: дизайн лаборатории и карточек, фото, зоны, анимации, цены, slugs, canonical, sitemap,
  Блог, FAQ, Контакты и их ссылки.
- Verification: scoped lint, `npm run typecheck`, `npm run build`, product unit/e2e, browser matrix
  1440/1024/768/390, no-JS HTML fallback, schema/brand audit и независимый read-only skeptic.
- Risk: существующий dirty worktree и фиксированная высота overview; rollback — отменить только
  hunks Amendment 42.

### Step 42 — products SEO/GEO, intro and progressive enhancement

- Status: `COMPLETED` (skeptic round 2: `PASS`; blocking findings: none).
- Reviewer: обязательный независимый read-only skeptic после локальных и браузерных проверок.

## Amendment 39 — компактная шапка и product control row (2026-07-25)

- Status: `COMPLETED` (skeptic: `PASS`).
- User approval: прямой запрос 2026-07-25.
- Reason: освободить больше вертикального пространства под визуальную сцену без уменьшения шрифта
  меню и без ухудшения размеров интерактивных элементов.
- Scope:
  1. Установить высоту общей шапки в диапазоне 60–64 px и сократить только вертикальные отступы.
  2. Сохранить телефонную кнопку высотой 40–44 px и существующий размер шрифта меню.
  3. Уплотнить строку `/products` «На главную / Выберите продукт / Получить разбор».
  4. Проверить desktop/mobile размеры, отсутствие переполнения и существующие Header/products тесты.
- Out of scope: тексты, горизонтальная компоновка, навигационные ссылки, цвета, шрифты, сцены и
  страницы отделов.
- Verification: scoped Prettier/ESLint, typecheck, Header unit, products production e2e с измерениями,
  production build, visual capture и skeptic-review.

## Amendment 38 — визуальная локация «Продукты и стоимость» (2026-07-24)

- Status: `COMPLETED` (skeptic round 2: `PASS`).
- User approval: получено прямым запросом и передачей 11 фотографий 2026-07-24.
- Reason: добавить отдельную интерактивную лабораторию продуктов в визуальном языке уже
  реализованного офиса, не меняя существующую страницу отделов.
- Scope:
  1. Оптимизировать общий кадр и десять крупных планов в AVIF/WebP.
  2. Создать единый конфигурационный файл с временными названиями, URL и процентными зонами.
  3. Реализовать `/products` и `/products/product-01` … `/products/product-10`, камерный вход,
     короткое переключение, прямое открытие URL и browser history.
  4. Переиспользовать Header, дизайн-токены, паттерны хотспотов/фотослоя/reduced motion без правок
     существующей страницы отделов.
  5. Проверить desktop/tablet/mobile, клавиатуру, отсутствие переполнения и загрузку изображений.
- Out of scope: описания, цены, выгоды, SEO-блоки, FAQ, калькулятор и финальные названия продуктов.
- Verification: `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run test`,
  `npm run build`, продуктовые e2e и браузерный визуальный аудит.
- Risk: рабочее дерево уже содержит несвязанные изменения; mitigation — новые product-файлы,
  производные изображений и точечные тесты, без переписывания office/department-компонентов.

## Task

Создать первый low-fidelity прототип интерактивной главной страницы Allqbit.

## Goal

Пошагово, с обязательным skeptic-review каждого шага и явным утверждением пользователя,
получить рабочий low-fidelity прототип интерактивного офиса: full-office overview, пять
интерактивных зон, hover/focus/touch, выбор отдела, 10/90-раскладка, переключение отделов,
возврат к overview, клавиатурная доступность, mobile touch flow, reduced-motion поведение —
без финального 3D-арта, персонажей, backend, CRM и калькулятора (см. "Первое milestone" в
CLAUDE.md).

## Source requirements

- `CLAUDE.md` (Mandatory strict execution protocol, Architecture/Copy/Motion/Responsive/
  Accessibility/Performance rules, Quality gate, First milestone)
- `docs/00-product-brief.md` … `docs/16-open-questions.md` (`docs/17–19` — process-protocol
  duplicates of `CLAUDE.md`, удалены 2026-07-16, см. `DECISIONS.md` "Аудит и облегчение workflow")
- `data/departments.json`, `data/homepage-copy.json`, `data/office-zones.json`
- `references/` (визуальные референсы пяти отделов, overview, логотип)
- Пользовательские сообщения: запрос на планирование с обязательным ведением статуса в
  `README.md` тремя метками (Не приступили/В работе/Выполнено) и строго последовательным
  исполнением по одному шагу

## Approval

- Plan status: `APPROVED`
- User approved: `YES`
- Approved at: 2026-07-14
- Approved scope: весь скелет из 8 шагов утверждён как последовательность; **детальный план
  каждого шага 2–8 будет заново пройден через planner → skeptic review плана → отдельное
  утверждение пользователя непосредственно перед своим стартом** — сейчас утверждено начало
  исполнения только Step 1 в полном объёме, описанном ниже.
- **Пересмотрено 2026-07-16 (аудит workflow, см. `DECISIONS.md`):** правило выше — источник
  токен-затратного цикла "re-plan перед каждым шагом", устранённого этой правкой. Начиная с этой
  даты: любой ещё не начатый шаг (Step 7.2/7.3/7.5/8/9) реализуется напрямую по уже написанному в
  этом файле объёму — без повторного вызова `planner`, если только не меняется scope или skeptic не
  проваливает один и тот же шаг подряд несколько раз (`CLAUDE.md` "Mandatory strict execution
  protocol", пункт 5). Уже утверждённые/пройденные шаги (1–7) это правило не переоткрывает.
- Amendment 3 (2026-07-15): скелет шагов расширен с 8 до 9 по прямому решению пользователя (OQ-D
  = (b) — "Разбить на два шага", см. `DECISIONS.md`, 2026-07-15 "Step 5: разбиение на два
  отдельных шага WORKPLAN (OQ-D, Amendment 3)"). Единый черновик "Step 5 — Desktop 10/90 shell"
  формально разбит на "Step 5 — Department selection state machine" и "Step 6 — Desktop 10/90
  shell"; бывшие Step 6/7/8 сдвинуты на Step 7/8/9. Полная формальная запись — см. `Plan
  amendments` → `Amendment 3` ниже.
- Amendment 4 (2026-07-16): по решению пользователя (OQ-M1 = (c), см. `DECISIONS.md` 2026-07-16
  "Step 7: ответы на OQ-M1–OQ-M4") между Step 7 и Step 8 добавлен новый шаг "Step 7.5 — Tablet
  touch flow" под нелинейной меткой (Step 8/Step 9 не переименованы — обоснование см. `Plan
  amendments` → `Amendment 4`). "Step 7 — Mobile touch flow" одновременно переработан под
  OQ-M3 = (b) (горизонтальная карусель вместо вертикального списка). Скелет теперь — 9
  канонически пронумерованных шагов + Step 7.5. Полная формальная запись — см. `Plan amendments`
  → `Amendment 4` ниже.
- Amendment 5 (2026-07-16): по решению пользователя (`AskUserQuestion`, «Прятать и на Tablet
  (Recommended)», см. `DECISIONS.md` 2026-07-16 "OQ-T1 переопределён") исходный ответ OQ-T1 = (b)
  ("показывать как есть") для уже `APPROVED` "Step 7.5 — Tablet touch flow" отменён:
  `interactionHint` теперь скрывается на Tablet тоже, тем же безусловным CSS-правилом, что вводит
  черновой "Step 7.2 — Overview full-screen (hide hero)". Затронуты Objective/In scope/Expected
  files/Acceptance criterion 12/"Open questions" Step 7.5 — старый текст сохранён для истории,
  помечен переопределённым, не удалён. Полная формальная запись — см. `Plan amendments` →
  `Amendment 5` ниже.
- Amendment 6 (2026-07-17): по прямому запросу пользователя при визуальной проверке шапки между
  Step 7.5 и Step 8 добавлен новый шаг "Step 7.6 — Header: clickable logo + tagline
  scale/centering" (та же нелинейная схема нумерации, что у Steps 7.2–7.5; Step 8/Step 9 не
  переименованы). Шаг пересматривает решения по шапке, введённые `COMPLETED` Step 7.4, открыто —
  через Amendment, не правкой закрытого шага задним числом. Полная формальная запись — см. `Plan
  amendments` → `Amendment 6` ниже.


---

> ⚠️ **Архив.** Завершённое (Этапы 1–2, Steps 1–18) вынесено в `archive/WORKPLAN.archive.md`.
> Клод НЕ читает архив без явной просьбы пользователя. Ниже — только активная работа.

---
## Milestone: Этап 3 — Sales pilot

- Status: `APPROVED` (утверждён пользователем 2026-07-21 после skeptic-ревью плана: FAIL раунд 1 —
  одна блокирующая находка B1, словарь аналитики расходился с `docs/15`; исправлено инлайн вместе с
  N1–N4 до утверждения. Переплан не потребовался.)
- Источник: `docs/13-implementation-plan.md` строки 15–17 — «Этап 3 — sales pilot: полная сцена,
  до/после, copy, CTA, analytics, calculator prototype, performance и accessibility».
- Границы milestone: пилот выполняется на ОДНОМ отделе — «Продажи» (`sales`). Этапы 4–7 переносят тот
  же шаблон на остальные четыре отдела, поэтому каждый механизм (окно «Ваша выгода», графика сцены)
  строится ОБОБЩАЕМО (принимает данные отдела), а контентом подключается ТОЛЬКО «Продажи». Разовое
  решение под один отдел запрещено — оно провалит перенос на Этапах 4–7. (Калькулятор и аналитика
  исключены из пилота — Amendment 23.)
- Опора на сделанное (Steps 17–18, специально под Этап 3): реестр разделов
  `src/features/office-machine/sections.ts`; каталог сцен `src/components/office/officeScenes.ts`
  (СГЕНЕРИРОВАН, руками не править); общий сцен-слой `SceneCrossfade` внутри `.stage`
  (`OfficeExperience.tsx`); экран отдела `DepartmentExperience.tsx` + `PainGainPanel.tsx`.
- Deliverable «copy» (`docs/13:17`) НЕ вынесен в отдельный шаг намеренно: деловая копия «Продаж»
  финализируется в тех шагах, где она рождается — выгоды окна «Ваша выгода» (`customerBenefits`,
  наполняется на этапе копирайта), копия CTA/подтверждения (Step 26), — и каждый из этих шагов несёт
  ux-strategist в ревьюерах. Отдельная приёмка «copy» не теряется: она распределена, а не пропущена
  (skeptic-ревью плана, N4). (Тексты калькулятора отпали вместе со Step 22 — Amendment 23.)

### Решения пользователя, зафиксированные до утверждения milestone (2026-07-21)

1. **Форма «до/после» — DOM/SVG-диаграмма процесса** (не фото-сцены). Следствие: арт-зависимости
   нет; «до/после» строится из шагов процесса (`ProcessStep`), Step 19 не требует новых оригиналов.
2. **Полная графика сцены (Step 21) — включена** в пилот.
3. **Калькулятор — ПАНЕЛЬ внутри экрана «Продажи»** (не отдельный раздел, не модаль). Следствие:
   Step 23 НЕ идёт через реестр разделов, `OfficeSectionId`/`url-sync` не трогаются.
4. **Аллокация двух бесхозных дефектов Step 18:** кадр ≥1584px — в Этап 3 (Step 27); флак
   `scene-transition.spec.ts` под 6 воркерами — на Этап 10 (hardening).
5. **Каскад блоков (0/1/2/3 с) не трогается** (Amendment 16): «до/после» встраивается ВОКРУГ него, не
   меняя тайминги. Отдельного одобрения каскада не требуется (решение основной сессии, не Amendment).

### Открытые вопросы — ГЕЙТЫ на конкретные шаги (не блокируют старт milestone)

Три бизнес-решения нельзя вывести из докcodeбазы; каждое блокирует ТОЛЬКО свой шаг, не ранние. Milestone
может быть утверждён и начат с ранних шагов; эти гейты снимаются перед их шагами (ответ, меняющий
scope, оформляется Amendment):

- **OQ-3-C (гейт Step 22–23): СНЯТ (Amendment 23).** Калькулятор отменён — формула/входы не нужны.
- **OQ-3-A (гейт Step 24–25): СНЯТ (Amendment 23).** Аналитика отложена — транспорт/consent не решаем.
- **OQ-3-D (гейт Step 26): РАЗРЕШЁН (Amendment 23) — вариант D-a.** Адресат CTA пилота = тот же Telegram,
  что у формы «Ваша задача» (`contactHref` + `/api/task`). Отдельного on-site lead-capture в пилоте нет.

### Зависимость от отложенной формы «Ваша задача» (Step 12.7)

Форма в `AWAITING_SKEPTIC`, закрытие отложено пользователем на конец проекта. Она обещает «Свяжемся с
вами в Telegram», но канал обратной связи НЕ собирает. Step 26 планируется так, чтобы CTA пилота (а)
не зависел от разблокировки формы и (б) не обещал контакта, который не собирается.

### Порядок и параллелизм

- Строго последовательно: **19 → 20 → 21** (сделано 19, 20). Калькулятор (22→23) и аналитика (24→25)
  ОТМЕНЕНЫ (Amendment 23), их цепочек больше нет.
- Step 27 (кадр ≥1584px) без апстрим-зависимостей — в любой момент/параллельно.
- Step 26 (CTA) разрешён OQ-3-D (Telegram), от других шагов не зависит; Step 28 — от 20 и 21; Step 29 —
  от оставшихся UI-шагов (21, 26).

### Amendment 23 — сокращение объёма Этапа 3 (2026-07-22)

- Status: `APPROVED` (прямые решения пользователя 2026-07-22).
- Reason: пользователь пересмотрел состав пилота — калькулятор не нужен, аналитику отложить, лид-захват
  свести к уже существующему каналу «Ваша задача» (Telegram).
- Изменения по шагам:
  - **Step 22 (движок калькулятора) — `CANCELLED`.** Гейт OQ-3-C (формула/входы) снят как неактуальный.
  - **Step 23 (UI калькулятора) — `CANCELLED`.**
  - **Step 24 (слой аналитики) — `CANCELLED` (отложено).** Гейт OQ-3-A снят. `docs/15` остаётся будущей
    спецификацией, в пилоте не реализуется.
  - **Step 25 (разметка событий) — `CANCELLED` (отложено):** без Step 24 инструментовать нечего.
  - **Step 26 (CTA) — РЕ-СКОУП, OQ-3-D разрешён (вариант D-a).** Все заявки идут туда же, куда форма
    «Ваша задача» (Telegram, `contactHref` + `/api/task`). On-site лид-захвата в пилоте нет.
    Конверсионное событие аналитики из шага убрано (аналитика отложена). Шаг сводится к: единый адресат
    заявок = Telegram «Ваша задача», честная копия без обещаний, достижимость/axe.
  - **Step 28 (ленивая загрузка) — сокращён:** цель ленивости — графика сцены (Step 21); калькулятор
    исключён; «до/после» заменено окном «Ваша выгода» (Amendment 21). Зависимость от Step 23 снята.
  - **Step 29 (консолидация a11y) — сокращён:** из «собранного пилота» убран калькулятор; проверяются
    сцена + графика + окно «Ваша выгода» + CTA. Зависимости от Step 23/25 сняты.
  - Steps 21 и 27 — без изменений; закрытие milestone — без изменений (список механизмов обновлён:
    без калькулятора/аналитики).
- Impact: активные оставшиеся шаги Этапа 3 — **21, 26, 27, 28, 29** + закрытие. Гейты OQ-3-C/OQ-3-A
  сняты, OQ-3-D разрешён. `docs/13` (источник milestone) расходится в части calculator/analytics —
  отмечено там же; `docs/15` помечен как отложенный.
- Примечание по статусу: `CANCELLED` — плановое терминальное состояние ВНЕ рабочего enum
  (PROPOSED…COMPLETED); применяется только к снятым с плана шагам через эту поправку.
- User approval: получено 2026-07-22 (прямые «отменяй» по 22/23 + выбор «Отложить» по аналитике 24/25 +
  «заявки туда же, куда Ваша задача» по 26).

---

## Step 19 — Контент-модель «до/после» для «Продаж» (данные + схема)

- Status: `COMPLETED` (skeptic `PASS`, блокирующих нет; 3 non-blocking — упоминание CRM защитимо и
  консистентно с утверждённой копией отдела, абсолют «ничего не теряется» — надёжностная
  формулировка, ux-ревью копии происходит на Step 20 при отрисовке. Переход к Step 20 разрешён.)
- Objective: расширить контент-модель шагами процесса «до/после» и заполнить их ТОЛЬКО для «Продаж»,
  не нарушив инвариант «ровно 5 отделов, ровно 5 пар боль→выгода».
- Dependencies: Step 2 (типы/схема), Step 18 (реестр). Форма — DOM/SVG (решение 1), поэтому фото не
  нужны.
- In scope:
  - `src/content/types.ts`, `src/content/schema.ts`: тип `ProcessStep` (по `docs/12`/`docs/06`:
    `id/label/description/actor?/status?`) и ОПЦИОНАЛЬНЫЕ `beforeSteps?`/`automationSteps?` на
    `Department`. Опциональность обязательна: пока заполнены только «Продажи», остальные 4 отдела
    валидны без этих полей.
  - `data/departments.json`: `beforeSteps`/`automationSteps` заполнены ТОЛЬКО для `sales` (деловой
    язык `docs/06`/`CLAUDE.md`; без AI/CRM/BPM в клиентской копии; без неподтверждённых обещаний).
  - Валидационные тесты (`departments.test.ts`, `invalid-fixtures.test.ts`).
  - Честная правка `docs/12`/`docs/06`: `docs/12:14-15` сейчас объявляет `beforeSteps/automationSteps`
    ОБЯЗАТЕЛЬНЫМИ — исправить на ОПЦИОНАЛЬНЫЕ и явно записать частичное заполнение (только `sales`,
    остальные отделы — на Этапах 4–7). Без этой правки схема (опциональные поля) молча противоречила
    бы доку (skeptic-ревью плана, N2).
- Out of scope: любой UI (Step 20); `visual`-поле; контент для 4 других отделов.
- Acceptance criteria:
  1. Схема валидирует `sales` с новыми полями и остальные 4 отдела без них.
  2. Инвариант «5 отделов, 5 пар боль→выгода» не нарушен.
  3. Битый `ProcessStep` (нет `label`/`description`, неверный `status`) отклоняется тестом.
  4. `docs/12`/`docs/06` отражают частичное закрытие без ложного «заполнены все отделы».
- Verification: `npm run format:check`, `lint`, `typecheck`, `test`, `build`.
- Risks: соблазн заполнить все 5 отделов — mitigation: AC ограничивает Sales. Текст черновой —
  помечается редактируемым, как `painPoints`.
- Rollback: `git revert`; изменения аддитивные.
- Ревьюер: frontend-architect (схема/опциональность), qa-reviewer (валидация).

---

## Step 20 — `BeforeAfterSequence` для «Продаж»: DOM/SVG-диаграмма [user-facing/motion]

- Status: `COMPLETED` (skeptic `PASS`, блокирующих нет. Правка аккордеона `flex:1`→`flex-shrink:0`
  подтверждена как исправление реального регресса и в scope; правка теста CTA сохраняет и усиливает
  сторож. 3 non-blocking вынесены на milestone ux/qa: CTA под сгибом на низких desktop (watch-item,
  не нарушение — внутренний скролл, видна с 1 кадра, Escape закрывает), возможное ужесточение
  reduced-motion-теста, смягчение «CRM»/абсолютов. Переход к Step 21 разрешён.)
- Objective: показать в экране «Продажи» трансформацию процесса «до/после» DOM/SVG-диаграммой,
  встроенной в экран отдела, не меняя тайминги принятого каскада.
- Dependencies: Step 19.
- In scope:
  - `src/components/departments/BeforeAfterSequence.tsx` (+css): семантический HTML/SVG (списки шагов
    «до» и «после», стрелки/маркеры — не Canvas); motion объясняет переход «было → стало»; оба
    состояния и их текст доступны без завершения анимации; ветка `prefers-reduced-motion` показывает
    оба состояния статично.
  - Интеграция в `DepartmentExperience.tsx` ТОЛЬКО для отдела с `beforeSteps` (обобщаемо; сегодня —
    Sales). Каскад блоков (Amendment 16) НЕ трогается: «до/после» встраивается вокруг него.
  - Тесты: unit (структура, reduced-motion, обобщаемость по данным), e2e `before-after.spec.ts`
    (оба состояния в DOM, клавиатура, смысл не только цветом).
  - `docs/07-motion-system.md`: раздел про «до/после» как motion-случай трансформации.
- Out of scope: калькулятор; графика Step 21; analytics (Step 25); фото-сцены; изменение таймингов
  каскада.
- Acceptance criteria:
  1. В «Продажах» видна диаграмма «до/после»; оба состояния и текст в accessible tree независимо от
     завершения анимации.
  2. При `prefers-reduced-motion: reduce` анимации нет, оба состояния читаемы.
  3. Смысл «до»/«после» — не только цветом (форма/подпись/структура).
  4. Инварианты укрытости кадра (Amendment 15/Step 18) не регрессируют; второй механизм рендера сцены
     не введён.
  5. Тайминги существующего каскада не изменены.
- Verification: `format:check`, `lint`, `typecheck`, `test`, `build`, `test:e2e` (серийно/пониженные
  воркеры — флак-митигация milestone).
- Manual: `npm run dev` → `?department=sales`, проверить «до/после» на Desktop/Tablet/Mobile, с
  reduced-motion и без; overview→sales не обнажает кадр.
- Risks: перегрузка экрана motion — mitigation: «не анимировать всё сразу»; конфликт с каскадом —
  mitigation: встраивание вокруг него, тайминги не трогаются.
- Rollback: `git revert`; интеграция изолирована условием наличия `beforeSteps`.
- Ревьюер: motion-engineer, ux-strategist, qa-reviewer (a11y).

- **Amendment 21 (2026-07-21) — окно «Выгоды для заказчика» вместо диаграммы «до/после».**
  - Status: `APPROVED` (прямой запрос пользователя + два выбора через уточняющие вопросы).
  - Reason: (1) диаграмма «до/после» делала экран отдела выше вьюпорта и давала вертикальную полосу
    прокрутки — нарушение принципа «одного окна» (без полос по горизонтали и вертикали во всём
    проекте, требование пользователя); (2) продукту нужнее короткое окно «что заказчик в итоге
    получает от автоматизации», единое на отдел, а не пошаговая трансформация процесса.
  - Previous scope (Step 20): `BeforeAfterSequence` — две колонки шагов «Сейчас»/«После автоматизации»
    из `beforeSteps`/`automationSteps`, рендер только у отдела с этими данными (Sales), анимированный
    коннектор-маршрут; тип `ProcessStep` в модели данных.
  - New scope (Amendment 21): `CustomerBenefits` («Выгоды для заказчика») — компактная карточка,
    ЕДИНАЯ для всех пяти отделов. Данные — `customerBenefits: string[]` (своё у каждого отдела, `[]`
    сейчас — текст наполняется на этапе копирайта). Появляется через **5 секунд** после открытия
    отдела **независимым отсчётом**, сбрасываемым при переходе в другой отдел; пункты печатаются по
    очереди (`TypedText`). `prefers-reduced-motion`: сразу и без печати. `BeforeAfterSequence.*`,
    `beforeSteps`/`automationSteps`, `ProcessStep`, их тесты и e2e `before-after.spec.ts` — удалены.
  - Impact: удаляет часть поставки Step 20 (диаграмма) и правит модель данных (`types.ts`/`schema.ts`/
    `data/departments.json`), доки `03/05/06/07/09/12`, тесты (unit `customer-benefits`,
    `department-experience`, `departments`, `invalid-fixtures`; e2e `customer-benefits.spec.ts`).
    Каскад болей/кнопок (0/1/2/3 с) НЕ трогается — у окна собственный отсчёт. Экран отдела на 1440×900
    помещается без прокрутки (проверено e2e). Инварианты сцены/каскада не затронуты.
  - New acceptance criteria: (1) окно единое во всех отделах; (2) появляется через 5 с независимо от
    каскада и отсчитывается заново при смене отдела; (3) печать по очереди, полный текст пункта в
    accessible tree с первого кадра; (4) `reduced-motion` — сразу, без печати; (5) экран отдела без
    вертикальной/горизонтальной прокрутки на 1440×900; (6) критический путь (заголовок/боли/CTA/
    «Закрыть») от окна не зависит.
  - User approval: **получено** 2026-07-21 — исходный запрос + выбор «Список пунктов-выгод» и «Полностью
    заменить на „Выгоды“» через уточняющие вопросы.

- **Amendment 22 (2026-07-21) — окно «Ваша выгода»: текст заголовка, позиция и задержка.**
  - Status: `APPROVED` (прямой запрос пользователя).
  - Reason: доводка только что принятого окна (Amendment 21) по трём пунктам.
  - Previous scope (Amendment 21): заголовок «Выгоды для заказчика»; окно между болями и блоком
    кнопок; появление через 5 секунд.
  - New scope (Amendment 22): (1) заголовок → **«Ваша выгода»**; (2) окно перенесено **НИЖЕ блока
    кнопок** (под CTA «Получить разбор…» и «Закрыть») в `DepartmentExperience`; (3) задержка появления
    **5 с → 10 с** (`REVEAL_DELAY_MS` в `CustomerBenefits.tsx`).
  - Impact: правки в `CustomerBenefits.tsx` (заголовок, константа), `DepartmentExperience.tsx`
    (порядок JSX), тесты (unit `customer-benefits`, `department-experience`; e2e `customer-benefits`:
    имя окна, таймауты 8с→13с, тайминги/названия), доки `03/05/06/07/12`. Прочее поведение
    (единство, независимый отсчёт, сброс при смене отдела, reduced-motion, отсутствие прокрутки) без
    изменений.
  - User approval: **получено** 2026-07-21.

---

## Step 20.5 — Визуальный редизайн главной по референсу: «хаос → офис» [user-facing]

- Status: `COMPLETED` (2026-07-22; skeptic `PASS`, блокирующих замечаний нет).
- Objective: привести первый экран главной к предоставленному референсу, не меняя утверждённый текст
  и не ломая существующий сценарий hero → интерактивный офис.
- In scope:
  - трёхчастная desktop-композиция: текст слева, объединённый визуал «хаос → офис» в центре,
    тёмные информационные карточки справа;
  - исходное фото `references/office-overview/01-company-overview.png` сохраняется в правой части
    композита; левая часть генерируется как офисный процессный хаос;
  - мягкий прозрачный переход, дымка и тёплая диагональная световая граница между состояниями;
  - текущие заголовки, подзаголовок, пункты, CTA, статистика, источники и ссылки сохраняются;
  - текущая машина состояний, интерактивный overview и экраны отделов не перестраиваются;
  - адаптивная раскладка, keyboard/focus и `prefers-reduced-motion` сохраняются.
- Out of scope: изменение бизнес-копии, архитектуры state machine, контента отделов и экранов после
  входа в интерактивный офис.
- Acceptance criteria:
  1. На desktop первый экран визуально повторяет референс по пропорциям, тёмным карточкам и композиции.
  2. Визуал читается как единое изображение «хаос слева → организованный офис справа», без жёсткого
     прямоугольного стыка; исходный офис узнаваем.
  3. Весь существующий текст главной остаётся доступен в DOM без подмены копией референса.
  4. Обе CTA и вход в интерактивный офис работают как до редизайна; отделы после входа доступны.
  5. На tablet/mobile блоки складываются без обрезки текста и зависимости от hover.
  6. Нет новых ошибок lint/typecheck/unit/build; browser review выполнен на desktop и mobile.
- Verification: `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run test`,
  `npm run build`; выборочные e2e главной; browser review 1680×900 и 390×844.
- Risks: вес нового hero-ассета и контраст текста на фото — mitigation: оптимизированные производные,
  градиентный scrim и живой HTML-текст поверх изображения.
- Rollback: удалить новый hero-композит/компонент и вернуть прежнюю трёхкомпонентную hero-сетку.
- User approval: получено 2026-07-22 прямым «Приступай» после уточнения перехода и прозрачности.
- Follow-up 2026-07-22 — `COMPLETED` (skeptic `PASS`): по прямому запросу пользователя удалены пять декоративных hero-подписей
  отделов (`Поддержка`, `Продажи`, `Логистика`, `Дирекция`, `HR`), поскольку их позиции не
  соответствовали помещениям на фото. Композит, переход и интерактивная карта после входа не
  изменяются.

### Amendment 25 — follow-up главной: ясный оффер автоматизации и ИИ (2026-07-22)

- Status: `COMPLETED` (skeptic round 2 `PASS`, блокирующих замечаний нет). Reason: завершённый Step 20.5 хорошо показывает проблему, но не объясняет услугу
  за первые 3–5 секунд; три тёмные карточки конкурируют с оффером. Previous scope: сохранённая копия и
  тяжёлая правая колонка. New scope: переработать **только homepage hero/header** и связанные
  `data`/стили/unit/e2e; прямое ТЗ пользователя является утверждением этой поправки.
- Scope/impact: desktop-сетка ориентировочно 36–40% / 38–44% / 20–24% (продающий блок / существующий
  композит «хаос → офис» / вторичная колонка). Сохранить логотип, палитру, офисное изображение,
  навигацию «Как мы работаем» / «О нас» / «Блог», office overview, страницы отделов, машину состояний,
  маршруты и обработчики. Не менять контент/поведение overview и department screens.
- Обязательная копия слева: eyebrow **«АВТОМАТИЗАЦИЯ БИЗНЕС-ПРОЦЕССОВ С ИИ»**; единственный `h1`
  **«Автоматизируем бизнес-процессы и внедряем ИИ — чтобы компания работала быстрее и без лишней
  ручной работы»** (медным выделить только «работала быстрее» ИЛИ «без лишней ручной работы»);
  подзаголовок **«Находим, где теряются заявки, время и деньги. Проектируем и внедряем решения для
  продаж, поддержки, HR, логистики, документов и управления.»**; маркеры **«Быстрее обработка
  заявок»**, **«Меньше ручной работы»**, **«Меньше ошибок и потерь»**, **«Больше контроля»**.
- CTA: **«Получить разбор процессов»** и header **«Обсудить автоматизацию»** используют один
  существующий коммерческий сценарий («Ваша задача»/contact/Telegram); **«Посмотреть карту
  процессов»** переиспользует действие/маршрут существующего **«Исследовать офис»**. Подпись:
  **«Покажем, что стоит автоматизировать в первую очередь — без технического жаргона.»** Центровой
  слоган header «Помогаем экономить ДЕНЬГИ и ВРЕМЯ» удалить.
- Визуал: оставить ровно три короткие проблемные метки **«Заявки теряются»**, **«Данные переносят
  вручную»**, **«Отчёты собирают вручную»** и тонкий маршрут **«Заявка → CRM → задача сотруднику →
  ответ клиенту»**; спокойный motion с полноценной статичной `prefers-reduced-motion`-веткой.
  Правую колонку заменить двумя компактными карточками: **«Что мы автоматизируем»** — «Заявки и CRM»,
  «Поддержку и коммуникации», «Документы и согласования», «Отчёты и контроль», «ИИ-ассистентов и базы
  знаний»; **«Что получает бизнес»** — «Быстрее первый ответ», «Меньше повторяющихся операций»,
  «Меньше потерянных задач и заявок», «Прозрачные процессы для руководителя». Без внутренних
  scrollbar, неподтверждённых цифр/кейсов и исследований на первом экране.
- Acceptance: (1) за 3–5 секунд ясны услуга, ИИ, функции, результат и два следующих шага; (2) около
  1920×920 hero полноэкранный без document/internal scroll, правая колонка визуально слабее левой;
  (3) responsive проверен на 1920/1440/1280/1024/768/390, tablet/mobile идут left → visual → cards,
  без горизонтального scroll/обрезки изображения; (4) один `h1`, семантические controls, видимые
  hover/focus-visible/active, контраст/alt, без CLS; (5) все CTA сохраняют существующие рабочие
  маршруты/обработчики, overview и отделы не регрессируют; (6) reduced motion и keyboard path работают.
- Verification: `npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run test`,
  `npm run build`, релевантный `npm run test:e2e`; browser review на всех шести ширинах, включая
  геометрию/scroll 1920×920 и CTA → существующие сценарии.
- Risks/rollback: риск переполнения точной длинной копией и перегруза motion — решать адаптивной
  типографикой/компактностью и статичной веткой; rollback ограничен homepage hero/header, их данными,
  тестами и стилями. User approval: получено 2026-07-22 прямым ТЗ «сразу внеси изменения».

### Amendment 26 — цельная многослойная hero-композиция (2026-07-22)

- Status: `COMPLETED` (собственный skeptic-review по прямому ТЗ пользователя: `PASS`, блокирующих
  замечаний нет). Reason: пользователь подтвердил, что функционально верный Amendment 25
  визуально остался тремя жёсткими колонками. Прямое ТЗ утверждает follow-up без нового плана.
- Scope: только homepage hero/header, их copy, CSS и проверки. Офисный композит должен проходить под
  левым текстом и правыми карточками, растворяться в тёплом фоне масками/градиентами и сохранять
  существующие Telegram/`ACTIVATE_CTA`, DOM-семантику, overview и экраны отделов.
- Acceptance: точные новые H1/подзаголовок/маркеры/CTA; H1 до 4 строк на широком экране; слоистый
  desktop без вертикальных швов; спокойные reduced-motion-safe микроанимации; адаптив 1920/1440/1280/
  1024/768/390 без horizontal/internal scroll; полный quality gate, homepage+a11y e2e и скриншоты.
- Verification: `npm run format`, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`,
  целевые production e2e и визуальный review всех шести размеров. User approval: прямое ТЗ
  2026-07-22 «сначала изучи, затем сразу внеси изменения».

### Amendment 27 — облегчение и точечная полировка homepage hero (2026-07-23)

- Status: `COMPLETED` (skeptic round 2 `PASS`, blocking findings: нет).
  Reason: после Amendment 26 hero остаётся перегружен нижней flow-панелью, problem markers, плотной
  левой дымкой, широкой переходной зоной и тяжёлыми карточками.
- In scope: только homepage hero. Полностью удалить из DOM/CSS нижнюю `.flow`-панель и
  `.chaosNotes` с их анимациями, aria-группами, props и ставшими ненужными
  `visualProblemPoints`/`visualProcessSteps` в data/schema/types/tests; замену не добавлять.
  Перебалансировать освободившийся низ. В `OfficeMachine.module.css`/`HeroOfficeVisual.module.css`
  ослабить левую светлую вуаль на 15–25%, сохранив контраст текста и различимую глубину офиса;
  сузить светло-размытую область диагонального перехода на 20–35%, сохранив линию и мягкое
  свечение. В `HeroInfoPanel.module.css` уменьшить плотность фона, рамки и иконок, сделать тень
  мягче, blur немного сильнее, добавить воздуха и деликатно сместить верхнюю карточку вправо,
  нижнюю влево.
- Out of scope: любая новая плашка/подпись; изменения `Header`, `HeroCopy`, видимой copy и CTA,
  DOM-порядка верхнего уровня, маршрутов, Telegram, `ACTIVATE_CTA`, office overview, отделов и их
  логики; новые библиотеки, данные, обещания или переработка изображения/экрана целиком.
- Acceptance: (1) flow-панели и problem markers нет ни визуально, ни в DOM; пустого нижнего зазора
  нет; (2) слева видны архитектура, силуэты и глубина, при этом H1/CTA уверенно читаются; (3)
  диагональная зона заметно уже и не стала резким швом; (4) карточки легче, читаемы и вторичны
  относительно H1/CTA, смещение спокойное; (5) смысл «хаос → порядок» и цельная премиальная
  композиция сохранены; (6) на 1920×920, 1440×900, 1280×800, 1024×768, 768×1024 и 390×844 нет
  horizontal/internal scroll и обрезки; (7) copy/header/Telegram/CTA/keyboard/reduced-motion/
  overview/departments работают без регрессий; итоговые screenshots сохранены.
- Verification: `npm run format`, `npm run lint`, `npm run typecheck`, `npm run test`,
  `npm run build`; целевые e2e `homepage-offer`, `accessibility-scan`,
  `reduced-motion-and-fallback`, `office-overview`; browser review и screenshots всех шести
  размеров, проверка hero/header CTA.
- Risks/rollback: ослабление вуали может снизить контраст, прозрачные карточки — потерять
  читаемость, удаление блоков — нарушить баланс/старые тесты. Контроль — визуальное сравнение всех
  размеров, accessibility и geometry checks. Rollback ограничен hero-компонентами/CSS, удалёнными
  content-полями и связанными тестами; офис и отделы не затрагиваются.

### Amendment 28 — проектные сценарии и редакционный P.S. в homepage hero (2026-07-23)

- Status: `COMPLETED` (skeptic round 2: `PASS`).
- Approval: прямое ТЗ пользователя 2026-07-23; изменение закрытого hero оформлено отдельным
  Amendment без переписывания Amendment 27.
- In scope: сохранить Header, H1, подзаголовок, четыре преимущества, офисный visual, вторичный CTA,
  Telegram и `ACTIVATE_CTA`; изменить основной CTA на «Получить бесплатный разбор процессов»;
  заменить две правые карточки одной полупрозрачной панелью «ПРОЕКТНЫЕ СЦЕНАРИИ» с тремя честно
  маркированными расчётными эффектами; добавить под ней кликабельный редакционный P.S. на Telegram;
  ослабить свечение/контраст диагонали на 10–15%; обновить content schema/types/docs/tests.
- Out of scope: новые библиотеки, формы, popup, кейсы/отзывы/внешняя статистика, новые problem
  markers/flow-цепочки, изменение маршрутов, state machine, overview или экранов отделов.
- Acceptance: все три сценария и маркировка расчётного эффекта видны без hover/внутреннего scroll;
  P.S. — ссылка с доступным именем и `focus-visible`; desktop держит панель справа поверх сцены,
  tablet/mobile — порядок copy → visual → scenarios → P.S.; на 1920×920 нет document scroll, на всех
  шести обязательных ширинах нет horizontal scroll, обрезки и наложений; reduced motion сохраняет
  статичный контент; production build, целевые e2e/a11y и итоговые PNG проходят.
- Verification: formatter, lint, typecheck, unit, production build; homepage/overview/reduced-motion/
  accessibility e2e; локальный браузерный geometry/CTA/CLS probe и PNG 1920×920, 1440×900,
  1280×800, 1024×768, 768×1024, 390×844; обязательный skeptic review.

---

### Amendment 29 — облегчение редакционного P.S. и приглушение диагонали (2026-07-23)

- Status: `COMPLETED` (skeptic round 2: `PASS`).
- Approval: прямое ТЗ пользователя 2026-07-23; follow-up к закрытому Amendment 28 оформлен отдельным
  Amendment.
- Objective / scope: только homepage hero: превратить существующую Telegram-ссылку P.S. в
  компактную прозрачную редакционную строку с медной линией через CSS-псевдоэлемент, меткой `P.S.`,
  неизменным текстом и маленькой стрелкой `→` сразу после текста; сохранить доступное имя,
  `hover`/`focus-visible`/`active` и отключить движение при reduced motion. В диагональном overlay
  снизить воспринимаемую интенсивность на 5–8%, не меняя stops, геометрию, ширину, положение и
  оттенок. Точечно обновить связанные unit/e2e-контракты.
- Out of scope: любые изменения панели «ПРОЕКТНЫЕ СЦЕНАРИИ», её размеров, структуры, текстов и
  чисел; copy/data, Header, H1, преимущества, CTA/Telegram/`ACTIVATE_CTA`, офисное изображение,
  белая дымка, ширина переходной зоны, маршруты, overview и отделы; новые блоки, библиотеки или
  анимации.
- Acceptance: P.S. остаётся одной доступной ссылкой на Telegram, сохраняет точные текст и
  aria-label, не имеет периметральной рамки, плотной заливки, тяжёлой тени или круглой кнопки;
  медная линия слева и стрелка после текста сохраняются при переносе; desktop по возможности
  однострочный; состояния клавиатуры/мыши/нажатия различимы, reduced motion статичен. Диагональ
  визуально спокойнее на 5–8%, но переход «хаос → порядок» читается; сценарии, CTA и первый экран
  без иных изменений, CLS, обрезки, внутренних или горизонтальных scroll.
- Verification: Prettier, lint, typecheck, unit, production build; homepage, accessibility,
  reduced-motion и overview e2e; production screenshots и geometry/CTA/CLS probe на 1920×920,
  1440×900, 1280×800, 1024×768, 768×1024 и 390×844 в `test-results/hero-postscript`; обязательный
  skeptic-review.
- Risks / rollback: прозрачный P.S. может потерять контраст на фото, перенос — разнести стрелку или
  вызвать overflow, приглушение — стереть границу перехода. Контроль — токены дизайн-системы,
  focus/contrast и шесть визуальных размеров. Rollback ограничен HeroInfoPanel, диагональными
  альфами HeroOfficeVisual и связанными тестами.

---

### Amendment 30 — общая шапка, крупный overview и служебные маршруты (2026-07-23)

- Status: `COMPLETED` (финальный skeptic review: `PASS`, 2026-07-23).
- Reason: общий Header дублирует Telegram-контакт вместо альтернативного канала, overview теряет
  высоту из-за отдельной строки инструкции, а публичные «Контакты»/«Вход» отсутствуют.
- Protected scope: hero, проектные сценарии, Telegram `contactHref`, `ACTIVATE_CTA`, фото офиса,
  координаты зон, corner markers, подписи/hover/focus/touch отделов, edge mask и экраны отделов не
  меняются.

#### Step 30.1 — единый Header и телефон

- Status: `COMPLETED`.
- Objective: заменить дублирующий Telegram CTA в общей шапке на доступный телефон и добавить полную
  публичную навигацию.
- In scope: общий `Header`; data/schema/types; ссылки «Контакты» (`/contacts`) и «Вход» (`/login`);
  `tel:+79375346575`, видимый номер `+7 (937) 534-65-75`, доступное имя из ТЗ; адаптивное
  disclosure-menu с touch targets ≥44px, Escape/focus-visible/active и без horizontal overflow.
- Out of scope: новые коммерческие обещания, изменение hero/Telegram CTA, реализация login.
- Dependencies: нет.
- Acceptance: один Header используется в homepage/overview/department states; старые
  «Обсудить автоматизацию» и его Telegram href отсутствуют из Header; desktop показывает пять
  ссылок и телефон, mobile сохраняет доступный номер и все ссылки.
- Verification: unit Header/content, `format:check`, `lint`, `typecheck`, relevant e2e; manual
  keyboard/touch/overflow at 1024/768/390.
- Risks / rollback: переполнение шапки и потеря state-machine возврата; откат ограничен Header и
  header content contract. Reviewer: frontend-quality + accessibility.

#### Step 30.2 — строка управления и геометрия overview

- Status: `COMPLETED`.
- Objective: вернуть сцене роль главного визуального объекта, не меняя интерактивные зоны.
- In scope: единая control row `← На главную` / `Выберите отдел` / Telegram CTA; удалить
  `instructionHint`; CSS-only увеличение высоты/центральной доли сцены; основной side copy сделать
  на 5–10% темнее.
- Out of scope: фото, `object-fit`/`object-position`, zone data/inline coordinates, corner markers,
  labels, hover/focus/touch/reduced-motion, edge mask, смысл/объём/позиция side copy.
- Dependencies: Step 30.1.
- Acceptance: desktop control row одноуровневая и центрирована по сцене; mobile сохраняет логический
  порядок; запрещённая подпись отсутствует; сцена выше baseline; все 5 зон полностью видны и
  совпадают с фото; на 1920×920 нет document scroll/CLS; контраст side copy AA.
- Verification: unit/e2e overview + protected-diff check + geometry/CLS/overflow probe and visual
  screenshots at 1920×920, 1440×900, 1280×800, 1024×768, 768×1024, 390×844.
- Risks / rollback: узкие side columns, переносы control row, обрезка сцены; откат ограничен
  overview markup/content/CSS. Reviewer: allqbit-art-direction + allqbit-product-ux.

#### Step 30.3 — `/contacts`, `/login` и финальная приёмка

- Status: `COMPLETED` (финальный skeptic review: `PASS`).
- Objective: дать новым ссылкам честные рабочие destination pages и подтвердить весь запрос.
- In scope: общий layout/Header для `/contacts` и `/login`; точный copy/links из ТЗ; responsive
  project-style placeholders; semantic `h1`, route metadata/title и `noindex`; unit/e2e/axe,
  production build, полный e2e и итоговые screenshots.
- Out of scope: login form/auth/API/accounts/admin route; выдуманные contact details.
- Dependencies: Steps 30.1–30.2.
- Acceptance: оба маршрута 200, используют общий Header, имеют по одному `h1`, правильный title и
  noindex; phone/Telegram/back links рабочие; все заявленные viewport без horizontal overflow,
  обрезки и новых console/a11y errors; все пять отделов, CTA, tel, mobile menu и возврат работают.
- Verification: `npm run format`, `format:check`, `lint`, `typecheck`, `test`, `build`, полный
  `test:e2e`; browser QA + final capture. Screenshots: `artifacts/office-overview/`.
- Risks / rollback: metadata/router regression; удалить route group/pages и вернуть navigation
  entries, не затрагивая office state machine. Reviewer: skeptic + frontend-quality + browser-qa.

#### Amendment 30A — усиление измеримых AC после skeptic plan review (2026-07-23)

- Status: `COMPLETED` (все усиленные AC подтверждены; финальный skeptic review: `PASS`).
- Reason: skeptic plan round 1 `FAIL` — Amendment 30 не закрепил все измеримые и визуальные
  ограничения исходного запроса.
- Old scope: «сцена выше baseline», общий responsive/header/route review без явных container-scroll,
  semantic-link и design-skill доказательств.
- New scope:
  1. Step 30.2 обязан записать before/after высоту сцены и доступный вертикальный бюджет на
     1920×920; сцена использует максимально возможную высоту без изменения 3:2/`object-fit` и без
     потери side copy. На desktop 1920×920, 1440×900 и 1280×800 у `.office`, `.stageRow`,
     `.officeCenter`, обеих story columns нет overflow (`scrollHeight === clientHeight` с допуском
     округления), текст не обрезан, document scroll отсутствует.
  2. «Выберите отдел» сохраняет маленький медный маркер, не имеет карточки/плотной подложки,
     остаётся спокойной вторичной инструкцией и визуально не конкурирует с Telegram CTA.
  3. Header phone, `/contacts`, `/login` и возвраты реализуются семантическими `<a>`/`Link`;
     state-machine reset в homepage может оставаться семантической кнопкой как действие текущего
     экрана. Точный порядок nav: «Как мы работаем», «О нас», «Блог», «Контакты», «Вход»; публичного
     «Админ» нет.
  4. Единый Header проверяется в hero, overview, department/task states, `/contacts` и `/login`.
  5. Обязательные design/review доказательства: `allqbit-art-direction`, `allqbit-product-ux`,
     `ui-ux-pro-max` и финальный строгий UI/UX + B2B-маркетинг review.
- Impact: production scope не расширен; уточняются только acceptance evidence и уже требуемая
  семантика/визуальная иерархия. В финальном отчёте указывается абсолютное и процентное изменение
  высоты сцены.
- Verification impact: geometry probe дополняется container overflow/text clipping checks,
  semantic element assertions и exact nav-order assertion; WORKLOG хранит отдельные exit status
  всех обязательных команд.

---

## Step 21 — Полная графика сцены «Продаж»: линии передачи данных / маркеры [user-facing/motion]

- Status: `PROPOSED` (первая реализация ОТКАЧЕНА по прямому решению пользователя 2026-07-22 —
  результат не принят. Оверлей маршрутов `SceneRouteOverlay`/`sceneRoutes.ts`, интеграция в
  `OfficeExperience`, правка `docs/02` и тесты удалены/восстановлены из HEAD; журналы возвращены.
  Подход к шагу пересматривается до повторного старта — см. `DECISIONS.md` 2026-07-22.)
- Objective: довести сцену «Продажи» до «полной» (`docs/13`) — графическая система маршрутов/линий
  передачи данных на сцене (продолжение Step 14; OQ-A2-5 отложил «полные линии» на Этап 3).
- Dependencies: Step 14 (графическая система логотипа), Step 20.
- In scope:
  - `src/components/graphics/*` (обобщаемый data-driven компонент линий/маркеров), интеграция в
    сцен-слой/экран «Продажи»; motion объясняет движение/маршрут/статус; reduced-motion ветка;
    SVG/DOM, не Canvas.
  - Тесты: unit (обобщаемость, reduced-motion), e2e (присутствие/доступность, нет autoplay-звука).
  - `docs/02-art-direction.md`: графическая система, пометка «предварительно, черновой арт».
- Out of scope: финальный арт (черновой per OQ-A2-7); графика других отделов.
- Acceptance criteria:
  1. На сцене «Продажи» есть графическая система маршрутов; она data-driven (обобщаема), не хардкод.
  2. `prefers-reduced-motion` отключает движение, смысл сохраняется статикой.
  3. Нет автозапуска звука; графика не блокирует критический контент и CTA.
  4. Анимация на трансформациях/opacity, без layout thrashing.
- Verification: `format:check`, `lint`, `typecheck`, `test`, `build`, `test:e2e`.
- Manual: `npm run dev` → `?department=sales`, визуал Desktop/Tablet/Mobile и reduced-motion; нет
  рывков (DevTools Performance).
- Risks: перегрузка сцены/«анимировать всё сразу» — mitigation: motion объясняет, а не украшает;
  черновой арт изменится — mitigation: data-driven, помечено предварительным.
- Rollback: `git revert`; графика аддитивна.
- Ревьюер: motion-engineer, ux-strategist, frontend-architect (perf).

---

## Step 22 — Движок калькулятора экономии (чистый модуль + формула) ❌ ОТМЕНЁН

- Status: `CANCELLED` (Amendment 23, решение пользователя 2026-07-22 — калькулятор исключён из пилота).
- ~~**ГЕЙТ: OQ-3-C**~~ снят вместе с шагом. Описание ниже сохранено как запись на случай возврата.
- Objective: чистый расчётный модуль: использует ВВОД пользователя, возвращает результат ВМЕСТЕ с
  разложением формулы и пометкой «предварительно». Без UI.
- Dependencies: Step 2. Разблокируется OQ-3-C.
- In scope:
  - `src/features/savings-calculator/{types,calculate}.ts` (чистая функция): входы по `docs/12`
    (кол-во операций, длительность, частота, стоимость часа, доля автоматизации, период) → результат
    + машиночитаемое разложение формулы + строка disclaimer.
  - Контент-конфиг для «Продаж» (значения по умолчанию, подписи, текст формулы, disclaimer) в `data/`
    + схема/типы.
  - Unit-тесты: расчёт зависит от входов; граничные входы; формула ↔ результат совпадают; результат
    НЕ выдаёт гарантированную выручку/деньги как факт (`CLAUDE.md` Copy — только «предварительно»).
- Out of scope: UI (Step 23); analytics; калькулятор других отделов.
- Acceptance criteria:
  1. Расчёт — чистая функция входов; детерминирован; изменение входа меняет результат.
  2. Возвращается разложение формулы (не только число) и disclaimer «предварительно».
  3. Модуль изолирован (не тянет клиентских зависимостей), обобщаем по данным отдела.
  4. Тесты покрывают граничные входы и «формула ↔ результат».
- Verification: `format:check`, `lint`, `typecheck`, `test`, `build`.
- Risks: формула молча обещает деньги как факт — mitigation: AC2 + disclaimer + ревью qa; выбор
  формулы без пользователя — mitigation: гейт OQ-3-C.
- Rollback: `git revert`; модуль изолирован.
- Ревьюер: frontend-architect (изоляция/обобщаемость), qa-reviewer (Copy/disclaimer).

---

## Step 23 — UI калькулятора: панель внутри экрана «Продаж» (client) [user-facing] ❌ ОТМЕНЁН

- Status: `CANCELLED` (Amendment 23, 2026-07-22 — калькулятор исключён из пилота). Описание ниже —
  запись на случай возврата.
- Objective: клиентская ПАНЕЛЬ калькулятора в экране «Продажи»: поля ввода, ПОКАЗ формулы, метка
  «предварительно», доступная и достижимая, не в Canvas.
- Dependencies: Step 22.
- In scope:
  - `src/features/savings-calculator/SavingsCalculator.tsx` (+css): семантические input/label/форма;
    вывод формулы и disclaimer видимы; клавиатура/фокус; смысл не только цветом; reduced-motion.
  - Интеграция как РАСКРЫВАЮЩАЯСЯ ПАНЕЛЬ/блок в `DepartmentExperience.tsx` (решение 3). Реестр
    разделов, `OfficeSectionId`, `url-sync` НЕ трогаются — калькулятор остаётся частью экрана отдела.
  - Тесты: unit (рендер, ввод меняет вывод, disclaimer присутствует), e2e (ввод → пересчёт → видимая
    формула; axe без serious/critical).
- Out of scope: движок (Step 22); analytics-события (Step 25); отдельный раздел/модаль; другие отделы.
- Acceptance criteria:
  1. Результат считается по ВВОДУ пользователя; изменение поля меняет результат в UI.
  2. Формула видима; результат помечен «предварительно».
  3. Достижим клавиатурой, фокус видим, смысл не только цветом; axe 0 serious/critical.
  4. Не в Canvas; state machine и реестр не затронуты (панель, не раздел).
- Verification: `format:check`, `lint`, `typecheck`, `test`, `build`, `test:e2e`.
- Manual: `npm run dev` → «Продажи» → ввести значения, проверить пересчёт, видимость формулы и
  disclaimer, клавиатурный проход, reduced-motion.
- Risks: калькулятор обещает экономию как факт — mitigation: AC2, ревью qa; панель ломает высоту
  экрана на низком desktop — mitigation: раскрывающийся блок, проверка высоты как в Step 12.7.
- Rollback: `git revert`.
- Ревьюер: frontend-architect (размещение), qa-reviewer (a11y/Copy), ux-strategist.

---

## Step 24 — Слой аналитики: типизированный `trackEvent` + транспорт ❌ ОТМЕНЁН/ОТЛОЖЕН

- Status: `CANCELLED` (Amendment 23, 2026-07-22 — аналитика отложена, из пилота исключена; `docs/15`
  остаётся будущей спецификацией). Описание ниже — запись на случай возврата.
- **ГЕЙТ: OQ-3-A — снят (Amendment 23).** Историческая запись описания: словарь событий и параметров
  задан утверждённым `docs/15-analytics-conversion.md` (источник истины для этого шага, если он будет
  возвращён; правка после skeptic-ревью плана, находка B1).
- Objective: тонкий, приватный по умолчанию слой аналитики (без cookie, без PII, без третьих сторон
  по умолчанию), реализующий словарь `docs/15`, не притягивая consent (consent — Этап 8).
- Dependencies: нет кодовых. Разблокируется OQ-3-A.
- In scope:
  - `src/features/analytics/*`: типизированный `trackEvent(name, payload)`, где имена событий и
    параметры взяты из `docs/15` (события пилота: `homepage_view`, `hero_primary_cta_click`,
    `office_interaction_start`, `department_preview`, `department_open`, `department_switch`,
    `before_after_start`, `before_after_complete`, `contact_open`, `lead_submit`,
    `lead_submit_success`, `lead_submit_error`; `diagnostic_*` — Этап 8, не пилот). Параметры из
    `docs/15`: `department, inputMode, motionMode, viewportCategory, source, campaign,
    entryDepartment, timeToFirstDepartment, timeToLead`. Транспорт — по OQ-3-A.
  - Опорная инструментовка как доказательство слоя: `department_open`, `contact_open` (по `docs/15`,
    без PII).
  - Тесты: unit (событие формируется/не шлёт PII), e2e (перехват транспорта на опорном событии).
  - При first-party транспорте: `src/app/api/analytics/route.ts` (без cookie, без PII —
    `docs/15`: «Не отправлять чувствительные данные»).
  - Краткая запись в `docs/09` (раздел «Открытые решения: analytics» закрывается для пилота — ссылка
    на `docs/15` как на действующий словарь).
- Out of scope: consent-баннер (Этап 8); дашборды; `diagnostic_*` (Этап 8); инструментовка до/после и
  калькулятора (Step 25).
- Acceptance criteria:
  1. `trackEvent` типобезопасен; РЕЕСТР имён и параметров совпадает с `docs/15` (лишних/переименованных
     имён нет; отклонение от `docs/15` допустимо только как явная реконсиляция дока в этом шаге).
  2. По умолчанию нет cookie и PII; secret/токен (если есть) не в клиентском бандле.
  3. Опорные события наблюдаемы в e2e через перехват транспорта.
  4. Слой не требует consent для пилота и это обосновано (cookieless/no-PII); транспорт с cookie
     блокируется до Этапа 8 (OQ-3-A).
- Verification: `format:check`, `lint`, `typecheck`, `test`, `build`, `test:e2e`.
- Risks: аналитика тянет consent раньше времени — mitigation: cookieless/no-PII, OQ-3-A; секрет в
  клиенте — mitigation: серверный транспорт, AC2.
- Rollback: `git revert`; слой аддитивен.
- Ревьюер: frontend-architect (транспорт/бандл), qa-reviewer (privacy/PII).

---

## Step 25 — Инструментовка событий пилота «Продажи» (по `docs/15`) ❌ ОТМЕНЁН/ОТЛОЖЕН

- Status: `CANCELLED` (Amendment 23, 2026-07-22 — без слоя Step 24 инструментовать нечего; отложено).
  Описание ниже — запись на случай возврата.
- Objective: разметить взаимодействия пилота событиями словаря `docs/15` через слой Step 24,
  обобщаемо (параметр `department` в payload), без изменения поведения UI.
- Dependencies: Step 20 (до/после), Step 23 (калькулятор), Step 24 (слой). **Конверсионное событие —
  Step 26** (см. ниже): что именно испускать на CTA, зависит от разрешения OQ-3-D, поэтому событие
  конверсии инструментуется в Step 26, а не здесь (правка после skeptic-ревью плана, N1).
- In scope (имена строго из `docs/15`):
  - `DepartmentNavigationRail`/машина — `department_switch`;
  - `BeforeAfterSequence` — `before_after_start`, `before_after_complete`;
  - параметры событий из `docs/15` (`department, motionMode, viewportCategory, source, …`), payload
    несёт `department` (обобщаемость на Этапы 4–7).
  - **Калькулятор: события `docs/15` НЕ содержит.** Если событие расчёта нужно — оно добавляется как
    ЯВНАЯ реконсиляция `docs/15` в этом шаге (имя в стиле словаря, напр. `calculator_estimate`), с
    правкой `docs/15`, а не выдумывается молча (тот же принцип, что реконсиляция `docs/12` в Step 19).
    По умолчанию, если польза не обоснована, событие калькулятора НЕ вводится.
  - e2e через перехват транспорта.
- Out of scope: событие конверсии/CTA (Step 26); события вне `docs/15` без явной реконсиляции;
  `diagnostic_*` (Этап 8); изменение UI; другие отделы.
- Acceptance criteria:
  1. Каждое инструментованное событие носит имя из `docs/15` (или явно реконсилировано с правкой
     `docs/15`); испускается ровно один раз на действие; payload содержит `department`.
  2. Инструментовка не меняет видимое поведение и не ломает a11y/фокус.
  3. e2e подтверждает события; чувствительные данные не передаются (`docs/15`).
- Verification: `format:check`, `lint`, `typecheck`, `test`, `build`, `test:e2e`.
- Risks: двойные/пропущенные события — mitigation: e2e на счётчик; событие по завершению действия, не
  по кадру; расхождение с `docs/15` — mitigation: AC1 (имена сверяются со словарём).
- Rollback: `git revert`; правки точечные.
- Ревьюер: frontend-architect, qa-reviewer.

---

## Step 26 — CTA пилота «Продажи» и выравнивание lead-capture [user-facing]

- Status: `PROPOSED` (гейт снят — шаг можно начинать).
- **OQ-3-D РАЗРЕШЁН (Amendment 23) — вариант D-a:** адресат заявок = тот же Telegram, что у формы
  «Ваша задача» (`contactHref` + маршрут `/api/task`). On-site lead-capture в пилоте НЕ делаем.
- Objective: назначение CTA пилота — единый Telegram-адресат (как у «Ваша задача»), без зависимости от
  закрытия формы и без обещания контакта, который не собирается.
- Dependencies: нет (OQ-3-D разрешён). НЕ зависит от закрытия Step 12.7.
- In scope (вариант D-a, Amendment 23):
  - Все заявки/CTA пилота ведут в тот же Telegram, что и форма «Ваша задача» (единый `contactHref` +
    маршрут `/api/task`); отдельного on-site lead-capture нет.
  - Копия CTA/подтверждения приведена в соответствие с тем, что реально происходит (переход в Telegram),
    без обещаний выручки/автономии.
  - Конверсионное событие аналитики НЕ инструментуется (аналитика отложена — Step 24/25, Amendment 23).
  - Тесты (unit/e2e): CTA ведёт к единому Telegram-адресату; axe без serious/critical.
- Out of scope: доработка формы «Ваша задача»; CRM/хранение заявок (Этап 8); лид-захват других отделов;
  аналитика конверсии (отложена).
- Acceptance criteria:
  1. CTA пилота ведёт в тот же Telegram, что и «Ваша задача»; функционирует независимо от статуса Step 12.7.
  2. Копия не обещает того, что не собирается/не делается.
  3. Путь достижим клавиатурой, фокус виден, axe 0 serious/critical.
- Verification: `format:check`, `lint`, `typecheck`, `test`, `build`, `test:e2e`.
- Risks: скрытая зависимость от формы — mitigation: AC1; обещание неполученного контакта —
  mitigation: AC2 + ревью qa/ux.
- Rollback: `git revert`.
- Ревьюер: ux-strategist (копия/обещания), qa-reviewer (a11y), frontend-architect.

---

## Step 27 — Отдача кадра на ширинах ≥1584px (потолок каталога + `scene-dolly`) [performance]

- Status: `PROPOSED`
- Objective: закрыть недоотдачу кадра на ≥1584px (кадр отдела 1872 на 1920 против потолка 1536) и
  смежное расхождение `scene-dolly` (кадр до 1.10× вёрсточной ширины, не учтён в `sizes`).
- Dependencies: нет апстрим. Требует локальных оригиналов `references/**` для регенерации.
- In scope:
  - `scripts/office-scenes.config.mjs`: добавить ширину 1920 в `SCENE_WIDTHS` (OQ-A2-4 допускал
    «+1920 при нужде»).
  - Регенерация `npm run assets:images` (перегенерирует производные И каталог через `writeCatalog`).
    Каталог руками НЕ править.
  - `src/components/office/departmentPhotos.ts`: пересмотреть `OFFICE_SCENE_SIZES` с учётом
    `scene-dolly` 1.10× и новой ширины.
  - Тесты: усилить `responsive-scene-framing.spec.ts` сторожем на ≥1584 (проверен на способность
    падать).
- Out of scope: флак `scene-transition.spec.ts` (Этап 10); новый арт.
- Acceptance criteria:
  1. На ≥1584px (в т.ч. 1920) кадр отдела получает производную не уже фактического кадра; сторож
     проверен на способность падать до правки.
  2. Изменение `SCENE_WIDTHS` прошло через конфиг + `npm run assets:images`; `officeScenes.ts` не
     редактирован руками (diff сгенерированного согласован с конфигом).
  3. Инварианты укрытости кадра (Amendment 15/Step 18) не регрессируют.
  4. `sizes` не отдаёт широкую производную узким вьюпортам (бюджет изображений не раздут).
- Verification: `npm run assets:images`, `git diff` (сверка сгенерированного), `format:check`, `lint`,
  `typecheck`, `test`, `build`, `test:e2e`.
- Manual: `npm run dev` на 1920×1080 → «Продажи», DevTools Network: грузится 1920-производная, не 1536.
- Risks: рост веса — mitigation: `sizes` не отдаёт широкую узким; недетерминизм sharp — mitigation:
  сверять НАБОР файлов, не байты (Step 10 AC1).
- Rollback: `git revert`.
- Ревьюер: frontend-architect (performance/пайплайн), qa-reviewer.

---

## Step 28 — Ленивая загрузка и пауза оффскрин-анимации пилотных ассетов [performance]

- Status: `PROPOSED`
- Objective: тяжёлые части пилота (графика сцены Step 21, ассеты сцены) не грузятся и не анимируются,
  пока не нужны (`CLAUDE.md` Performance rules). (Калькулятор исключён — Amendment 23; «до/после»
  заменено окном «Ваша выгода», Amendment 21.)
- Dependencies: Step 20, Step 21.
- In scope: ленивое подключение оправданно тяжёлого (графика сцены; dynamic import где оправдано);
  пауза несущественной анимации при скрытии/`VISIBILITY_CHANGE`; нет React-обновлений на каждый pointer
  move; трансформации/opacity вместо layout thrashing. Тесты/замеры.
- Out of scope: изменение поведения фич; perf других отделов.
- Acceptance criteria:
  1. Тяжёлые ассеты (графика сцены) не запрашиваются, пока пользователь их не достиг.
  2. Несущественная анимация приостанавливается при скрытии.
  3. Нет layout thrashing и state-обновлений на каждый pointer move.
  4. Функциональность и a11y не деградируют от ленивости (fallback/скелет корректен).
- Verification: `format:check`, `lint`, `typecheck`, `test`, `build`, `test:e2e`.
- Manual: DevTools Network/Performance — отложенная загрузка и пауза анимации при скрытии;
  reduced-motion.
- Risks: ленивость ломает SSR/первый кадр — mitigation: полезный HTML сразу, скелет; чрезмерная
  фрагментация — mitigation: делить только оправданно тяжёлое.
- Rollback: `git revert`.
- Ревьюер: frontend-architect (performance), qa-reviewer.

---

## Step 29 — Консолидация accessibility и reduced-motion пилота «Продажи» [user-facing/motion]

- Status: `PROPOSED`
- Objective: единая проверка a11y и reduced-motion для СОБРАННОГО пилота (сцена + графика + окно «Ваша
  выгода» + CTA), закрывающая требование Этапа 3 «accessibility». (Калькулятор исключён — Amendment 23.)
- Dependencies: Steps 20, 21, 26.
- In scope: расширить `accessibility-scan.spec.ts` и reduced-motion-тесты на полный пилот. Полный
  чек-лист `docs/14` «Accessibility»: клавиатурный путь, видимый фокус, КОРРЕКТНЫЙ ВОЗВРАТ фокуса,
  **screen reader states** (объявляемые статусы динамических блоков — окно «Ваша выгода»), контраст,
  **zoom 200%**, смысл не только цветом, Escape закрывает, нет Canvas без HTML-эквивалента, нет
  autoplay-звука; критический контент не зависит от завершения анимации; исправить найденные
  a11y-дефекты пилотных компонентов. (screen reader states + zoom 200% дописаны по skeptic-ревью
  плана, N3 — `docs/14:33`.)
- Out of scope: a11y других отделов; consent (Этап 8); визуальная регрессия (Этап 10).
- Acceptance criteria:
  1. axe 0 serious/critical на полном пилоте (Desktop + Mobile).
  2. Весь пилот проходится клавиатурой, фокус виден и корректно возвращается, Escape закрывает, смысл
     не только цветом.
  3. При reduced-motion весь пилот функционален, критический контент доступен без анимации.
  4. Нет autoplay-звука; нет Canvas без HTML-эквивалента.
  5. Динамические статусы (окно «Ваша выгода» появилось/текст напечатан) объявляются скринридеру; при
     zoom 200% пилот остаётся работоспособным без потери контента (`docs/14`).
- Verification: `format:check`, `lint`, `typecheck`, `test`, `build`, `test:e2e`.
- Manual: клавиатурный проход всего пилота; reduced-motion; фокус и Escape; zoom 200% (браузер);
  проверка объявляемых статусов (снимок accessibility tree / screen reader).
- Risks: a11y-дефект найден поздно — mitigation: каждый UI-шаг несёт свои a11y-AC, здесь консолидация;
  ложное «зелёное» axe (не проверяет контраст текста поверх фото) — mitigation: ручная проверка
  контраста композита скрима (прецедент Amendment 8).
- Rollback: `git revert`.
- Ревьюер: qa-reviewer (a11y), ux-strategist, motion-engineer (reduced-motion).

---

## Закрытие milestone Этап 3

По завершении Step 29 (до старта Этапа 4) — milestone review по `CLAUDE.md` §6: `frontend-architect`,
`qa-reviewer`, `ux-strategist`, `motion-engineer` (пилот целиком user-facing и motion-heavy). Свести
находки в `WORKLOG.md`. НЕ переходить к Этапу 4 при остающемся Blocker/Critical. Отдельно проверить
главную цель milestone: механизмы (окно «Ваша выгода», графика сцены) обобщаемы и готовы к переносу на
Этапы 4–7, а не разовые под «Продажи». (Калькулятор и аналитика исключены из пилота — Amendment 23.)

---

## Amendment 31 — бережная премиальная доработка страницы отделов (2026-07-23)

- Status: `APPROVED` (прямое подробное ТЗ пользователя от 2026-07-23).
- Reason: восстановить исходную одноэкранную композицию отдела после неудачной переработки и повысить
  визуальное качество без нового макета.
- Scope:
  - удалить `BeforeAfterSequence` из рабочего экрана отдела, сохранив сам независимый компонент;
  - сохранить геометрию «rail слева → заголовок сверху → боли слева / решение справа → результат
    снизу»;
  - заменить центральную кнопку «Закрыть» компактной кнопкой «← Назад к офису» над rail;
  - добавить обобщаемый `CustomerBenefits`/«Результат для бизнеса» с зарезервированным местом,
    задержкой 10 секунд и CTA, появляющейся последней;
  - унифицировать лёгкие полупрозрачные поверхности, бронзовые active/focus-состояния и
    transform/opacity-only motion;
  - адаптировать печать решения по длине текста, максимум около 2 секунд, с cleanup таймеров;
  - убрать page/internal scroll на desktop 1366×768, 1440×900, 1763×864, 1920×1080.
- Out of scope: новый макет, глобальная шапка, маршруты, новые тяжёлые библиотеки, переписывание
  контента, другие страницы, удаление данных `beforeSteps`/`automationSteps`.
- Files expected:
  - `src/components/departments/DepartmentExperience.tsx/.module.css`;
  - `src/components/departments/PainGainPanel.tsx/.module.css`;
  - `src/components/departments/DepartmentCopy.module.css`;
  - `src/components/departments/DepartmentCTA.module.css`;
  - `src/components/departments/CustomerBenefits.tsx/.module.css` (new);
  - `src/components/office/OfficeExperience.tsx/.module.css`;
  - `src/components/office/DepartmentNavigationRail.module.css`;
  - scoped unit/e2e tests and live journals.
- Acceptance:
  1. Desktop document and all main containers have no scroll overflow at the four required sizes.
  2. All five departments and all five pain→solution pairs switch without mixed text or stale timers.
  3. «Результат для бизнеса» appears after 10 seconds in reserved space; CTA appears last; reduced
     motion shows complete content immediately.
  4. «Закрыть» is absent; «Назад к офису» uses the existing close/overview route and has visible
     focus.
  5. Layout does not shift while typing/revealing, background photo remains materially visible,
     console has no application errors.
- Verification: `format:check`, `lint`, `typecheck`, focused unit/e2e, `test`, `build`; Playwright
  screenshots/DOM measurements at required desktop viewports; final `git diff`.
- Rollback: revert only Amendment 31 paths; pre-existing dirty worktree is preserved.

### Step 31 — premium one-screen departments

- Status: `COMPLETED`.
- Dependencies: current restored department components (clean relative to `HEAD`); current shared
  Header/office shell from Amendment 30 is preserved.
- Reviewer: mandatory read-only skeptic after local checks.

---

## Amendment 32 — визуальная полировка отделов и реальный импульс «боль → решение» (2026-07-23)

- Status: `APPROVED` (прямое подробное ТЗ пользователя от 2026-07-23).
- Reason: Amendment 31 восстановил одноэкранную композицию, но оставил статичный центральный
  коннектор, слишком однотонные поверхности и печать решения, начинающуюся раньше требуемого
  пространственного импульса.
- Scope:
  - сохранить текущие DOM-порядок и геометрию «rail → заголовок → боли / решение → результат + CTA»;
  - унифицировать существующие тёплые полупрозрачные поверхности, локальные scrim-градиенты,
    типографику и active/hover/focus-состояния без изменения контента;
  - добавить плавно перемещающийся бронзовый индикатор активного пункта rail;
  - заменить статичный коннектор измеряемым SVG-импульсом от реально выбранной строки к левой границе
    решения; пересчитывать путь при resize, отменять предыдущий запуск и не обновлять React state на
    каждом кадре;
  - синхронизировать входящий отклик карточки, печать и исчезающий курсор; reduced motion показывает
    актуальный текст сразу без направленного движения;
  - сохранить 10-секундную задержку результата, его зарезервированную высоту и CTA последней.
- Out of scope: тексты/data, изображения и их mapping, Header, state machine, URL/history/routes,
  другие страницы, перестановка/увеличение блоков, mobile accordion logic, тяжёлые библиотеки,
  Canvas/WebGL и циклические эффекты.
- Files expected:
  - `src/components/departments/PainGainPanel.tsx/.module.css`;
  - `src/components/departments/PainSolutionImpulse.tsx/.module.css` (new);
  - `src/components/departments/TypedText.tsx/.module.css`;
  - `src/components/departments/DepartmentCopy.module.css`;
  - `src/components/departments/DepartmentExperience.module.css`;
  - `src/components/departments/CustomerBenefits.tsx/.module.css`;
  - `src/components/departments/DepartmentCTA.module.css`;
  - `src/components/office/DepartmentNavigationRail.tsx/.module.css`;
  - `src/components/office/OfficeExperience.module.css`;
  - scoped unit/e2e tests and live journals.

### Step 32 — premium surfaces + interruptible pain→solution impulse

- Status: `COMPLETED`.
- Dependencies: `COMPLETED` Amendment 31; existing dirty worktree is preserved.
- Acceptance:
  1. Content/Header/photo mapping/state-machine/URL control hashes remain unchanged.
  2. Composition and DOM order are unchanged; principal boxes at 1763×864 move no more than 4px.
  3. All five departments at 1366×768, 1440×900, 1763×864 and 1920×1080 have no document/internal
     overflow; result/CTA/all pains/full text remain visible and the photo remains materially visible.
  4. Rail keeps `aria-current`, hidden status and a non-colour marker; one bronze indicator moves in
     350–500ms without changing rail height; active thumbnail ≤1.03, hover ≤1.02 / 3px.
  5. Re-selecting the current pain does not restart motion; another pain starts exactly one 500–800ms
     impulse from its real right edge to the solution card, with path recomputed on resize.
  6. Rapid A→B→C leaves one current run; only C may highlight/type, with no mixed text or stale timer.
  7. Typing starts after the current impulse reaches the card; border/light-wave/cursor are one-shot,
     transform/opacity-only and cause no layout shift.
  8. Reduced motion renders no moving impulse or typing sequence and exposes the full current text
     immediately.
  9. Result timing/height/order and CTA href/label remain unchanged; console/page errors are absent.
  10. Post-implementation designer critique and mandatory independent read-only skeptic return no
      blocking finding; Step becomes `COMPLETED` only after skeptic `PASS`.
- Verification: focused unit/e2e; `npm run format:check`; `npm run lint`; `npm run typecheck`;
  `npm run test`; `npm run build`; `npm run test:e2e -- --workers=1`; browser screenshots/DOM
  measurements at the four desktop viewports; `git diff --check`; final control hashes and scoped diff.
- Risks: dirty-worktree overlap (touch only listed hunks); pulse/typing/resize race (monotonic run id,
  cleanup timers/observer, current-run guards); layout thrash (batched DOM reads, SVG/CSS animation);
  photo suppression (no full-stage dark overlay).
- Rollback: reverse only Amendment 32 hunks and delete only its new pulse files; do not restore from
  `HEAD` or touch earlier dirty changes.
- Reviewer: `ui-ux-pro-max` + `frontend-design` before/after; mandatory read-only skeptic after checks.

---

## Amendment 33 — точечная иерархия поверхностей и интерактивность «Ваша задача» (2026-07-24)

- Status: `APPROVED` (прямое подробное ТЗ пользователя от 2026-07-24).
- Reason: текущая композиция и поведение сохраняются, но близкие по тону поверхности слабо
  различаются, мелкий текст нижнего блока требует большей читаемости, а неактивный пункт rail
  «Ваша задача» недостаточно явно воспринимается как действие.
- Scope:
  - сделать `DepartmentCopy` едва светлее, `painCard` спокойнее, `gain` слегка теплее, а
    `CustomerBenefits` немного глубже только через прозрачность, внутренний блик, границу и тень;
  - минимально поднять кегль/контраст/межстрочность существующего текста `CustomerBenefits`, сохранив
    его высоту, сетку и порядок «основной результат → дополнительные эффекты → CTA»;
  - оформить только неактивный пункт «Ваша задача» тонким контуром, CSS-стрелкой справа и
    сдержанным hover/focus-откликом, визуально слабее активного отдела.
- Out of scope: DOM/композиция/сетка, размеры и положение основных блоков, тексты, CTA, Header,
  фотографии, data, маршруты/state machine, pain-selection, таймеры, импульс/typing/reveal-motion,
  новые крупные элементы, новые цвета и любые scroll-контейнеры.
- Expected files:
  - `src/components/departments/DepartmentCopy.module.css`;
  - `src/components/departments/PainGainPanel.module.css`;
  - `src/components/departments/CustomerBenefits.module.css`;
  - `src/components/office/DepartmentNavigationRail.module.css`;
  - scoped e2e assertions and live journals.

### Step 33 — minimal surface/readability/task-item polish

- Status: `COMPLETED`.
- Dependencies: `COMPLETED` Amendment 32; preserve the pre-existing dirty worktree.
- Acceptance:
  1. Protected hashes for content/Header/photos/state/URL/rail markup/pain-motion remain unchanged.
  2. DOM order, grid tracks and principal boxes are preserved within 4px in the 5 departments ×
     1366×768, 1440×900, 1763×864 and 1920×1080 matrix; result height does not grow or move.
  3. Copy is slightly lighter, pain neutral, gain subtly warmer and result slightly deeper without
     a palette change, heavy plaque, suppressed photo or overload.
  4. Bottom copy is more readable while all text, three-part grid, CTA and reveal order stay
     unchanged and fully fit without internal/page scroll.
  5. Only inactive «Ваша задача» gets a decorative right arrow, thin contour and hover/focus
     affordance; arrow motion is ≤2px transform-only, target remains 44px, active department wins.
  6. Logic, impulse/typing/timers and reduced motion are unchanged; console/page errors are absent.
  7. Post-implementation designer critique and mandatory read-only skeptic have no blocking finding.
- Verification: baseline/final screenshots, computed styles/line counts/DOMRects and 5×4 overflow
  matrix; protected hashes; focused e2e; format, lint, typecheck, unit, build, full e2e;
  `git diff --check` and scoped diff.
- Risks: typography wrap at 1366; pseudo-arrow min-content width; over-visible surface deltas;
  dirty-worktree overlap. Mitigation: sub-pixel/single-step type changes, existing internal spacing
  only, CSS scoping under `.taskItem`, 4px geometry gate, no protected file edits.
- Rollback: reverse only Amendment 33 CSS/test/journal hunks; never restore from `HEAD`.

---

## Amendment 34 — дословная замена контента пяти отделов (2026-07-24)

- Status: `APPROVED` (прямое подробное ТЗ пользователя от 2026-07-24).
- Reason: заменить текущий контент страницы отделов единственным контрольным источником —
  приложенным файлом `Вставленный текст(8).txt` — без редакторских изменений и без изменения
  существующей визуальной/интерактивной модели.
- Scope:
  - перенести текст дословно, исключая только служебные метки, разделители и исходные маркеры списка;
  - установить порядок `support → sales → logistics → hr → executive`;
  - сохранить для каждого отдела headline, subtitle, ровно 5 упорядоченных пар
    «Что происходит сейчас»/«После внедрения», основной результат и 3 дополнительных;
  - добавить `PainPoint.howItWorks?: string` только для сценариев, где источник содержит
    `Как работает:`, и показать его компактно после решения на desktop/mobile;
  - сохранить CTA labels/href, фото, rail/back, state/URL, композицию, импульс, печать,
    reveal-delay и reduced-motion;
  - удалить прежний контент, не подтверждённый новым источником;
  - менять типографику/внутренние отступы только по результатам измеренного переполнения; обрезание,
    ellipsis и новые scroll-контейнеры запрещены.
- Out of scope: редизайн, рефакторинг общей архитектуры, отдельные компоненты на отдел, изменение CTA,
  изображений, маршрутов, тайминга результата, других страниц и исходного вложения.
- Assumptions/risks:
  - `problem` соответствует исходному «Подзаголовку»;
  - `customerBenefits` хранит `[основной, дополнительный 1, дополнительный 2, дополнительный 3]`;
  - прежние `beforeSteps`/`automationSteps` удаляются как неподтверждённый и неиспользуемый экраном
    текст;
  - длинные строки могут потребовать минимальной responsive-типографики после DOM-замеров.
- Rollback: отменить только hunks Amendment 34; не восстанавливать файлы из `HEAD` и не затрагивать
  прежний dirty worktree.

### Step 34 — verbatim department data + optional «Как работает»

- Status: `COMPLETED` (skeptic round 2: `PASS`; blocking findings: none).
- Dependencies: `COMPLETED` Amendment 33; источник доступен только для чтения.
- Acceptance:
  1. Порядок данных и rail: Поддержка, Продажи, Логистика, HR, Дирекция.
  2. Перенесены 5 headline, 5 subtitle, 25 проблем, 25 решений, 5 support-only `howItWorks`,
     5 основных и 15 дополнительных результатов без редакторских изменений.
  3. Схема требует ровно 5 сценариев и ровно 4 результата; `howItWorks` опционален и отсутствует
     во всех сценариях вне поддержки.
  4. UI показывает основной результат и все три дополнительных; CTA и 10-секундный reveal не
     изменены.
  5. `howItWorks` появляется после решения на desktop/mobile; существующие typing/impulse/reduced
     motion не смешивают тексты при переключении.
  6. На 1366×768, 1440×900, 1763×864 и 1920×1080 нет page/internal overflow, обрезания, ellipsis
     или наложений; весь требуемый контент доступен.
  7. Геометрия, фото, rail/back, CTA, state/URL и другие страницы не изменены.
  8. Обязательный read-only skeptic возвращает `PASS`.
- Verification: точечные unit/component/e2e проверки; `format:check`, `lint`, `typecheck`, `test`,
  `build`, `test:e2e -- --workers=1`; браузерные DOM-замеры и screenshots; `git diff --check`.

---

## Amendment 35 — последовательная загрузка терминала сцены «Анализ» (2026-07-24)

- Status: `APPROVED` (прямое ТЗ пользователя от 2026-07-24).
- Reason: текущая печать терминала начинается одновременно с загрузочной заставкой и нарушает
  требуемую последовательность Windows → QBit → текст.
- Scope:
  - сохранить существующую загрузочную стилистику и порядок двух логотипов;
  - запускать появление терминала и печать только после полного завершения логотипа QBit;
  - уменьшить текущие интервалы печати символов и удаления опечаток на 10%;
  - сохранить reduced-motion: полный текст сразу, без заставки и печати;
  - не менять контент, композицию, навигацию и остальные сцены.
- Acceptance:
  1. В сцене «Анализ» Windows появляется раньше QBit; видимый терминал и первый печатаемый символ
     появляются только после полного ухода QBit.
  2. Интервалы обычных символов и удаления/ввода опечаток равны 90% от текущих значений.
  3. Повторный вход в сцену корректно перезапускает последовательность без stale-таймеров.
  4. Reduced motion сразу показывает полный текст и не показывает логотипы.
  5. Typecheck, focused tests, build, browser verification и read-only skeptic проходят.
- Risks: рассинхронизация CSS/JS таймингов; stale timeout при быстром переключении сцен.
- Rollback: отменить только hunks Amendment 35; не затрагивать прежний dirty worktree.

### Step 35 — analysis boot sequence + 10% faster typing

- Status: `COMPLETED` (skeptic: `PASS`; blocking findings: none).
- Dependencies: текущая реализация страницы «Как мы работаем».
- Reviewer: обязательный независимый read-only skeptic после локальных и браузерных проверок.

---

## Amendment 36 — полная навигация шапки с пунктом «Главная» (2026-07-24)

- Status: `APPROVED` (прямое ТЗ пользователя от 2026-07-24).
- Reason: в шапке отсутствует явная ссылка «Главная», а состав и порядок пунктов не соответствует
  требуемому.
- Scope:
  - установить порядок: «Главная», «О нас», «Стоимость», «Блог», «FAQ», «Контакты», «Вход»;
  - «Главная» ведёт на `/`; на homepage дополнительно вызывает существующий возврат state machine
    к hero;
  - «О нас» ведёт на существующую страницу `/how-we-work`, «Вход» — на `/login`;
  - «Стоимость», «Блог», «FAQ», «Контакты» остаются безопасными заглушками `href="#"`;
  - сохранить существующий desktop/mobile дизайн, disclosure, focus и Escape.
- Acceptance:
  1. Все семь пунктов отображаются в точном порядке на всех страницах с общей шапкой.
  2. «Главная» с внутренних страниц открывает `/`, а на homepage возвращает первый экран.
  3. «О нас» и «Вход» ведут на существующие маршруты; четыре незаполненных пункта не меняют URL.
  4. Mobile menu закрывается после рабочей навигации и остаётся открытым для заглушки.
  5. Focus, типы, тесты, сборка, browser verification и read-only skeptic проходят.
- Risks: семь пунктов могут переполнить промежуточную desktop-ширину; same-route `/` может не
  сбросить homepage state без явного callback.
- Rollback: отменить только hunks Amendment 36; не затрагивать прежний dirty worktree.

### Step 36 — header navigation

- Status: `COMPLETED` (skeptic: `PASS`; blocking findings: none).
- Dependencies: существующие `Header`, `heroLinks` и homepage state-machine callback.
- Reviewer: обязательный независимый read-only skeptic после проверок.

---

## Amendment 37 — переименование пункта «Стоимость» (2026-07-24)

- Status: `APPROVED` (прямое ТЗ пользователя от 2026-07-24).
- Scope: заменить только видимую подпись «Стоимость» на «Продукт и Стоимость» в общей шапке и
  синхронизировать документацию/тест; сохранить `href="#"`, порядок и поведение.
- Acceptance: новое название отображается на desktop/mobile и не создаёт переполнение шапки.
- Rollback: вернуть только подпись Amendment 37.

### Step 37 — header label

- Status: `COMPLETED` (skeptic: `PASS`; blocking findings: none).
- Reviewer: обязательный независимый read-only skeptic после проверок.

---

## Amendment 40 — реальные продукты, отдельные URL и SEO (2026-07-25)

- Status: `COMPLETED` (skeptic: `PASS`; blocking/non-blocking findings: none).
- User approval: прямое ТЗ пользователя от 2026-07-25.
- Scope:
  - сохранить текущие 10 зон, фото 01–10, приближение, левую навигацию и переключения;
  - создать единую конфигурацию «зона → продукт → URL → фото → точный текст → цена → layout → SEO»;
  - канонические URL `/products/{product-slug}`, редиректы со старых `/products/product-XX`;
  - четыре доступных состояния контента, общий P.S., мобильный последовательный поток;
  - metadata, canonical, Open Graph, ItemList, BreadcrumbList, Service/Offer/Organization, sitemap;
  - проверить все 10 цепочек, history/reload, адаптивность, console/overflow и соответствие цен.
- Out of scope: замена фотографий, изменение координат зон, редизайн отделов или крупная миграция.
- Risks: плотность точного текста в one-window desktop; overlap с объектами на разных кадрах;
  рассинхронизация URL/контента во время анимации.

### Step 40 — products content, routes, presentation and indexing

- Status: `COMPLETED`.
- Acceptance: требования вложенного ТЗ выполнены; проверки и read-only skeptic возвращают `PASS`.

---

## Amendment 41 — общая страница выбора продуктов в механике отделов (2026-07-25)

- Status: `COMPLETED` (skeptic round 2: `PASS`).
- User approval: прямое ТЗ пользователя от 2026-07-25.
- Scope:
  - изменить только overview `/products`, не меняя страницы отдельных продуктов;
  - сохранить лабораторное фото, десять физических зон, конфигурацию продуктов, URL, цены и
    существующий камерный переход;
  - заменить постоянные рамки, номера и большой tooltip на компактные метки в визуальной механике
    `DepartmentHotspot`: скрытая граница зоны, угловые маркеры только при hover/focus, раскрытие одной
    минимальной цены внутри метки;
  - задать индивидуальные позиции меток в существующей конфигурации продукта;
  - на mobile оставить одну выбранную метку на фото и компактную нижнюю панель настоящих ссылок;
  - обновить только продуктовые overview-тесты и провести browser/design/skeptic review.
- Acceptance:
  1. В покое видны десять непересекающихся компактных названий без рамок зон и номеров.
  2. Hover/focus показывает угловые направляющие физического объекта и одну минимальную цену без
     отдельного плавающего окна; reduced motion поддержан.
  3. Все десять ссылок сохраняют цепочку зона → продукт → цена → URL → фото → карточка.
  4. Mobile не накладывает десять меток: выбор в нижней панели показывает одну активную метку и
     цену, «Подробнее» открывает продукт.
  5. Страница отделов и страницы отдельных продуктов не изменены; проверки и read-only skeptic
     возвращают `PASS`.
- Risks: пересечения меток на промежуточных ширинах и потеря связи метки с физической зоной.
- Rollback: отменить только изменения Amendment 41 в product overview/config/tests.

### Step 41 — compact product markers and overview verification

- Status: `COMPLETED` (skeptic round 2: `PASS`).
- Reviewer: обязательный независимый read-only skeptic после локальных и браузерных проверок.

## Plan Amendment 42 — раздел «Документы» (2026-07-26)

- Reason: запрос пользователя — добавить в сайт раздел «Документы», открывающийся из шапки и
  визуально соответствующий разделу «Продукт и Стоимость».
- Old scope: раздела не существовало; в `heroLinks` было семь пунктов.
- New scope:
  - маршрут `/documents` (layout + page) по образцу `/products`: общий `Header`, одноэкранная
    оболочка `100dvh`, safe-area, `overflow: hidden`;
  - фотография `public/dox/dox.png` как фон рабочей области (`cover`, без blur, мягкий
    градиентный скрим, темнее слева — в зоне интерфейса, светлее справа снизу — в зоне подписанных
    архивных папок), плюс детерминированные производные AVIF/WebP;
  - функциональный каталог: слева список документов с категориями, справа предпросмотр (~60%
    высоты рабочей области) и информационная зона с кнопкой «Скачать документ»;
  - data-driven слой `src/features/documents/documents.ts` (тип `DocumentItem`, тестовые данные,
    `sortOrder`, `isPublished`, категории) и шов загрузки `useDocuments` под будущую админ-панель;
  - состояния загрузки, ошибки с «Повторить», пустого каталога, отсутствия предпросмотра;
  - на мобильном — два состояния: список и выбранный документ с возвратом.
- Impact: `data/homepage-copy.json` (+1 пункт навигации), `src/app/sitemap.ts` (+1 URL),
  `package.json` (+`assets:documents`), `docs/04-homepage-copy.md` (навигация). Существующие
  страницы, их тексты и общая структура проекта не менялись.
- Out of scope: реальные юридические документы, серверная генерация preview для офисных форматов,
  адресация конкретного документа в URL, админ-панель.
- User approval: получено (постановка задачи 2026-07-26).
- Rollback: удалить `src/app/documents/`, `src/features/documents/`, `public/dox/{previews,files}`,
  производные фона, тесты раздела и откатить четыре точечные правки выше.

### Step 42 — раздел «Документы»

- Status: `COMPLETED` (локальные проверки и браузерная приёмка пройдены; независимый skeptic по
  этой задаче не вызывался — пользователь потребовал реализацию сразу после анализа).

## Plan Amendment 43 — доставка заявки `/contacts` через существующий сценарий n8n (2026-07-29)

- Reason: запрос пользователя — форма обратной связи на `/contacts` не доходила до Telegram.
  Пользователь предоставил рабочий сценарий прежнего сайта «Qbit_sait/Webhook → Telegram
  notification» и потребовал завести передачу с нового сайта через него.
- Old scope: `POST /api/contact` слал в n8n только структурированные поля (`name`, `phone`,
  `telegram`, `process` и метаданные). Узел Telegram этого сценария читает `body.message`; при его
  отсутствии в чат уходила бы служебная заглушка «Webhook received in n8n» без данных заявки.
- New scope:
  - `src/features/contacts/contactMessage.ts` — сборка текста заявки для Telegram (без разметки:
    узел Telegram отправлен без `parse_mode`);
  - поле `message` в payload `/api/contact`; структурированные поля сохранены;
  - `chat_id` сайтом не передаётся — получатель остаётся настройкой n8n;
  - тесты: `contact-message.test.ts` (новый), контракт payload в `contact-route.test.ts`;
  - README и `.env.example` — точный вид production-адреса webhook и отсутствие проверки секрета
    в предоставленном сценарии.
- Impact: `src/app/api/contact/route.ts`, `src/features/contacts/contactMessage.ts`,
  два файла тестов, `README.md`, `.env.example`. Форма, схема валидации, страница и другие роуты не
  менялись.
- Out of scope: правки самого сценария n8n, проверка секрета внутри n8n, запись заявок в БД.
- User approval: получено (постановка задачи 2026-07-29).
- Rollback: убрать `message` из payload и удалить `contactMessage.ts` с его тестом.

### Step 43 — payload заявки под сценарий n8n → Telegram

- Status: `COMPLETED` (локальные проверки пройдены; независимый skeptic не вызывался — пользователь
  потребовал реализацию сразу, доставка не проверена вживую до задания
  `N8N_CONTACT_WEBHOOK_URL`).

## Plan Amendment SEO-01 — первый пакет технических SEO-исправлений (2026-07-29)

- Reason: запрос пользователя после внешнего снимка production
  (`allqbit-production-snapshot-2026-07-29.zip`). Снимок зафиксировал три подтверждённых дефекта
  разметки и две недоработки, перечисленные пользователем как «первый пакет».
- Old scope:
  - на карточке продукта `<h2>Обзор</h2>` (no-JS fallback) стоит в DOM ПЕРЕД `<h1>` продукта;
  - на `/how-we-work` четыре `<h3>` заметок корковой доски стоят в DOM ПЕРЕД единственным `<h1>`;
  - `sitemap.xml` проставляет `lastModified` только у `/faq`, `/blog` и статей;
  - description продуктов собирается как `summary` + цена и доходит до ~300 символов;
  - `/blog/<неизвестный-slug>` отдаёт 404 без `<h1>` и с общим `<title>`.
- New scope (один связный шаг `SEO-01`, пять правок):
  1. `ProductInformation.tsx` — перенос no-JS заголовка панели ПОСЛЕ `<header>` с `<h1>`.
  2. `HowWeWorkPage.tsx` + `HowWeWorkPage.module.css` — перенос `office-route-panel` в DOM выше
     слоёв поверхностей; порядок отрисовки сохраняется явными `z-index`, а не позицией в DOM.
  3. `sitemap.ts` + серверный слой — `lastModified` из настоящих `updated_at` базы.
  4. `products.ts` — отдельное SEO-описание 120–160 символов + тест длины.
  5. `src/app/blog/not-found.tsx` — сегментная 404 блога.
  6. Правка Caddy для `www → non-www` — только подготовлена текстом, на сервер НЕ применяется.
- Impact: `src/features/products/ProductInformation.tsx`,
  `src/features/products/ProductsExperience.module.css` (если потребуется),
  `src/features/how-we-work/HowWeWorkPage.tsx`, `src/features/how-we-work/HowWeWorkPage.module.css`,
  `src/app/sitemap.ts`, `src/server/content/*.ts`, `src/features/products/products.ts`,
  `src/app/blog/not-found.tsx`, тесты, `SEO_FIX_BATCH_01_REPORT.md`.
- Out of scope (прямой запрет пользователя): редиректы старых URL, изменение кеширования,
  оптимизация изображений и JS, подключение аналитики, изменение дизайна, деплой.
- Нумерация: `SEO-01`, а не «44». Номер 44 уже занят шагом «final research publication»
  (см. начало файла и запись в `WORKLOG.md` от 2026-07-25) — сквозная нумерация в этом проекте
  уже давала коллизии на 42 и 43, и ещё одна сделала бы журнал неоднозначным.
- Почему один шаг, а не пять: пользователь поставил пакет как единое задание, правки независимы и
  проверяются одним прогоном. Skeptic вызывается один раз по всему диффу — не пропускается.
- User approval: получено (постановка задачи 2026-07-29).
- Rollback: `git checkout -- ` по перечисленным файлам; `not-found.tsx` удалить.

### Step SEO-01 — первый пакет технических SEO-исправлений

- Acceptance criteria:
  - на каждой публичной странице ровно один `<h1>`, и он идёт в DOM раньше любых `<h2>`/`<h3>`;
  - визуальный результат не изменился (сверка скриншотов до/после);
  - `sitemap.xml` содержит `lastmod` только там, где есть настоящая дата из базы;
  - ни один product description не длиннее 160 символов, все не короче 120;
  - `/blog/<неизвестный-slug>` — 404, один `<h1>` «Статья не найдена», `noindex`;
  - правка Caddy показана, но не применена;
  - `typecheck`, `lint`, `test`, `build`, затронутые e2e — зелёные.
- Status: `COMPLETED` (skeptic PASS, раунд 3, 2026-07-29)

## Plan Amendment SEO-02 — устранение блокирующей ошибки линта (2026-07-29)

- Reason: запрос пользователя. `npm run lint` оставался красным одной предсуществующей ошибкой,
  и это блокировало деплой пакета `SEO-01`.
- Old scope: `HowWeWorkPage.tsx` держал готовность акцента в двух состояниях «номер готовой сцены»
  и сбрасывал их ТРЕМЯ синхронными `setState` в теле эффекта — каскадные рендеры,
  `react-hooks/set-state-in-effect`.
- New scope: только `HowWeWorkPage.tsx` — индекс сцены и номер её включения объединены в одно
  состояние `scene`; готовность сравнивается с токеном показа (`activation` + режим движения)
  вместо номера сцены; синхронные `setState` из эффекта убраны. Правило не отключается,
  `eslint-disable` не вводится.
- Деплой и применение Caddy вынесены в отдельный шаг `SEO-03`. Причина — находка независимого
  ревью: половина прежних критериев (редиректы, 23 адреса, заголовки в production) физически
  проверяема только ПОСЛЕ выкатки, и одним вердиктом такой шаг не закрывается. Объём работ при
  разделении не меняется: и правка линта, и деплой поручены пользователем в одной постановке.
- Out of scope (прямой запрет пользователя): оптимизация скорости, аналитика, редиректы старых URL,
  изменение кеширования, новые видимые тексты, изменения дизайна.
- User approval: получено (постановка задачи 2026-07-29).
- Rollback: `git checkout -- src/features/how-we-work/HowWeWorkPage.tsx`.

### Step SEO-02 — устранение ошибки линта

- Acceptance criteria:
  - `npm run lint` — exit 0, без `eslint-disable` и без правки конфигурации ESLint;
  - поведение и анимации `/how-we-work` не изменились, проверено на desktop и mobile,
    включая краевой случай возврата в сцену в окне между блокировкой перехода (1120 мс) и
    задержкой акцента (1500 мс);
  - весь quality gate зелёный: `format:check`, `lint`, `typecheck`, `test`, `build`, весь Playwright.
- Status: `COMPLETED` (skeptic PASS, 2026-07-29)

## Plan Amendment SEO-03 — деплой пакета и применение редиректа Caddy (2026-07-29)

- Reason: постановка пользователя от 2026-07-29 — «после успешной проверки задеплой SEO-пакет №1»
  и «примени редирект Caddy». Выделено из `SEO-02` по находке независимого ревью.
- Scope: выкатка на production кода шагов `SEO-01` и `SEO-02`; применение подготовленного
  редиректа `www → non-www` в `Caddyfile` через `caddy validate` + `caddy reload`.
- Out of scope (прямой запрет пользователя): оптимизация скорости, аналитика, редиректы старых URL,
  изменение кеширования, новые видимые тексты, изменения дизайна.
- User approval: получено (постановка задачи 2026-07-29).

### Состояние production ДО деплоя — опорные точки отката

| Что | Значение |
| --- | --- |
| commit | `469e90520c349cbb28d7338f3f8b7f5eb8413a04` |
| Docker image ID | `sha256:83a425ebc838757b74850dc91957f4c7b0909d7972d300afaa1fabd3c5e64c31` |
| образ собран | 2026-07-29T11:27:51+03:00 |
| контейнер | `a93f84b3e97f`, запущен 2026-07-29T08:33:02Z |
| BUILD_ID | `oEDuygj9UpGTJTR-B_0di` |
| `Caddyfile` sha256 | `7bf55465852f268cbc3f5a075248c23b130d75bb1e31a9da3278a031f92a8f61` |
| `Caddyfile` mtime | 2026-06-06 06:56:45 +0300 |
| свободно на `/opt` | 51 ГБ |

### Rollback (записан ДО выкатки)

Код:

```bash
cd /opt/allqbit
git reset --hard 469e90520c349cbb28d7338f3f8b7f5eb8413a04
docker tag sha256:83a425ebc838757b74850dc91957f4c7b0909d7972d300afaa1fabd3c5e64c31 allqbit-site:latest
docker compose up -d --force-recreate allqbit-site
```

Прежний образ НЕ удаляется при деплое — новый получает тот же тег `allqbit-site:latest`, старый
остаётся доступен по своему ID. Проверить наличие перед откатом: `docker images --no-trunc | grep 83a425eb`.

Caddy:

```bash
cp /opt/supabase/volumes/proxy/caddy/Caddyfile.bak-2026-07-29 /opt/supabase/volumes/proxy/caddy/Caddyfile
docker exec supabase-caddy caddy validate --config /etc/caddy/Caddyfile --adapter caddyfile
docker exec supabase-caddy caddy reload --config /etc/caddy/Caddyfile --adapter caddyfile
```

**Важно про общий прокси.** `supabase-caddy` обслуживает не только сайт, но и домены боевого
Supabase на этой же машине. Поэтому: правка только в блоке `allqbit.ru`, обязательный
`caddy validate` ДО применения, `reload` (без простоя) вместо `restart`, и резервная копия
`Caddyfile` создаётся до любой записи. Неверный конфиг положил бы и Supabase — это и есть главный
риск шага.

### Step SEO-03 — деплой и Caddy

- Acceptance criteria:
  - деплой прошёл, контейнер `healthy`, новый BUILD_ID отличается от `oEDuygj9UpGTJTR-B_0di`;
  - все четыре варианта `www`/`non-www` × `http`/`https` для `/test?x=1` заканчиваются на
    `https://allqbit.ru/test?x=1`; с `https://www` — ровно один переход;
  - 23 адреса `sitemap.xml` отвечают 200, canonical ведёт на non-www;
  - `sitemap.xml` и `robots.txt` открываются;
  - на карточках продуктов `H1` раньше `H2`, на `/how-we-work` `H1` раньше `H3`;
  - ошибок консоли в браузере нет, визуализация не изменилась;
  - домены Supabase продолжают отвечать после `reload`.
- Status: `COMPLETED` (skeptic PASS, 2026-07-29)

## Plan Amendment SEO-04 — пакет SEO №2: постоянные переадресации старых адресов (2026-07-29)

- Reason: постановка пользователя от 2026-07-29 — «внедри пакет SEO №2 — редиректы старых URL».
  Пакет №1 такие переадресации прямо исключал (`Out of scope` шага `SEO-03`); здесь запрет снят
  самим заказчиком, поэтому оформлено отдельной поправкой, а не правкой закрытого шага.
- Scope: пять постоянных переадресаций `/pricing`, `/integrations`,
  `/blog/ai-document-automation`, `/blog/lead-automation-telegram-crm`,
  `/blog/n8n-business-automation`; автоматические тесты на них; выкатка на production.
- Out of scope (прямой запрет пользователя): новые страницы, новые тексты, изменение дизайна,
  изменение любых существующих текстов. Отдельно запрещено трогать `/ai-assistant-for-business`,
  `/blog/ai-assistant-business-tasks`, `/blog/ai-agents-for-business`,
  `/blog/data-parser-automation`, `/blog/telegram-bot-dlya-biznesa`.
- User approval: получено (постановка задачи 2026-07-29).

### Step SEO-04 — таблица переадресаций, тесты, деплой

- Objective: старые адреса отвечают одним постоянным переходом на действующий адрес того же
  смысла, с сохранением query-параметров, без цепочек и без переадресации на главную.
- In scope: `src/lib/legacyRedirects.ts` (таблица + список исключений), `next.config.ts`
  (`redirects()`), `src/tests/unit/app/legacy-redirects.test.ts`,
  `src/tests/e2e/legacy-redirects.spec.ts`, журналы и отчёт о деплое.
- Acceptance criteria:
  1. Каждый из пяти адресов отвечает 301 или 308 с `Location` ровно на заказанный адрес.
  2. Query-параметры доживают до получателя без изменений.
  3. Получатель отвечает 200 без второго перехода (цепочки нет).
  4. Ни один получатель не равен `/`.
  5. Пять исключённых адресов не переадресуются (код ответа не 3xx).
  6. Дизайн и тексты не изменены: правки не касаются компонентов, стилей и контента.
  7. Зелёные `format:check`, `lint`, `typecheck`, `test`, `build`, весь Playwright.
  8. После деплоя каждый старый адрес проверен на production.
- Verification: локальный quality gate целиком + полный Playwright + ручной обход production.
- Risks: (а) шаблон в `source` мог бы захватить исключённый адрес — снято требованием точных путей
  и unit-тестом; (б) список фиксируется на сборке, а slug продукта/статьи редактируется в
  админ-панели — снято unit-тестом на существование получателя в исходных данных, остаточный риск
  записан в отчёте; (в) переадресация перекрыла бы существующую страницу — снято unit-тестом на
  отсутствие маршрута у источника.
- Rollback: удалить `redirects()` из `next.config.ts` (переадресации живут только в коде сайта,
  `Caddyfile` этим шагом не трогается) и пересобрать образ; опорные точки — в отчёте.
- Status: `AWAITING_SKEPTIC`

### Состояние production ДО деплоя SEO-04 — опорные точки отката

| Что                | Значение                                                                  |
| ------------------ | ------------------------------------------------------------------------- |
| commit             | `9c529e223adfff19f2073d9ac9903281af7e92f3`                                |
| Docker image ID    | `sha256:739b2443d436124dfad955b3557d478f27240db944d4738d2d075238d646e12b` |
| образ собран       | 2026-07-29T14:40:37Z                                                      |
| контейнер          | `eac33ca0d076`                                                            |
| BUILD_ID           | `5wVqZ4NpA_9bjL07DBxu0`                                                   |
| `Caddyfile` sha256 | `38c3b34b948cb0f8e55b923b2e025a6e761199916cdf14ad1f802dc3438a4e60`        |
| резервная копия    | `/opt/allqbit-data/backups/allqbit-20260729-193608.tar.gz` (68 КБ)        |
| свободно на `/opt` | 51 ГБ                                                                     |

`Caddyfile` этим шагом НЕ трогается: переадресации живут в образе сайта. Хеш записан, чтобы после
деплоя доказать, что конфигурация общего прокси (он же обслуживает боевой Supabase) не изменилась.

Rollback:

```bash
cd /opt/allqbit
git reset --hard 9c529e223adfff19f2073d9ac9903281af7e92f3
docker tag sha256:739b2443d436124dfad955b3557d478f27240db944d4738d2d075238d646e12b allqbit-site:latest
docker compose up -d --force-recreate allqbit-site
```
